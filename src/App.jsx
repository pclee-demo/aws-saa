import { useState } from "react";

const data = {
  networking: {
    tab: "🌐 Networking",
    items: [
      { name: "🌐 VPC Fundamentals", isHeader: true, color: "#2E86AB" },
      { name: "VPC", full:"Virtual Private Cloud", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏰", tagline: "Your private network in AWS", analogy: "Building your own walled city inside AWS — you control who gets in and out.", keyPoints: ["Isolated network environment per account/region", "Public subnets (internet-facing) vs private subnets", "Security Groups + NACLs for layered firewalling"], when: "Always — every AWS resource lives inside a VPC", pricing: "VPC free. Charges for NAT Gateway, VPN, VPC Peering data transfer." },
      { name: "Public Subnet", full: "Public Subnet", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌐🏘️", tagline: "Subnet with a route to the internet", analogy: "The front of house — resources here have a public entrance and can be reached directly from the internet.", keyPoints: ["Route table has 0.0.0.0/0 pointing to an Internet Gateway", "Resources can be assigned public IPs", "Where you place load balancers, bastion hosts, NAT Gateways"], when: "Resources that must be reachable from the internet: ALBs, bastion hosts, NAT Gateways", pricing: "Free. You pay for the resources inside and data transfer." },
      { name: "Private Subnet", full: "Private Subnet", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒🏘️", tagline: "Subnet with no direct internet route", analogy: "The back office — resources here are invisible to the internet. Outbound traffic goes via NAT Gateway; inbound only from within the VPC.", keyPoints: ["No route to Internet Gateway in its route table", "Outbound internet via NAT Gateway placed in a public subnet", "Where you place app servers, databases, Lambda-in-VPC"], when: "Any resource that should not be publicly reachable: EC2 app servers, RDS, ECS tasks", pricing: "Free. NAT Gateway for outbound internet costs ~$0.045/hr + $0.045/GB." },
      { name: "Internet Gateway", full: "Internet Gateway (IGW)", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌐🚪", tagline: "Allows internet traffic in/out of VPC", analogy: "The main front door of your VPC — without it, nothing inside can talk to the internet and nothing from the internet can reach you.", keyPoints: ["Attached to the VPC itself — NOT to a subnet. One IGW per VPC. It sits at the VPC boundary, not inside any subnet", "A subnet becomes 'public' by having a route table entry pointing 0.0.0.0/0 to the IGW — the IGW doesn't make a subnet public on its own", "Required for public subnets to have internet access", "Horizontally scaled, highly available — no bandwidth limit, no single point of failure"], when: "Any resource in a public subnet that needs internet access (EC2, ALB, etc.)", pricing: "Free. You pay for data transfer, not the gateway itself." },
      { name: "Elastic IP", full: "Elastic IP Address (EIP)", color: "#E8983C", lightBg: "#FEF5ED", icon: "📌🌐", tagline: "Static public IPv4 address you own — survives instance stop/start and can be remapped", analogy: "A permanent postal address that you own independently of any building. You can move it from one house (EC2 instance) to another at any time — the address never changes even when the building changes.", keyPoints: ["A static public IPv4 address allocated to your AWS account — it persists until you explicitly release it", "By default, EC2 instances get a new public IP every time they stop/start; an EIP stays the same forever", "Remap instantly: detach from a failed instance and associate with a healthy replacement — DNS updates propagate immediately", "Charged when NOT associated: AWS charges ~$0.005/hr for an EIP that exists but is not attached to a running instance (to discourage hoarding unused addresses)", "One EIP per instance (typically on the primary ENI); up to 5 EIPs per region by default (request increase via support)", "NAT Gateway, Network Load Balancer, and EC2 instances can all use EIPs — useful when you need a stable IP for partner whitelisting"], when: "EC2 that must have a consistent public IP for partner/firewall whitelisting; failover scenario where you remap an IP to a healthy instance; NLB needing static IPs per AZ", pricing: "Free when attached to a running instance. ~$0.005/hr when unattached. Exam tip: 'static public IP' + 'survive stop/start' = EIP. 'Charged when idle' is the classic billing trap." },
      { name: "ENI", full: "Elastic Network Interface (ENI)", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔌🖥️", tagline: "Virtual network card — attach multiple to one EC2 for dual-homing, failover, or licensing", analogy: "A network cable port on your server. EC2 can have multiple ports (ENIs) plugged into different subnets simultaneously — one for management traffic, one for data traffic, each with its own IP and security groups.", keyPoints: ["Every EC2 has at least one ENI (eth0 = primary) — it holds the private IP, optional public IP, MAC address, and security group associations", "Additional ENIs (eth1, eth2…) can be attached to the same instance — each gets its own private IP, SGs, and can be in a different subnet (same AZ)", "Detach and reattach: a secondary ENI can be detached from a failed instance and reattached to a standby — MAC address and private IP move with it (useful for software tied to a MAC)", "Dual-homed instance: eth0 in a private subnet (app traffic), eth1 in a management subnet (admin access) — separate traffic with separate SG policies", "ENA (Elastic Network Adapter): higher-performance ENI type for supported instance families — up to 100 Gbps; separate from basic ENI concept", "EFA (Elastic Fabric Adapter): specialized high-performance ENI for HPC/MPI workloads — OS-bypass networking skips the OS kernel entirely so app talks directly to the NIC; delivers ultra-low, consistent microsecond latency. Supports MPI (Message Passing Interface) for tightly-coupled parallel jobs (genomics, CFD, molecular dynamics). Linux only for OS-bypass; same-AZ only. Pair with Cluster Placement Group for lowest inter-node latency. ENA is fast but kernel-mediated — EFA is the answer when the exam says HPC + inter-node communication + low latency"], when: "Dual-NIC EC2 (management + data plane); failover using MAC-based licensing; HPC with EFA; low-level network architecture questions", pricing: "ENIs themselves are free. You pay for any associated EIPs or data transfer. Exam tip: 'software license tied to MAC address' + 'failover' = detach/reattach ENI." },
      { name: "NAT Gateway", full: "NAT Gateway", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔒🚪", tagline: "Outbound internet for private subnets", analogy: "A one-way door — private resources can make outbound requests to the internet (to download updates, call APIs) but inbound connections from the internet are blocked.", keyPoints: ["Sits in a PUBLIC subnet — not the private subnet. It needs a route to the Internet Gateway to reach the internet itself; the private subnet's route table then points outbound traffic to the NAT Gateway", "Traffic flow: Private EC2 → NAT Gateway (public subnet, same AZ) → Internet Gateway → Internet", "Managed by AWS — highly available within a single AZ; if that AZ fails, the NAT Gateway in it is unavailable", "HA design — one NAT Gateway per AZ: deploy a NAT Gateway in the public subnet of each AZ, then create a separate route table for each AZ's private subnet pointing 0.0.0.0/0 to the NAT Gateway in the same AZ. Each AZ is then self-contained — an AZ failure only affects that AZ, not the others", "Why not one shared NAT Gateway? Single point of failure across all AZs; cross-AZ traffic also incurs data transfer charges (~$0.01/GB)", "Why per-AZ route tables? If all private subnets shared one route table pointing to NAT GW in AZ-A, then private EC2s in AZ-B and AZ-C would route cross-AZ — paying transfer fees and losing resilience if AZ-A fails"], when: "EC2/Lambda in private subnets needing outbound internet (OS updates, external APIs) without being publicly reachable", pricing: "~$0.045/hr per NAT Gateway + $0.045/GB data processed. Full HA = 3 NAT Gateways (one per AZ) = ~$0.135/hr baseline. Exam tip: NAT Gateway always goes in the public subnet, never the private subnet." },
      { name: "🛡️ Security & Traffic Control", isHeader: true, color: "#E8593C" },
      { name: "Security Groups", full:"Security Groups", color: "#E8593C", lightBg: "#FEF0ED", icon: "🧱🔑", tagline: "Stateful firewall for instances", analogy: "A personal bodyguard per server — you define exactly which IPs and ports are allowed in or out.", keyPoints: ["Stateful: allow inbound → response auto-allowed out", "Applied at instance/ENI level", "Default: deny all inbound, allow all outbound"], when: "Always — every EC2, RDS, Lambda in VPC needs one", pricing: "Free." },
      { name: "NACLs", full: "Network Access Control Lists", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔐📋", tagline: "Stateless firewall for subnets", analogy: "A border checkpoint for an entire neighbourhood — must explicitly allow both inbound AND outbound.", keyPoints: ["Stateless: must set both inbound AND outbound rules", "Applied at the subnet level", "Rules evaluated in number order — first match wins"], when: "Extra subnet-level defence, blocking IP ranges", pricing: "Free." },
      { name: "VPC Endpoints", full: "VPC Endpoints", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔗🏰", tagline: "Private access to AWS services without internet", analogy: "A private tunnel from your VPC directly to S3, DynamoDB, or other AWS services — traffic never leaves the AWS network.", keyPoints: ["Gateway Endpoints: free, for S3 and DynamoDB only", "Interface Endpoints (PrivateLink): for 100+ AWS services, uses an ENI in your subnet", "Eliminates need for NAT Gateway just to reach AWS services"], when: "EC2/Lambda in private subnets accessing S3, DynamoDB, or other AWS services without internet routing", pricing: "Gateway Endpoints: free. Interface Endpoints: ~$0.01/hr per AZ + $0.01/GB data." },
      { name: "Interface Endpoint", full: "VPC Interface Endpoint", color: "#E8983C", lightBg: "#FEF5ED", icon: "🔌🏰", tagline: "ENI in your subnet for private AWS service access", analogy: "A private phone line to an AWS service installed inside your subnet — calls go over the AWS backbone, never touching the public internet.", keyPoints: ["Powered by PrivateLink — creates an ENI with a private IP in your subnet", "Supports 100+ services: SSM, Secrets Manager, SQS, SNS, EC2 API…", "Works in private subnets with no internet access at all", "Different from Gateway Endpoints (free, S3/DynamoDB only)"], when: "Resources in private subnets that need to call AWS APIs without routing through NAT Gateway", pricing: "~$0.01/hr per AZ + $0.01/GB. Often cheaper than NAT Gateway at high data volumes." },
      { name: "PrivateLink", full: "AWS PrivateLink", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔗🔒", tagline: "Expose your service privately to other VPCs", analogy: "A private delivery hatch — other VPCs or AWS accounts access your service without it ever touching the public internet or requiring VPC peering.", keyPoints: ["Traffic stays on AWS backbone — no internet exposure", "Used by Interface Endpoints to reach AWS services privately", "You can publish your own services via PrivateLink too", "No VPC peering or route table changes needed on consumer side"], when: "Sharing services between VPCs/accounts privately; accessing AWS or SaaS services without internet", pricing: "~$0.01/hr per endpoint per AZ + $0.01/GB data processed." },
      { name: "🔗 Hybrid Connectivity", isHeader: true, color: "#7D6608" },
      { name: "VPC Peering", full: "VPC Peering Connection", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔗🏰", tagline: "Direct private connection between two VPCs — same or different accounts/regions", analogy: "Knocking a doorway through a shared wall between two offices. Traffic flows directly between the two rooms over private IP — no going out to the street (internet) and back in.", keyPoints: ["One-to-one: a peering connection links exactly two VPCs — traffic flows privately over the AWS backbone (no internet, no VPN)", "Cross-account and cross-region: can peer VPCs in different AWS accounts and even different regions (inter-region peering adds data transfer charges)", "NOT transitive: if VPC A ↔ VPC B and VPC B ↔ VPC C, VPC A cannot reach VPC C through B. You must create a direct A ↔ C peering if needed. This is the most tested VPC Peering fact", "Setup requirements: (1) Create peering connection and accept it, (2) Add routes in both VPCs' route tables pointing the peer CIDR to the peering connection, (3) Update security groups to allow traffic from the peer CIDR", "No overlapping CIDR blocks: the two VPCs must have non-overlapping IP ranges — peering fails if CIDRs overlap", "No bandwidth bottleneck: VPC peering uses the same AWS backbone with no single gateway to saturate; scales with instance bandwidth", "When to choose peering vs Transit Gateway: peering = simple two-VPC connection, lower cost. TGW = hub connecting many VPCs, supports transitive routing but costs more per attachment"], when: "Two VPCs that need to communicate privately; same or cross-account; simpler and cheaper than TGW for just two VPCs", pricing: "No hourly charge. Data transfer: free within same AZ, ~$0.01/GB cross-AZ, standard inter-region data transfer rates for cross-region. Exam tip: 'not transitive' is the critical VPC Peering constraint — always use TGW or a full mesh of peering connections for many-to-many VPC routing." },
      { name: "Transit Gateway", full:"AWS Transit Gateway", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔀🏢", tagline: "Central hub connecting VPCs and on-prem", analogy: "A regional airport — instead of direct flights between every city pair (VPC peering), all routes go through the hub. Add a new city, connect it once.", keyPoints: ["Connects hundreds of VPCs and on-prem networks through one gateway", "Replaces complex full-mesh VPC peering", "Supports cross-region peering and multicast", "Attach VPCs as TGW attachments — enable route propagation per attachment so routes are automatically advertised into the TGW route table (no manual route maintenance)", "Direct Connect integration: create a transit VIF (virtual interface) on your Direct Connect connection and associate it with the TGW via a Direct Connect Gateway — this gives on-prem access to ALL attached VPCs through one connection", "Scalability advantage: with 25 VPCs, the alternative (private VIF per VPC) requires 25 separate VIFs and 25 VGWs. TGW + one transit VIF = one connection to reach all 25 VPCs", "Private VIF vs Transit VIF: private VIF connects Direct Connect to a single VGW (one VPC). Transit VIF connects Direct Connect to a TGW (up to 5,000 VPCs). Use transit VIF when scaling beyond a handful of VPCs"], when: "Large environments with many VPCs needing to communicate, or connecting multiple VPCs to on-prem", pricing: "$0.05/hr per attachment + $0.02/GB data processed." },
      { name: "VPN", full: "AWS Site-to-Site VPN", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒🌐", tagline: "Encrypted tunnel from on-prem to AWS", analogy: "A secure underground pipe connecting your office to AWS — all traffic is encrypted and travels over the public internet.", keyPoints: ["Encrypted IPsec tunnel between on-prem and VPC", "Faster to set up than Direct Connect (minutes vs weeks)", "Two tunnels per connection for redundancy", "Pair with Direct Connect for encrypted private link", "Two required components: Virtual Private Gateway (VGW) on the AWS side — attached to your VPC; Customer Gateway (CGW) on the on-premises side — an AWS resource that points to the public IP of your physical VPN device (router/firewall)", "VGW is AWS-managed; CGW is just a config object in AWS representing your on-prem device. The actual tunnel runs between them over the public internet", "Exam tip: IGW is NOT used for VPN. VGW = AWS side, CGW = on-prem side — always"], when: "Secure on-prem to AWS connectivity without paying for Direct Connect", pricing: "$0.05/hr per VPN connection + data transfer." },
      { name: "VPN CloudHub", full: "AWS VPN CloudHub", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒🌐🔀", tagline: "Hub-and-spoke VPN — branch offices talk to AWS AND each other via one VGW", analogy: "A town square (VGW) that all villages (branches) connect to. Instead of building roads between every pair of villages, each village builds one road to the town square. Village A reaches Village B by going: A → town square → B.", keyPoints: ["Not a separate AWS service — a design pattern using a single Virtual Private Gateway (VGW) with multiple Site-to-Site VPN connections attached", "All branch offices connect via VPN to the same VGW. The VGW acts as the hub — branch-to-branch traffic routes: Branch A → VGW → Branch B", "Each branch needs: its own Customer Gateway (CGW) + a unique BGP ASN (Autonomous System Number) — the VGW uses BGP ASNs to distinguish between branches and advertise routes", "All traffic is IPsec encrypted — each spoke is a standard VPN tunnel, so branch-to-branch traffic is encrypted through AWS", "Branch-to-branch traffic routes through AWS — adds some latency vs direct connections, but avoids the cost of direct branch-to-branch links", "Limit: up to 10 VPN connections per VGW", "vs Transit Gateway: TGW supports thousands of connections (VPCs + on-prem), transitive routing, more features — but higher cost. CloudHub is simpler and cheaper for a small number of branch sites"], when: "Q describes multiple branch offices needing to connect to AWS AND communicate with each other over existing internet connections at low cost → VPN CloudHub (not Transit Gateway for small-scale, not Direct Connect)", pricing: "$0.05/hr per VPN connection (same as standard VPN) + data transfer. No additional charge for CloudHub itself. Exam tip: fingerprint is 'multiple remote sites + communicate with each other through AWS + low cost'. Unique BGP ASN per branch is a requirement." },
      { name: "Direct Connect", full: "AWS Direct Connect", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔌", tagline: "Dedicated private line to AWS — fast and consistent, but NOT encrypted by default", analogy: "A private cable from your office directly to AWS — no public internet, but the cable itself is not a sealed envelope. Anyone who taps the physical line can read the traffic unless you add encryption on top.", keyPoints: ["More reliable & consistent than VPN — no internet congestion, predictable latency", "1 Gbps or 10 Gbps speeds", "NOT encrypted by default: Direct Connect is a private connection but traffic is not encrypted at the network layer. To encrypt DX traffic you need: (1) MACsec (Layer 2 encryption on supported DX connections), or (2) run a Site-to-Site VPN over the DX connection (IPsec over private link)", "Pair with VPN for encrypted private link — VPN over DX gives you both private routing and network-layer encryption", "Virtual Interface (VIF) types: Private VIF = connects to a single VPC via a VGW. Public VIF = access AWS public services (S3, DynamoDB) without traversing internet. Transit VIF = connects to a Transit Gateway, enabling access to many VPCs through one DX connection", "Exam rule: many VPCs + Direct Connect = Transit VIF + Transit Gateway. One VPC + Direct Connect = Private VIF + VGW"], when: "Enterprises needing consistent high-bandwidth low-latency AWS access; NOT sufficient alone when network-layer encryption is required", pricing: "~$0.30/hr (1G) or $2.25/hr (10G). Data out ~$0.02/GB. Exam trap: 'encrypted at the network layer' → Direct Connect alone is WRONG. Use VPN (IPsec) or VPN-over-DX." },
      { name: "⚖️ Load Balancing", isHeader: true, color: "#1A7A4A" },
      { name: "ELB", full:"Elastic Load Balancing", color: "#E8983C", lightBg: "#FEF5ED", icon: "⚖️", tagline: "Distribute traffic across servers", analogy: "A traffic cop spreading requests evenly. Three types: ALB, NLB, GLB.", keyPoints: ["ALB = Layer 7, HTTP/HTTPS, path/host routing", "NLB = Layer 4, TCP/UDP, ultra-low latency", "GLB = Layer 3, for firewalls & appliances"], when: "Multiple servers handling the same app", pricing: "ALB: ~$0.016/hr + $0.008/LCU-hr. NLB: ~$0.016/hr + $0.006/NLCU-hr." },
      { name: "ALB", full: "Application Load Balancer", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔀", tagline: "Smart HTTP/HTTPS load balancer", analogy: "A hotel concierge — routes you to the right room (microservice) based on URL path or hostname.", keyPoints: ["Routes by path (/api → A, /web → B) or host", "WebSockets, HTTP/2, gRPC support", "Native container/Lambda targets"], when: "Web apps, APIs, microservices needing content-based routing", pricing: "~$0.016/hr + $0.008/LCU-hr." },
      { name: "NLB", full: "Network Load Balancer", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "⚡⚖️", tagline: "Ultra-fast TCP/UDP load balancer", analogy: "A high-speed toll booth — routes packets as fast as possible without inspecting them.", keyPoints: ["Layer 4 (TCP/UDP/TLS)", "Millions of requests/second", "Static IP per AZ — useful for whitelisting"], when: "Extreme performance, gaming, IoT, static IP requirement", pricing: "~$0.016/hr + $0.006/NLCU-hr." },
      { name: "GLB", full: "Gateway Load Balancer (GWLB)", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔀🛡️", tagline: "Load balance traffic through 3rd-party virtual network appliances — firewalls, IDS/IPS, deep packet inspection", analogy: "A security checkpoint wired into the highway itself. Every car (packet) must pass through the checkpoint (your Palo Alto or Fortinet firewall appliance) before reaching its destination — the driver doesn't know or care, it's invisible.", keyPoints: ["Layer 3/4 gateway that sends traffic to a fleet of virtual appliances (firewalls, IDS/IPS, traffic inspection tools) and returns it to the original destination transparently", "Uses GENEVE protocol (port 6081) to encapsulate traffic — preserves original source/destination while tunnelling through appliances", "Deployed in front of your VPC traffic flow: attach to route tables so all inbound/outbound traffic is intercepted and inspected", "Works with PrivateLink-based Endpoint Services: the appliance vendor runs appliances in their own VPC and exposes them via PrivateLink; your VPC sends traffic through a Gateway Load Balancer Endpoint to reach them", "Health checks: GWLB monitors appliance health and only routes to healthy instances; scales the appliance fleet automatically", "Use case: enterprise security requirement to inspect all VPC traffic through a managed firewall appliance (Palo Alto, Fortinet, CheckPoint) without changing application architecture", "vs Network Firewall: AWS Network Firewall is AWS-native managed; GWLB is for 3rd-party bring-your-own appliances"], when: "You must route traffic through 3rd-party firewall/IDS appliances (Palo Alto, Fortinet, etc.) at scale — GWLB makes them transparent and highly available", pricing: "~$0.008/hr per GLB endpoint + $0.004/GB data processed. Exam tip: 'third-party firewall appliance', 'transparent traffic inspection', 'virtual appliance fleet' → Gateway Load Balancer. AWS-native deep packet inspection → AWS Network Firewall." },
      { name: "🔀 DNS & Content Delivery", isHeader: true, color: "#9B59B6" },
      { name: "DNS Record Types", full: "DNS Record Types: A, CNAME, Alias, PTR", color: "#C0392B", lightBg: "#FDEDEC", icon: "📋🌍", tagline: "The four record types that appear most on the SAA exam", analogy: "A record = a street address (name → IP). CNAME = a nickname that points to another name. Alias = a smarter CNAME that AWS invented. PTR = a reverse lookup (IP → name, like looking up who owns a phone number).", keyPoints: [
        "A record (Address): maps a hostname to an IPv4 address — e.g. api.example.com → 1.2.3.4. The most fundamental DNS record. For IPv6 the equivalent is AAAA record.",
        "CNAME (Canonical Name): maps a hostname to another hostname — e.g. www.example.com → myapp.elb.amazonaws.com. The target must itself resolve to an IP via its own A record. CNAME cannot be used at the zone apex (naked domain) — e.g. you CANNOT create a CNAME for example.com itself, only for subdomains like www.example.com",
        "Alias record (Route 53-specific): like a CNAME but smarter — maps a hostname to an AWS resource endpoint (ALB, CloudFront, S3 website, API Gateway, Elastic Beanstalk, etc.). Key advantages over CNAME: (1) works at the zone apex — you CAN create an Alias for example.com itself; (2) free — Route 53 does not charge for Alias queries to AWS resources; (3) health-check aware — Route 53 can health-check the target automatically",
        "Alias vs CNAME: if the question says 'root domain' or 'zone apex' (example.com with no subdomain) → must use Alias, never CNAME. If pointing to an AWS resource → prefer Alias (free + apex support). If pointing to a non-AWS hostname → use CNAME",
"Cost-effective exam trap: Alias queries to AWS resources (including other Route 53 records) are FREE — Route 53 does not charge per query. CNAME queries are charged. So 'route covid19survey.com → www.covid19survey.com most cost-effectively' = Alias record (CNAME is invalid at apex anyway, and would cost more even if it were valid)",
        "PTR record (Pointer): reverse DNS — maps an IP address back to a hostname. Used for: email server validation (spam filters check that the sending IP's PTR record matches the mail server hostname), security auditing, logging human-readable hostnames from IP addresses",
        "PTR lives in a special reverse DNS zone: for IP 1.2.3.4, the PTR record lives in the zone 3.2.1.in-addr.arpa. AWS manages reverse DNS for EC2 public IPs automatically; you can request custom PTR records for Elastic IPs via AWS Support"
      ], when: "Route 53 configuration questions: 'map domain to ALB' → Alias. 'Map subdomain to another domain' → CNAME. 'Root domain to AWS resource' → must be Alias. 'Reverse DNS / email deliverability' → PTR.", pricing: "Exam tip: Alias queries to AWS resources are free. CNAME queries are charged at standard Route 53 query rates ($0.40/million). Always choose Alias over CNAME when pointing to AWS resources." },
      { name: "Route 53", full:"Amazon Route 53", color: "#C0392B", lightBg: "#FDEDEC", icon: "🌍📡", tagline: "DNS & domain management", analogy: "The internet's phone book — translates 'mysite.com' into an IP address.", keyPoints: ["Routing policies: Simple, Weighted, Latency-based, Failover, Geolocation, Geoproximity, Multi-value", "Geolocation routing: route users based on their geographic location (continent, country, or US state) — e.g. US users → US endpoint, EU users → EU endpoint; can return 'no answer' to deny access entirely", "Geoproximity routing (Traffic Flow only): route based on location AND shift traffic bias toward/away from a region using a bias value", "Latency routing: route to the region with lowest latency for the user — not necessarily geographically closest", "Failover routing: primary + secondary; Route 53 health checks trigger automatic DNS failover to secondary", "Health checks trigger automatic failover — works across regions for active-passive DR"], when: "Pointing domain names to AWS, global routing, DR failover, geo-restricting access by country", pricing: "$0.50/hosted zone/month · $0.40/million queries · Health checks ~$0.50/endpoint/month." },
      { name: "Route 53 Resolver", full: "Route 53 Resolver: Hybrid DNS Resolution", color: "#E8593C", lightBg: "#FEF0ED", icon: "🌍🔀", tagline: "Bridge DNS between your VPC and on-premises networks", analogy: "Route 53 Resolver is a DNS switchboard operator — when your VPC asks 'where is db.internal.corp?', the operator decides: answer from AWS records, or forward the question to the on-prem DNS server.", keyPoints: ["Default: every VPC has a built-in Resolver at VPC_CIDR+2 (e.g. 10.0.0.2) that answers Route 53 private hosted zones and public DNS", "Outbound endpoint: an ENI in your VPC that forwards specific DNS queries from your VPC to your on-premises DNS server — use when VPC resources need to resolve on-prem hostnames (e.g. legacy-app.corp.internal)", "Inbound endpoint: an ENI in your VPC that accepts DNS queries FROM on-premises sent over VPN/Direct Connect — use when on-prem servers need to resolve AWS private DNS records (e.g. rds.amazonaws.com private names)", "Forwarding rule: tells the Resolver which domain suffixes to send to which DNS server — e.g. 'all queries for *.corp.internal → forward to 10.1.1.53 (on-prem DNS)'", "Rule association: forwarding rules must be associated with specific VPCs to take effect", "Memory aid: Outbound = queries flow OUT of AWS to on-prem. Inbound = queries flow IN from on-prem to AWS."], when: "Hybrid DNS: VPC needs to resolve on-prem hostnames (outbound endpoint + forwarding rule) or on-prem needs to resolve AWS private DNS (inbound endpoint)", pricing: "$0.125/hr per endpoint (per AZ) + $0.40/million queries forwarded." },
{ name: "CloudFront", full: "Amazon CloudFront + CDN Caching", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌩", tagline: "CDN — cache content at the edge", analogy: "Instead of every user fetching your files from one server in us-east-1, CloudFront places cached copies at 200+ locations worldwide. Paris users get files from Frankfurt, Tokyo users from Tokyo — no ocean crossing.", keyPoints: ["Edge locations cache your content (static files, images, HTML, APIs)", "Cache behaviour set by TTL (Time To Live) — how long content stays at edge before re-fetching from origin", "Cache hit = served from edge instantly. Cache miss = CloudFront fetches from origin, caches it", "Invalidations let you force-clear the cache when content changes", "Origin can be S3, ALB, EC2, or any HTTP endpoint", "Geo restriction (geoblocking): whitelist or blacklist specific countries — CloudFront returns 403 Forbidden to blocked countries; uses its own IP-to-country database", "Whitelist = only listed countries can access. Blacklist = all countries except listed ones are blocked", "For country-level content licensing restrictions → CloudFront geo restriction is the simplest built-in solution"], when: "Global content delivery; reducing origin server load; protecting S3 with OAC/OAI; HTTPS for S3 static sites; restricting content by country", pricing: "~$0.0085–$0.02/GB transfer. 1TB + 10M requests/month free tier. Geo restriction: free." },
      { name: "Lambda@Edge", full: "Lambda@Edge / CloudFront Functions", color: "#7D6608", lightBg: "#FEFCE8", icon: "λ🌩", tagline: "Run code at CloudFront edge", analogy: "A tiny function that intercepts requests or responses at the edge — before they ever reach your origin.", keyPoints: ["Lambda@Edge: full Lambda at regional edge caches", "CloudFront Functions: lightweight JS at all 200+ PoPs, <1ms", "Uses: URL rewrites, auth checks, A/B testing, header injection"], when: "Auth at edge, URL rewrites, personalisation without hitting origin", pricing: "CloudFront Functions: $0.10/million. Lambda@Edge: $0.60/million + compute." },
      { name: "OAC / OAI", full: "Origin Access Control / Identity", color: "#E8983C", lightBg: "#FEF5ED", icon: "🔒🌩", tagline: "Lock S3 to CloudFront only", analogy: "A VIP backstage pass — only CloudFront can access your S3 bucket directly; everyone else is blocked.", keyPoints: ["Prevents direct public access to S3 origin", "OAC is the modern replacement for OAI", "Set in CloudFront + S3 bucket policy"], when: "Serving private S3 content exclusively through CloudFront", pricing: "Free." },
      { name: "Global Accelerator", full: "AWS Global Accelerator", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🏎️", tagline: "Speed up dynamic traffic globally", analogy: "CloudFront caches. Global Accelerator routes — it sends live traffic through AWS's private backbone instead of the slow public internet.", keyPoints: ["2 static anycast IPs globally", "Routes to nearest healthy endpoint", "TCP/UDP — no caching", "Routes traffic through AWS private backbone instead of public internet — faster and more reliable even for uncached content", "No caching at all — every request reaches your backend; latency improvement comes from the network path, not content serving", "Static IPs: 2 Anycast IPs that work globally — same IPs regardless of region. Useful for firewall allowlisting across many regions/accounts", "Automatic failover: if your endpoint in one region goes unhealthy, GA reroutes to next healthy region in <30 seconds", "CloudFront vs Global Accelerator: CloudFront = Layer 7, caches content AT the edge, HTTP/HTTPS only. Global Accelerator = Layer 3/4, routes THROUGH the edge, any TCP/UDP. If content can be cached → CloudFront. If dynamic/non-HTTP/needs static IPs → Global Accelerator"], when: "Dynamic content, gaming, VoIP, non-HTTP — where caching doesn't apply; multi-region failover; static IPs for firewall allowlisting", pricing: "$0.025/hr per accelerator + $0.015/GB." },
      { name: "API Gateway", full: "Amazon API Gateway", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🚪", tagline: "Front door for your APIs", analogy: "A bouncer — checks requests, enforces rules, routes to the right backend.", keyPoints: ["Three API types: REST API, HTTP API, WebSocket API", "REST API: stateless request/response — each call independent; no persistent connection; full features (usage plans, API keys, caching, custom authorisers)", "HTTP API: stateless like REST but simpler and 70% cheaper — use when you don't need REST's advanced features", "WebSocket API: stateful, full-duplex persistent connection between client and server — server can push messages to client at any time without a new request; ideal for chat, live dashboards, notifications, gaming", "Rate limiting, throttling, and auth (IAM, Cognito, Lambda authoriser) built into all types", "Pairs with Lambda for fully serverless APIs; also integrates with HTTP backends, Step Functions, SQS", "Stage variables: key-value config per stage (dev/staging/prod) — swap Lambda ARNs, backend URLs, or feature flags per environment without code changes"], when: "Q asks about stateless client-server API → REST or HTTP API. Q asks about stateful or real-time two-way communication → WebSocket API", pricing: "REST: $3.50/million requests. HTTP: $1.00/million. WebSocket: $1.00/million messages + $0.25/million connection-minutes. Exam tip: WebSocket = stateful + full-duplex; REST/HTTP = stateless + request-response." },
    ]
  },
  compute: {
    tab: "☁️ Compute",
    items: [
      { name: "🖥️ Virtual Machines & Serverless", isHeader: true, color: "#E8593C" },
      { name: "EC2", full: "Elastic Compute Cloud", color: "#E8593C", lightBg: "#FEF0ED", icon: "🖥️", tagline: "Virtual servers in the cloud", analogy: "Renting a computer — you choose the OS, size, and software.", keyPoints: ["Pay per hour/second", "Choose CPU, RAM, storage, OS", "You manage the server", "To access AWS services (e.g. DynamoDB, S3): attach an IAM role via an instance profile — never hardcode credentials on the instance", "Instance profile = the attachment mechanism that links an IAM role to an EC2 instance; the EC2 service (ec2.amazonaws.com) assumes the role on the instance's behalf and delivers temporary credentials via IMDS"], when: "Full control over environment; lift-and-shift migrations", pricing: "On-Demand · Reserved (1–3yr, up to 72% off) · Spot (up to 90% off) · Savings Plans" },
      { name: "AMI", full: "Amazon Machine Image", color: "#C0392B", lightBg: "#FDEDEC", icon: "📸", tagline: "Snapshot template for EC2", analogy: "A golden master disk image — stamp out identical EC2 instances instantly.", keyPoints: ["Includes OS, software, config", "Launch new EC2 instances from it", "Share across accounts/regions"], when: "Replicating environments, auto-scaling launch templates, DR", pricing: "Storage charged as EBS snapshot (~$0.05/GB/month). Creating/copying is free." },
      { name: "Spot Fleet", full: "EC2 Spot Fleet", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🚢🎲", tagline: "Manage a mixed fleet of Spot + On-Demand instances across types and AZs", analogy: "Instead of betting on one flight being cheap, you tell a travel agent 'get me 100 seats, mix of economy and business, across any airline, whatever is cheapest right now' — the fleet does the shopping for you.", keyPoints: ["A Spot Fleet is a collection of Spot Instance requests (and optionally On-Demand) managed as a single unit — you define a target capacity (vCPUs or instances) and the fleet maintains it", "Launch specifications (launch templates): define multiple instance types, sizes, and AZs — the fleet picks the cheapest available combination from your list", "Allocation strategies: lowestPrice (cheapest pool first — may concentrate interruptions), diversified (spread across pools — more resilient), capacityOptimized (pick the pool with most spare capacity — lowest interruption rate, recommended), priceCapacityOptimized (balance of both)", "Spot Fleet vs Auto Scaling Group: ASG manages On-Demand with optional Spot mix; Spot Fleet is purely Spot-centric with optional On-Demand top-up. For modern workloads, EC2 Auto Scaling with mixed instances policy is generally preferred", "Interruption handling: if a Spot instance is reclaimed, the fleet automatically launches a replacement from another pool to maintain target capacity", "Request types: request (one-time, no replacement) or maintain (continuously replaces interrupted instances to hold target capacity)", "Persistent spot request: if your Spot Instance is interrupted, the request is automatically reopened and a new instance launched when capacity is available again", "Cancelling a spot request does NOT terminate the associated instance — you must cancel the request first, then manually terminate the instance (otherwise a new instance will be launched to fulfil the request)", "Spot Fleet maintains target capacity: when a Spot Instance in the fleet is terminated, the fleet automatically launches a replacement from another pool", "Spot Fleet with weighted capacity: assign different weights to instance types (e.g. 4xlarge = weight 4) so target capacity is in compute units, not raw instance count"], when: "Large-scale fault-tolerant workloads (batch, big data, ML training) needing flexible instance selection across types and AZs to minimise cost and interruption risk", pricing: "Pay Spot pricing per instance. Fleet management itself is free. On-Demand fill-in charged at On-Demand rates." },
      { name: "Lambda", full: "AWS Lambda", color: "#E8983C", lightBg: "#FEF5ED", icon: "λ", tagline: "Run code without servers", analogy: "A vending machine — push a button, it runs, you pay only for that moment.", keyPoints: ["No server management", "Triggered by events (S3, API Gateway, SQS…)", "Pay per execution in milliseconds", "Default networking: Lambda runs in an AWS-owned VPC — has direct access to public internet and public AWS APIs (S3, DynamoDB, etc.) with no extra config", "VPC-enabled Lambda: when you attach Lambda to your own VPC (for private resource access e.g. RDS), it loses default internet access. Must add a NAT Gateway in a public subnet and route 0.0.0.0/0 through it to restore internet/public API access", "Concurrency scaling: Lambda scales extremely fast (thousands of concurrent executions in seconds) — deploy CloudWatch Alarms on ConcurrentExecutions and Invocations to catch runaway loops, throttling risks, or unexpected cost spikes before they escalate", "Lambda Layers: package shared code/libraries (e.g. pandas, shared utils) into a Layer attached to multiple functions — update once, all functions pick it up. Keeps deployment packages small and avoids duplicating dependencies across functions"], when: "Event-driven tasks, API backends, scheduled jobs", pricing: "1M requests/month free · $0.20/1M after · $0.0000166667/GB-sec compute" },
      { name: "📈 Auto Scaling", isHeader: true, color: "#7D6608" },
      { name: "Auto Scaling", full: "EC2 Auto Scaling", color: "#7D6608", lightBg: "#FEFCE8", icon: "📈", tagline: "Automatically add/remove EC2 instances", analogy: "A thermostat for your server fleet — adds capacity when hot, removes it when quiet, always hitting the target.", keyPoints: ["Scale out on high CPU/load, scale in when quiet", "Min, desired, and max instance counts", "Works with ALB/NLB for traffic distribution", "Scheduled, dynamic, or predictive scaling modes"], when: "Any variable-traffic production workload — pairs with ELB for HA", pricing: "Auto Scaling itself is free. You pay for the EC2 instances it launches." },
      { name: "ASG", full: "Auto Scaling Group", color: "#7D6608", lightBg: "#FEFCE8", icon: "📦📈", tagline: "The fleet of EC2s that Auto Scaling manages", analogy: "The actual pool of servers behind your load balancer — Auto Scaling policies tell the group to grow or shrink, and the ASG does the work of launching and terminating instances.", keyPoints: ["Defined by: Launch Template (what to launch) + min/desired/max (how many)", "Integrates with ALB/NLB — new instances auto-registered, unhealthy ones replaced", "Health checks: EC2 (instance status) or ELB (app-level) — use ELB for smarter replacements", "Lifecycle hooks: pause instances during launch or termination to run custom scripts"], when: "Any horizontally scalable EC2 workload; the foundation all scaling policies attach to", pricing: "ASG is free. You pay for the EC2 instances it launches." },
      { name: "Target Tracking", full: "Target Tracking Scaling Policy", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🎯📈", tagline: "Keep a metric at a target value — simplest policy", analogy: "Cruise control in a car — you set the speed you want (e.g. 50% CPU) and it automatically adjusts the throttle (instance count) to stay there without you touching anything.", keyPoints: ["You pick a target: 50% CPU, 1000 requests/target, 70% ALB request count", "AWS automatically creates and manages the CloudWatch alarms for you", "Scales out fast, scales in conservatively (built-in cooldown)", "Recommended as the default choice for most workloads"], when: "Default policy for most ASGs — simplest to configure and most responsive", pricing: "Free. CloudWatch alarms it creates are also free." },
      { name: "Step Scaling", full: "Step Scaling Policy", color: "#E8593C", lightBg: "#FEF0ED", icon: "🪜📈", tagline: "Scale by different amounts depending on breach size", analogy: "A graded response — a small fire gets one extinguisher, a big fire gets the whole brigade. The larger the alarm breach, the more instances you add.", keyPoints: ["You define alarm thresholds and corresponding adjustment amounts", "Example: CPU 60–80% → add 2 instances; CPU >80% → add 5 instances", "Does not wait for cooldown before reacting to a worsening metric", "More control than target tracking; more complex to configure"], when: "Workloads with spiky, unpredictable load where you want proportional response", pricing: "Free." },
      { name: "Simple Scaling", full: "Simple Scaling Policy", color: "#9B59B6", lightBg: "#F5EEF8", icon: "➕📈", tagline: "One alarm, one fixed adjustment — legacy policy", analogy: "A light switch — when the alarm fires, add (or remove) a fixed number of instances, then wait for a cooldown before acting again.", keyPoints: ["Single CloudWatch alarm → fixed adjustment (add N, remove N, or set to N)", "Must wait for cooldown period (default 300s) before scaling again", "Cannot respond to a worsening condition mid-cooldown", "Largely superseded by Step Scaling and Target Tracking"], when: "Simple workloads or legacy configs — prefer Target Tracking for new setups", pricing: "Free." },
      { name: "Scheduled Scaling", full: "Scheduled Scaling Action", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🗓️📈", tagline: "Scale on a schedule you predict in advance", analogy: "Setting your alarm clock before a busy day — you know traffic spikes every Monday at 9am, so you pre-warm the fleet at 8:50am before the load arrives.", keyPoints: ["Set min/max/desired at specific times using cron or one-time expressions", "Executes before the load arrives — unlike reactive policies", "Combine with dynamic policies: scheduled sets the floor, target tracking handles variability on top", "Useful for batch jobs, business-hours patterns, known events"], when: "Predictable load patterns: business hours, weekly peaks, scheduled batch jobs, pre-warming for launches", pricing: "Free." },
      { name: "Predictive Scaling", full: "Predictive Scaling Policy", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔮📈", tagline: "ML-based forecast scaling before load arrives", analogy: "A weather forecast for your traffic — AWS analyses historical patterns and pre-scales your fleet before the predicted load hits, rather than reacting after the fact.", keyPoints: ["Uses ML to forecast based on your ASG's historical CloudWatch data (needs 24hr minimum)", "Launches instances in advance of predicted load — proactive not reactive", "Forecast mode: view predictions without acting. Forecast + scale: act on them", "Combine with target tracking for both proactive and reactive coverage"], when: "Workloads with recurring, predictable patterns (daily/weekly cycles) where cold-start lag matters", pricing: "Free." },
      { name: "📦 Containers & PaaS", isHeader: true, color: "#9B59B6" },
      { name: "ECS", full:"Elastic Container Service", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📦", tagline: "Run Docker containers", analogy: "A shipping yard that manages standardized Docker containers for you.", keyPoints: ["AWS-managed container orchestration", "Works with Docker", "Uses EC2 or Fargate underneath"], when: "Docker workloads with AWS-native orchestration", pricing: "ECS itself is free. Pay for EC2 or Fargate resources." },
      { name: "ECS Task Role vs Execution Role", full: "ECS IAM Roles: Task Role (app permissions) vs Task Execution Role (infrastructure permissions)", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📦🔑", tagline: "Two separate IAM roles — one for your app, one for ECS infrastructure. They are not interchangeable.", analogy: "A delivery driver (ECS agent) needs a keycard to enter the warehouse and pick up parcels (Task Execution Role — pull images, write logs). The parcels themselves contain letters authorised to access certain bank vaults (Task Role — app calling S3, DynamoDB). The driver's keycard doesn't open bank vaults, and the letters don't open the warehouse.", keyPoints: ["Task Execution Role: used by the ECS agent (not your app) to perform infrastructure tasks — pull container images from ECR, write container logs to CloudWatch Logs, fetch secrets/parameters from Secrets Manager or SSM Parameter Store at startup. Attach this role to the task definition's executionRoleArn field", "Task Role: used by the APPLICATION CODE running inside the container to call AWS services — S3, DynamoDB, SQS, SNS, etc. ECS injects temporary credentials into the container via the container metadata endpoint. Attach this to the task definition's taskRoleArn field", "They are independent: a container can have both, either, or neither. Having one does not grant the permissions of the other", "Common exam trap: 'ECS container cannot access S3' → missing or incorrect Task Role (not Execution Role). 'ECS cannot pull image from ECR or write to CloudWatch' → missing or incorrect Task Execution Role", "No hardcoded credentials: Task Role follows the same principle as EC2 instance roles — the application receives auto-rotating temporary credentials from AWS STS via the metadata endpoint (169.254.170.2 for ECS). No access keys needed in code or environment variables", "Least privilege: scope the Task Role policy to only the exact actions and resources the container needs — e.g. s3:GetObject on a specific bucket prefix, not s3:* on *", "ECS vs EKS equivalent: EKS uses IRSA (IAM Roles for Service Accounts) to achieve the same per-workload IAM scoping that ECS Task Roles provide"], when: "Q says 'ECS container cannot call S3/DynamoDB/SQS' → add/fix Task Role. 'ECS cannot pull ECR image or write CloudWatch logs' → add/fix Task Execution Role. Always distinguish which role controls which layer.", pricing: "Exam tip: Task Role = your app's AWS permissions (what the code can call). Task Execution Role = ECS agent's permissions (pull images, write logs, fetch secrets). Two separate roleArn fields in the task definition: taskRoleArn and executionRoleArn." },
      { name: "EKS", full: "Elastic Kubernetes Service", color: "#2E86AB", lightBg: "#EBF5FB", icon: "⎈", tagline: "Managed Kubernetes", analogy: "ECS but with Kubernetes — the industry-standard container orchestrator.", keyPoints: ["AWS manages Kubernetes control plane; you manage worker nodes (EC2) or use Fargate (serverless)", "Pod: the smallest deployable unit in Kubernetes — one or more containers that share a network namespace and storage; think of it as the Kubernetes equivalent of a single running task", "By default all Pods on the same EC2 node share the node's IAM role — no least-privilege isolation between workloads", "IRSA (IAM Roles for Service Accounts): map a Kubernetes service account to a specific IAM role — Pods using that service account automatically get only those IAM permissions; enforces least privilege per workload", "IRSA flow: create IAM role → annotate Kubernetes service account with role ARN → assign service account to Pod spec → Pod gets short-lived credentials via projected token", "More flexible than ECS; steeper learning curve; portable across clouds (EKS Anywhere)"], when: "Existing K8s workloads, multi-cloud portability, or fine-grained Pod-level IAM access control (IRSA)", pricing: "$0.10/hr per cluster + EC2/Fargate for worker nodes." },
      { name: "Fargate", full: "AWS Fargate", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🚀", tagline: "Serverless containers", analogy: "ECS/EKS without worrying about the underlying servers at all.", keyPoints: ["No EC2 to manage", "Works with ECS or EKS", "Pay per vCPU + memory used"], when: "Containers with zero server management", pricing: "$0.04048/vCPU-hr + $0.004445/GB-hr." },
      { name: "Beanstalk", full: "AWS Elastic Beanstalk", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌱", tagline: "Deploy apps without managing infra", analogy: "Like Heroku on AWS — upload code, it handles the rest.", keyPoints: ["Auto-provisions EC2, ELB, scaling", "Supports many languages/runtimes", "EC2 under the hood — you can still access it"], when: "Quick deployments without configuring infrastructure", pricing: "Beanstalk free. Pay for EC2, ELB, RDS it provisions." },
      { name: "🌍 Edge & Lightweight Compute", isHeader: true, color: "#5D6D7E" },
      { name: "Outposts", full:"AWS Outposts", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏭", tagline: "Run AWS infrastructure on-premises — same APIs, same hardware", analogy: "A slice of an AWS data centre delivered to your building. You get actual AWS rack hardware installed on-prem, and it runs the same EC2, EBS, RDS, EKS, and S3 APIs you use in the cloud — fully managed by AWS remotely.", keyPoints: ["AWS ships and installs physical rack hardware at your facility (42U rack or smaller 1U/2U form factors)", "Runs a subset of AWS services locally: EC2, EBS, ECS, EKS, RDS, S3 on Outposts, ElastiCache", "Same AWS APIs, CLI, Console — your apps don't know they're on-prem vs cloud", "Low-latency local processing: data stays on-prem for latency-sensitive or data-residency requirements", "Connected to the parent AWS region via VPN/Direct Connect for management; local workloads run even if connectivity drops", "Use cases: manufacturing/industrial (process locally, sync to cloud), healthcare data residency, financial services needing sub-ms latency to on-prem systems"], when: "Compliance requires data to stay on-prem; need ultra-low latency to local systems; want AWS APIs without cloud round-trips", pricing: "3-year commitment, all-inclusive pricing per rack. Roughly $150k–$400k+ depending on configuration." },
      { name: "Lightsail", full: "Amazon Lightsail", color: "#9B59B6", lightBg: "#F5EEF8", icon: "💡", tagline: "Simple cloud platform for small apps — VPS with fixed pricing", analogy: "EC2 with the complexity stripped out — you pick a bundle (CPU + RAM + storage + transfer), pay one flat monthly price, and get a virtual server up in minutes. Like DigitalOcean or Heroku, but on AWS.", keyPoints: ["Fixed monthly bundles: e.g. 2 vCPU + 2 GB RAM + 60 GB SSD + 3 TB transfer for ~$10/month", "Includes: VM, SSD storage, static IP, DNS management, load balancer, CDN — all in one simplified console", "One-click apps: WordPress, LAMP, MEAN, Node.js, Magento, etc.", "No VPC, IAM, security group complexity — simplified networking model", "Can peer with VPC for connectivity to other AWS services if needed", "Not designed for enterprise-scale or fine-grained control — use EC2 for that"], when: "Simple websites, blogs, dev/test environments, small apps with predictable traffic and budget — not for production-grade scalable workloads", pricing: "From $3.50/month (512 MB RAM) to $160/month (32 GB RAM). Predictable flat pricing." },
      { name: "🔧 Batch & Image Management", isHeader: true, color: "#7D6608" },
      { name: "AWS Batch", full: "AWS Batch", color: "#7D6608", lightBg: "#FEFCE8", icon: "📦⚙️", tagline: "Managed batch computing on EC2/Spot — run containerised jobs at scale with no cluster to manage", analogy: "A job dispatch centre: you submit tasks to a queue, Batch provisions exactly the right amount of compute (EC2 or Fargate) to run them, then tears it all down when finished — you never touch the servers.", keyPoints: ["Core concepts: Job (a unit of work defined as a Docker container), Job Queue (jobs wait here), Compute Environment (EC2 or Fargate fleet that processes the queue)", "No time limit: unlike Lambda (max 15 min), Batch jobs can run for hours or days — no timeout ceiling", "Managed Compute Environment: AWS provisions, scales, and terminates EC2 instances automatically; supports Spot instances for up to 90% cost savings on fault-tolerant jobs", "Unmanaged Compute Environment: you manage the EC2 instances yourself and Batch just schedules jobs onto them — more control, more ops overhead", "Job dependencies: chain jobs so Job B only starts after Job A completes — enables multi-stage pipelines", "Array jobs: submit one job definition that spawns N child jobs — each child processes a different slice of data (e.g. process 1,000 S3 files in parallel with one job submission)", "Fargate or EC2: Fargate Batch = no instance management, good for unpredictable workloads; EC2 Batch = choose GPU/HPC instance types, better for heavy compute"], when: "Long-running compute jobs that exceed Lambda's 15-min limit: genomics, financial modelling, rendering, ML preprocessing, simulation, ETL — anything batch-oriented where you want managed, auto-scaling compute", pricing: "AWS Batch itself is free — you pay only for the EC2 or Fargate capacity consumed while jobs run. Use Spot instances in the compute environment to cut costs by up to 90%. Exam tip: 'long-running batch job', 'no time limit', 'managed HPC/compute' → AWS Batch (not Lambda)." },
      { name: "EC2 Image Builder", full: "EC2 Image Builder", color: "#C0392B", lightBg: "#FDEDEC", icon: "🏗️📸", tagline: "Automate the creation, testing, and distribution of hardened AMIs and container images", analogy: "An assembly line for golden images — it installs software, applies patches, runs security tests, and stamps out a finished AMI automatically on a schedule, so your fleet always launches from a fresh, compliant base image.", keyPoints: ["Pipeline: source base AMI → build phase (install software via SSM components) → test phase (run tests including Inspector vulnerability scan) → distribute (copy AMI to regions, share with accounts)", "Components: reusable build/test scripts written in YAML (like SSM documents) that install packages, apply configs, run validation tests", "Schedule: run the pipeline on a cron to rebuild AMIs automatically after OS patches or CVE fixes — ensures your AMIs are never stale", "Inspector integration: automatically run Amazon Inspector vulnerability scans on the new image before distributing — blocks distribution if critical CVEs found", "Container image support: same pipeline concept works for Docker images published to ECR", "No extra cost for Image Builder itself — you pay for the EC2 instance time during build and test, plus storage of the AMI (EBS snapshot costs)"], when: "Maintaining up-to-date, patched, compliance-hardened AMIs for production EC2 fleets and Auto Scaling groups; automating the 'golden image' pipeline that previously required manual effort", pricing: "Image Builder is free. Pay for EC2 instance hours during build/test + EBS snapshot storage for the resulting AMI. Exam tip: 'automate AMI creation', 'golden image pipeline', 'patch compliance for EC2' → EC2 Image Builder." },
    ]
  },
  storage: {
    tab: "🗄️ Storage",
    subTabs: [{ key: "ebs", label: "💾 EBS Types" }],
    ebsCompare: [
      { type: "gp3", category: "SSD", maxIOPS: "16,000", maxTP: "1,000 MB/s", multiAttach: false, bootVol: true, use: "Default — boot, dev/test, small-mid DBs. IOPS and throughput set independently.", price: "$0.08/GB/mo" },
      { type: "gp2", category: "SSD", maxIOPS: "16,000", maxTP: "250 MB/s", multiAttach: false, bootVol: true, use: "Legacy general SSD — IOPS tied to size. Prefer gp3 for all new volumes.", price: "$0.10/GB/mo" },
      { type: "io2 Block Express", category: "SSD", maxIOPS: "256,000", maxTP: "4,000 MB/s", multiAttach: true, bootVol: true, use: "Mission-critical DBs (Oracle, SQL Server). Highest IOPS + 99.999% durability.", price: "$0.125/GB/mo + $0.065/IOPS/mo" },
      { type: "io1", category: "SSD", maxIOPS: "64,000", maxTP: "1,000 MB/s", multiAttach: true, bootVol: true, use: "High-IOPS legacy. Same price as io2 with worse specs — use io2 for new.", price: "$0.125/GB/mo + $0.065/IOPS/mo" },
      { type: "st1", category: "HDD", maxIOPS: "500", maxTP: "500 MB/s", multiAttach: false, bootVol: false, use: "Big data, Kafka, log processing — high-throughput sequential reads.", price: "$0.045/GB/mo" },
      { type: "sc1", category: "HDD", maxIOPS: "250", maxTP: "250 MB/s", multiAttach: false, bootVol: false, use: "Cold archival data — cheapest EBS, infrequent sequential access.", price: "$0.015/GB/mo" },
    ],
    items: [
      { name: "🪣 S3 Object Storage", isHeader: true, color: "#2E86AB" },
      { name: "S3", full: "Simple Storage Service", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🪣", tagline: "Object storage for anything", analogy: "A giant hard drive in the cloud — store any file and access it via URL.", keyPoints: ["Stores files as 'objects' in 'buckets'", "11 nines (99.999999999%) durability", "Multiple storage classes — see S3 Classes tab"], when: "Files, images, backups, logs, static websites", pricing: "Varies by class. Standard ~$0.023/GB/month. See S3 Classes tab." },
      { name: "S3 Standard", full: "S3 Standard", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🪣⭐", tagline: "Default — frequent access, 3+ AZs", analogy: "Your everyday desk drawer — instant access, highest cost, most resilient.", keyPoints: ["99.99% availability, 11 9s durability", "Replicated across ≥3 AZs", "No retrieval fee, no minimum duration"], when: "Frequently accessed data: active apps, content distribution", pricing: "~$0.023/GB/month. No retrieval fee." },
      { name: "S3 Encryption", full: "S3 Server-Side Encryption Options", color: "#C0392B", lightBg: "#FDEDEC", icon: "🪣🔐", tagline: "Three server-side encryption modes — who holds the key?", analogy: "SSE-S3 = AWS locks and keeps the key. SSE-KMS = you own a key in a keychain (KMS) that AWS uses on your behalf — you can audit and rotate it. SSE-C = you bring your own key every single request — AWS uses it then immediately forgets it.", keyPoints: ["SSE-S3 (AES-256): AWS manages keys entirely — simplest, no extra cost; enabled by default on all new buckets since 2023; each object is encrypted with its own unique key, and that key is itself encrypted by a regularly rotated root key managed by AWS", "SSE-KMS: uses a KMS Customer Managed Key (CMK) — you control rotation policy, access policy, and get CloudTrail audit logs of every key use; required for compliance needing key audit trails", "SSE-C (Customer-Provided): you supply the encryption key in every request header; AWS encrypts/decrypts then discards the key — AWS never stores it; you manage key storage yourself", "Client-side encryption: you encrypt before uploading — AWS stores ciphertext and never sees plaintext; you manage the entire encryption process", "Enforce encryption: use a bucket policy with condition aws:SecureTransport to deny HTTP, and s3:x-amz-server-side-encryption to deny unencrypted PutObject requests", "In transit: always use HTTPS/TLS — separate from server-side encryption"], when: "Choose SSE-S3 for simplicity, SSE-KMS for compliance/audit trails, SSE-C when you must hold the key yourself", pricing: "SSE-S3: free. SSE-KMS: $1/CMK/month + $0.03/10k API calls. SSE-C: free (you manage key storage). Exam tip: 'company manages its own keys with audit trail' → SSE-KMS CMK." },
      { name: "S3 Bucket Policy", full: "S3 Bucket Policy (Resource-Based)", color: "#C0392B", lightBg: "#FDEDEC", icon: "🪣📜", tagline: "JSON policy attached directly to the bucket — controls who can access it and how", analogy: "A sign on the door of a filing room that lists exactly who is allowed to enter, what they're allowed to do inside, and any conditions (e.g. must use HTTPS, must be from this VPC).", keyPoints: ["Resource-based policy attached to an S3 bucket — applies to the bucket and all objects inside it", "Must specify a Principal (unlike IAM identity-based policies) — can be AWS accounts, IAM roles/users, services, or * (public)", "Common use cases: grant cross-account access, enforce HTTPS-only (deny aws:SecureTransport false), block public access for specific conditions, restrict to a specific VPC (aws:SourceVpc), require SSE encryption on uploads", "Deny public access: use bucket policy condition with NotPrincipal + Deny, or enable S3 Block Public Access settings (which override bucket policy for public grants)", "Cross-account: the calling account's IAM policy must also allow the action — both the bucket policy AND the caller's identity policy must permit it (unless the bucket policy grants the full cross-account allow)", "Condition keys: aws:SourceVpc (restrict to VPC endpoint), aws:SourceIp (IP range), s3:prefix (limit to folder path), s3:x-amz-server-side-encryption (enforce encryption type on PutObject)", "Bucket policy vs IAM policy: bucket policy is evaluated at the resource, IAM policy is evaluated at the identity. For same-account access, either works. For cross-account access, both must allow."], when: "Cross-account S3 access, enforcing HTTPS/encryption, restricting to a VPC, making buckets public (static websites), granting CloudFront OAC access", pricing: "Free. No charge to create or evaluate bucket policies." },
      { name: "S3 Access Points", full: "Amazon S3 Access Points", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🚪", tagline: "Named endpoints with their own policies — simplify per-app or per-prefix access control", analogy: "Instead of one enormous sign on the door listing every person's permissions for every folder, you create separate door handles for each team — each handle has its own small sign that only lists that team's rules.", keyPoints: ["An S3 Access Point is a named network endpoint attached to a bucket — each one has its own access point policy (like a bucket policy) scoped to that endpoint", "Each access point can restrict access to a specific prefix, enabling fine-grained per-service or per-team isolation without bloating a single bucket policy", "Access point policies are additive with the bucket policy — the bucket policy must grant s3:* to the access point ARN (or use a delegating bucket policy) for access point policies to take effect", "Scalability: a bucket policy has a 20 KB size limit — with many services, a single bucket policy becomes unmanageable. Access points scale: each service gets its own access point + policy, independently managed", "Network origin control: an access point can be restricted to a specific VPC (requests from outside that VPC are rejected) — stronger than a bucket policy VPC condition alone", "Object Lambda Access Points: a special type that runs a Lambda function on the data as it's returned — transform/redact/enrich data on the fly without storing multiple copies", "Multi-region access points: a single global endpoint that routes S3 requests to the bucket replica with the lowest latency across regions"], when: "Multiple apps/services sharing one bucket each needing access to only their own prefix — access points give each a scoped policy without a monolithic bucket policy", pricing: "No additional charge for access points. You pay standard S3 request and storage rates." },
      { name: "S3 IA", full: "S3 Standard-Infrequent Access", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🪣🔒", tagline: "Cheaper storage, pay to retrieve", analogy: "A filing cabinet in the back room — cheaper to store, but you pay a fee to pull something out.", keyPoints: ["Same durability as Standard (3+ AZs)", "30-day minimum storage duration", "Per-GB retrieval fee"], when: "Backups, DR files accessed a few times per month", pricing: "~$0.0125/GB/month · $0.01/GB retrieval. 30-day minimum." },
      { name: "S3 One Zone-IA", full: "S3 One Zone-Infrequent Access", color: "#E8983C", lightBg: "#FEF5ED", icon: "🪣1️⃣", tagline: "Single AZ, cheaper IA", analogy: "Standard-IA stored in one data centre — cheaper, but data lost if that AZ fails.", keyPoints: ["Single AZ only (vs 3+ for Standard)", "20% cheaper than Standard-IA", "Data at risk if AZ is destroyed"], when: "Reproducible/derived data you rarely access (thumbnails, processed files)", pricing: "~$0.01/GB/month · $0.01/GB retrieval. 30-day minimum." },
      { name: "Intelligent-Tiering", full: "S3 Intelligent-Tiering", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🤖", tagline: "Auto-moves objects between tiers", analogy: "A smart filing system that watches how often you use each file and moves it to the cheapest tier automatically.", keyPoints: ["Monitors access; auto-moves between Frequent/Infrequent/Archive", "No retrieval fees, no minimum duration", "Small per-object monitoring fee"], when: "Unpredictable or changing access patterns", pricing: "~$0.023 – ~$0.0025/GB auto-tiered + $0.0025/1000 objects monitored." },
      { name: "Glacier Instant", full: "S3 Glacier Instant Retrieval", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🧊⚡", tagline: "Archive with millisecond retrieval", analogy: "A deep freezer with a microwave — cheap storage, instant access when you need it.", keyPoints: ["Millisecond retrieval", "90-day minimum duration", "Higher retrieval cost than IA"], when: "Quarterly-access data: medical images, news archives", pricing: "~$0.004/GB/month · $0.03/GB retrieval. 90-day minimum." },
      { name: "Glacier Flexible", full: "S3 Glacier Flexible Retrieval", color: "#7D6608", lightBg: "#FEFCE8", icon: "🧊🕐", tagline: "Very cheap archive, minutes–hours retrieval", analogy: "A warehouse — very cheap, but you order a retrieval and wait for it.", keyPoints: ["Expedited (1–5 min) · Standard (3–5 hr) · Bulk (5–12 hr)", "90-day minimum duration", "Bulk retrievals are free"], when: "Compliance archives retrieved occasionally", pricing: "~$0.0036/GB/month · Expedited $0.03/GB · Bulk free." },
      { name: "Glacier Deep Archive", full: "S3 Glacier Deep Archive", color: "#C0392B", lightBg: "#FDEDEC", icon: "🧊🏔️", tagline: "Cheapest AWS storage — 12hr retrieval", analogy: "A vault buried underground — lowest possible cost, but retrieving anything takes 12 hours.", keyPoints: ["~$0.00099/GB/month — cheapest in AWS", "Standard: 12 hrs · Bulk: up to 48 hrs", "180-day minimum duration"], when: "7–10 year compliance retention (financial, healthcare)", pricing: "~$0.00099/GB/month · Standard retrieval $0.02/GB · Bulk $0.0025/GB." },
      { name: "S3 Storage Lens", full: "Amazon S3 Storage Lens — Org-Wide Storage Analytics Dashboard", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🪣🔭", tagline: "Single dashboard for S3 usage, activity, and cost-optimisation across all accounts and regions", analogy: "A CFO's financial dashboard for all your S3 storage. Instead of calling each department (account) individually to ask how much storage they're using and what they're spending, the dashboard aggregates everything into one view — with recommendations for where to cut costs.", keyPoints: ["S3 Storage Lens provides org-wide visibility into S3 storage: total storage, object count, request activity, and cost-efficiency metrics across all buckets, accounts, and regions in your AWS Organization", "Free tier: 29 usage and activity metrics available at no cost — total storage, object count, bucket count, requests by type (GET, PUT, DELETE). Updated daily", "Advanced metrics (paid): 35+ additional metrics including access pattern analysis, lifecycle rule recommendations, data protection metrics, and detailed cost-efficiency insights (~$0.20/million objects/month)", "Recommendations: flags specific buckets with actionable suggestions — e.g. 'bucket X has 90% of objects with zero recent access — consider a lifecycle policy'. Recommendations appear alongside the metrics that triggered them", "Drill-down: start at org level → account → region → bucket → prefix. Isolate which specific account or bucket is driving storage growth or cost", "Export: daily metrics can be exported to an S3 bucket as CSV or Parquet for custom BI analysis, Athena queries, or QuickSight dashboards", "S3 Storage Lens vs S3 Analytics: Storage Lens = broad org-wide visibility across ALL buckets and accounts (many metric types). S3 Analytics = deep access-pattern analysis on ONE bucket for Standard→Standard-IA transition decisions only. Different tools for different purposes"], when: "Q asks about org-wide S3 storage visibility, usage trends across accounts, or identifying cost optimisation opportunities across all buckets → S3 Storage Lens. Q asks about analysing access patterns in a specific bucket to decide on Standard→IA tiering → S3 Analytics.", pricing: "Free tier: 29 metrics per bucket per day at no charge. Advanced metrics: ~$0.20/million objects/month. Exam tip: Storage Lens = organisation-wide dashboard. S3 Analytics = single-bucket access pattern analysis for Standard→IA recommendation." },
      { name: "S3 Analytics", full: "S3 Analytics — Storage Class Analysis", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣📊", tagline: "Analyses access patterns to recommend Standard → Standard-IA transitions only", analogy: "A librarian who watches which books haven't been checked out in months and flags them for the cheaper back-shelf storage. But the librarian only knows about one type of back shelf (Standard-IA) — they can't recommend the basement archive (Glacier) or the offsite warehouse (Deep Archive). You decide those transitions yourself.", keyPoints: ["S3 Analytics monitors object access frequency in a bucket and generates data-driven recommendations for transitioning objects from Standard to Standard-IA", "Standard → Standard-IA is the ONLY transition S3 Analytics recommends — it does not cover Glacier, Glacier Deep Archive, One Zone-IA, or Intelligent-Tiering transitions", "How it works: enable on a bucket (or prefix/tag filter), and after 30 days of observation it begins producing recommendations. Reports are delivered daily as a CSV to an S3 bucket you specify", "S3 Analytics does NOT create Lifecycle policies automatically — it provides the analysis and recommendation; you must manually create a Lifecycle policy based on the findings", "Visualisation: results are viewable in the S3 console Storage Class Analysis tab, showing access frequency distribution over time (daily, weekly, monthly breakdowns)", "For Glacier/Deep Archive transitions: skip Analytics — configure Lifecycle policies directly based on your known retention requirements (e.g. 'move to Glacier after 90 days'). Analytics won't help with these", "Cost: charged per million objects monitored per month (~$0.10/million objects/month)"], when: "Q asks what S3 Analytics can recommend → Standard to Standard-IA only. Q asks how to identify objects to transition to Standard-IA based on access patterns → S3 Analytics (30 day observation period). Q asks about Glacier transitions → Lifecycle policy directly, not Analytics.", pricing: "Exam trap: S3 Analytics ≠ all storage class transitions. It covers Standard → Standard-IA only. This single-transition limitation is the most tested fact about S3 Analytics. For all other tier decisions, use Lifecycle policies directly." },
      { name: "S3 Lifecycle", full: "S3 Lifecycle Policies", color: "#E8593C", lightBg: "#FEF0ED", icon: "♻️", tagline: "Auto-transition or expire objects", analogy: "A conveyor belt — objects automatically move to cheaper classes or get deleted after N days.", keyPoints: ["Transition: move to cheaper classes after N days. Expiration: auto-delete after N days", "Target rules by prefix, object tags, or minimum object size", "Waterfall model — transitions only flow downward through the tiers, never back up:", "  Standard → Standard-IA → Intelligent-Tiering → One Zone-IA → Glacier Instant → Glacier Flexible → Deep Archive", "Each tier can transition to any tier below it — you can skip tiers (e.g. Standard directly to Deep Archive)", "Minimum durations: 30 days in Standard before → Standard-IA or One Zone-IA; 30 days in Standard-IA before → any Glacier class", "Cannot transition upward — restoring from Glacier creates a temporary copy via a Restore operation, not a Lifecycle rule"], when: "Any bucket where data ages: logs, backups, media files; combine with File Gateway for hybrid NFS tiering", pricing: "Rules are free. Per-request transition fee ~$0.01/1000 requests." },
      { name: "S3 Object Lock", full: "S3 Object Lock (WORM)", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔒🪣", tagline: "Write Once Read Many — prevent deletion or overwrite", analogy: "Laminating a document — once locked, no one can alter or destroy it, not even an admin, until the retention period expires.", keyPoints: ["Two retention modes: Governance (privileged users can override) and Compliance (nobody can — not even root)", "Retention period: set a Retain Until Date on an object version — AWS refuses deletes/overwrites until that date", "Different versions of the same object can each have different retention modes and periods", "Legal Hold: indefinite lock with no expiry date — independent of retention period; toggled on/off by authorised users", "Must enable Object Lock at bucket creation — cannot be added to an existing bucket", "Default retention: set at bucket level applies to new objects; explicit object-level settings override the bucket default"], when: "Compliance data retention (FINRA, SEC, HIPAA); ransomware protection; audit-proof backups", pricing: "No extra charge for Object Lock itself. Normal S3 storage rates apply." },
      { name: "Glacier Vault Lock", full: "S3 Glacier Vault Lock", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒🧊", tagline: "Immutable WORM policy on a Glacier vault — locked forever, even from root", analogy: "Pouring concrete over a filing cabinet. Once the lock is completed, not even AWS or the root account can change or remove the policy — it's permanent by design.", keyPoints: ["Enforces a WORM (Write Once Read Many) compliance policy on a Glacier vault — objects cannot be deleted or overwritten for the retention period defined in the policy", "Two-step lock process: (1) Initiate lock — creates a lock in InProgress state with a 24-hour validation window; test the policy, abort if needed. (2) Complete lock — makes the policy permanent and irreversible. Once completed, it cannot be changed or deleted by anyone including root", "Vault Lock policy (immutable) vs Vault Access policy (mutable): Vault Lock policy defines compliance rules (e.g. deny delete for 7 years) — locked permanently. Vault Access policy = normal IAM-style permissions — can be updated anytime", "Glacier Vault Lock vs S3 Object Lock: Vault Lock = vault-level WORM on Glacier archives. S3 Object Lock = per-object WORM on S3 standard buckets. Both enforce WORM but at different layers and granularity", "Exam triggers: 'regulatory compliance', 'data must not be deleted for X years', 'immutable archive', 'even root cannot delete', 'SEC 17a-4 / HIPAA / FINRA / CJIS' → Glacier Vault Lock"], when: "Long-term compliance archives where data must be provably immutable — financial records, healthcare data, legal documents, audit logs", pricing: "No extra charge for Vault Lock itself. Standard Glacier storage rates apply (~$0.004/GB/month Flexible, ~$0.00099/GB/month Deep Archive)." },
      { name: "S3 Transfer Acceleration", full: "S3 Transfer Acceleration", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🏎️", tagline: "Speed up S3 uploads globally", analogy: "Instead of sending a file across the slow public internet to S3, it enters AWS via the nearest CloudFront edge and travels the fast AWS backbone to your bucket.", keyPoints: ["Uses CloudFront edge network as upload on-ramp", "Biggest benefit for users far from the S3 bucket region", "Enable per-bucket; uses a different endpoint URL", "Useful for large files or globally distributed uploaders"], when: "Users uploading large files to S3 from across the world", pricing: "$0.04/GB additional for transfers accelerated via edge locations outside US. Test first — only pay if faster." },
      { name: "S3 Replication", full: "Amazon S3 Replication (CRR / SRR)", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🪣🔁", tagline: "Automatically copy objects to another bucket — same region or cross-region", analogy: "A photocopier that automatically duplicates every document you file and sends a copy to a branch office — cross-region for DR, same-region for compliance or log aggregation.", keyPoints: ["CRR (Cross-Region Replication): replicate to a bucket in a different region — for DR, geo-compliance, latency reduction for distant users", "SRR (Same-Region Replication): replicate within the same region — for log aggregation across accounts, live staging environments, compliance (keep copy in separate account)", "Versioning required: must be enabled on BOTH source and destination buckets before replication is configured", "Existing objects NOT replicated by default: replication only applies to new objects after the rule is created; use S3 Batch Replication to copy existing objects", "Delete behaviour: delete markers are NOT replicated by default (optional); permanent deletes (version deletions) are never replicated — protects against accidental or malicious deletion at destination", "Ownership override: optionally change object ownership to destination bucket owner — important for cross-account replication so destination account controls the objects", "Replication Time Control (RTC): optional add-on that guarantees 99.99% of objects replicated within 15 minutes — backed by SLA; used when RPO matters", "Replication is asynchronous — not instantaneous; standard replication typically completes in seconds to minutes", "One-time cross-region copy of existing objects — two valid approaches: (1) S3 Batch Replication: configure CRR replication rule via S3 console, run batch replication job to copy existing objects, then delete the replication configuration — fully managed, no CLI needed; (2) aws s3 sync: CLI command that copies new/changed objects from source to destination bucket across regions — simple, scriptable, one-time or repeated", "S3 Batch Replication vs standard CRR: standard CRR only replicates NEW objects after the rule is created. Batch Replication explicitly copies existing objects — use it to backfill or do a one-time migration of objects that predate the replication rule", "aws s3 sync: syncs source → destination, skipping objects that already exist with the same ETag/size. Works cross-region and cross-account. Good for one-time copies or ongoing sync scripts without setting up formal replication rules"], when: "DR to another region (CRR), compliance requiring data in separate account/region, aggregating logs from multiple buckets, reducing read latency for global users by placing copies closer; one-time cross-region copy of existing objects", pricing: "Exam tip: replication transfer fee ~$0.02/GB + standard PUT request costs at destination. RTC adds ~$0.01/GB. Key exam facts: (1) versioning required both ends, (2) existing objects excluded by default, (3) delete markers not replicated by default." },
      { name: "S3 Presigned URLs", full: "Amazon S3 Pre-Signed URLs", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🔗", tagline: "Temporary URL granting time-limited access to a private S3 object — no AWS credentials needed by the recipient", analogy: "A dated guest pass — you sign a note that says 'whoever holds this can enter room 42 until 5pm today'. The guest doesn't need their own key or any AWS account; the pass expires automatically.", keyPoints: ["Generated by an AWS identity (IAM user or role) using the SDK/CLI — embeds the signer's credentials and an expiry time into the URL", "Anyone with the URL can perform the permitted action (GET to download, PUT to upload) until the URL expires — no AWS account or IAM permissions needed by the recipient", "Default expiry: up to 12 hours via AWS Management Console; up to 7 days via SDK/CLI with temporary credentials (STS AssumeRole); shorter is safer", "PUT presigned URL: allow a client to upload directly to S3 without any AWS credentials — client sends a PUT request to the URL with the file; never passes through your server", "Common pattern: your backend generates the presigned URL and returns it to the client; client uploads/downloads directly from S3 — reduces load on your server and eliminates data transit costs", "Security: if the signer's IAM identity loses access (role deleted, SCP applied), existing presigned URLs immediately stop working — tied to the signer's current permissions", "Vs bucket policy: presigned URL = time-limited access for one object. Public bucket policy = permanent access to all matching objects. Presigned URL is safer for controlled sharing"], when: "Temporarily sharing a private S3 file without making the bucket public; direct browser-to-S3 file uploads; time-limited download links for customers", pricing: "Exam tip: free to generate. Standard S3 request and data transfer charges apply when the URL is used. Key exam pattern: 'share private S3 file without credentials' or 'direct upload from browser to S3' → presigned URL." },
      { name: "💾 Block Storage (EBS & Instance Store)", isHeader: true, color: "#E8593C" },
      { name: "EBS Overview", full: "Elastic Block Store", color: "#E8593C", lightBg: "#FEF0ED", icon: "💾", tagline: "Persistent block storage for EC2", analogy: "A hard drive you attach to an EC2. Persists independently — detach from one instance, attach to another.", keyPoints: ["Attached to one EC2 at a time (typically)", "Persists after instance stop/terminate", "Two families: SSD (gp3, gp2, io2, io1) and HDD (st1, sc1)", "Charged for provisioned GB/month — you pay for 100 GB even if you only use 1 GB"], when: "Any EC2 needing persistent disk storage", pricing: "Charged per GB provisioned/month + IOPS for io1/io2. Snapshots ~$0.05/GB/month." },
      { name: "Instance Store", full: "EC2 Instance Store", color: "#7D6608", lightBg: "#FEFCE8", icon: "⚡💾", tagline: "Temporary high-speed local disk physically attached to the host", analogy: "A whiteboard on the wall of your office — blazing fast to write on, but when you leave (stop/terminate the instance), everything gets erased. Don't use it for anything you can't afford to lose.", keyPoints: ["Physically attached to the EC2 host — delivers the highest possible disk I/O (lower latency than EBS)", "Ephemeral: data is lost when the instance is stopped, terminated, or the underlying host fails", "Data survives a reboot but NOT a stop/start or hardware failure", "Cannot be detached and reattached like EBS — tied to the instance lifetime", "Free — included in the instance price (certain instance families: i3, i3en, d2, h1, etc.)", "Use cases: buffers, caches, scratch space, temporary data — anything where loss is acceptable"], when: "High-performance temporary storage: Hadoop shuffle data, cache, temp files, buffers — NOT for anything persistent", pricing: "Free (included with instance). Exam tip: if the question mentions 'highest IOPS' + 'temporary/ephemeral' → Instance Store. If it mentions 'persistent' → EBS." },
      { name: "gp3 (SSD)", full: "General Purpose SSD v3", color: "#2E86AB", lightBg: "#EBF5FB", icon: "💾⚡", tagline: "Default SSD — cheapest, fastest general use", analogy: "A modern NVMe SSD — fast baseline and you can independently dial up IOPS or throughput without paying for more storage.", keyPoints: ["3,000 IOPS & 125 MB/s included free regardless of size", "Scale to 16,000 IOPS & 1,000 MB/s independently", "20% cheaper than gp2 — migrate gp2 → gp3"], when: "Default for most workloads: boot volumes, dev/test, small-to-mid databases", pricing: "~$0.08/GB/month. Extra IOPS: $0.005/IOPS above 3k. Extra throughput: $0.04/MB/s above 125." },
      { name: "gp2 (SSD)", full: "General Purpose SSD v2", color: "#9B59B6", lightBg: "#F5EEF8", icon: "💾🔵", tagline: "Legacy general SSD — IOPS tied to size", analogy: "An older SSD where speed and size are locked together — need more IOPS? Buy more storage, even if you don't need it.", keyPoints: ["IOPS = 3× GB (min 100, max 16,000)", "Burst to 3,000 IOPS for volumes < 1TB", "IOPS and throughput cannot be set independently"], when: "Legacy only — prefer gp3 for all new volumes", pricing: "~$0.10/GB/month. No separate IOPS charge but you overpay on storage to hit IOPS targets." },
      { name: "io2 Block Express", full: "Provisioned IOPS SSD io2 Block Express", color: "#C0392B", lightBg: "#FDEDEC", icon: "💾🔴", tagline: "Highest-performance SSD for critical DBs", analogy: "A race-spec SSD — extreme guaranteed IOPS for your most demanding databases.", keyPoints: ["Up to 256,000 IOPS & 4,000 MB/s", "Sub-millisecond latency", "99.999% durability — highest of any EBS type"], when: "Mission-critical DBs: Oracle, SQL Server, large RDS/Aurora needing consistent ultra-high IOPS", pricing: "~$0.125/GB/month + $0.065/provisioned IOPS/month." },
      { name: "io1 (SSD)", full: "Provisioned IOPS SSD io1", color: "#E8983C", lightBg: "#FEF5ED", icon: "💾🟠", tagline: "Older provisioned IOPS SSD", analogy: "The predecessor to io2 — same concept but lower max IOPS and durability.", keyPoints: ["Up to 64,000 IOPS (vs 256k for io2)", "99.9% durability (vs 99.999% for io2)", "Same price as io2 — io2 is better value"], when: "High-IOPS legacy workloads — AWS recommends io2 for new volumes", pricing: "~$0.125/GB/month + $0.065/IOPS/month. Same as io2, worse specs." },
      { name: "st1 (HDD)", full: "Throughput Optimized HDD", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "💿🟢", tagline: "High-throughput HDD for big sequential reads", analogy: "A spinning disk built for streaming large files — optimised for sequential reading, not random access.", keyPoints: ["Max 500 IOPS & 500 MB/s throughput", "Optimised for large sequential I/O", "Cannot be a boot volume — only SSD types (gp2, gp3, io1, io2) can be boot volumes"], when: "Big data, Kafka, log processing — large sequential files", pricing: "~$0.045/GB/month." },
      { name: "sc1 (HDD)", full: "Cold HDD", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "💿🏔️", tagline: "Cheapest EBS — infrequent sequential access", analogy: "The cheapest spinning disk in AWS — for data you rarely touch but need attached to an EC2.", keyPoints: ["Max 250 IOPS & 250 MB/s", "Lowest cost EBS volume", "Cannot be a boot volume — boot volumes must be SSD-based (gp2, gp3, io1, io2)", "Boot volume = the EBS volume attached as /dev/xvda (root device) that holds the OS; EC2 cannot start without it"], when: "Cold archival data still needing EC2 block access", pricing: "~$0.015/GB/month — cheapest EBS option." },
      { name: "EBS Snapshots", full: "EBS Snapshots", color: "#7D6608", lightBg: "#FEFCE8", icon: "📸💾", tagline: "Point-in-time backup of EBS volumes", analogy: "A photo of your hard drive — stored in S3, incremental after the first.", keyPoints: ["Incremental: only changed blocks after first snapshot", "Copy cross-region for DR", "Restore as new EBS volume or create AMI"], when: "Backing up EC2 disks, cross-region DR, creating AMIs", pricing: "~$0.05/GB/month. Only changed blocks charged after first full snapshot." },
      { name: "I/O Operations", full: "IOPS & Throughput", color: "#2E86AB", lightBg: "#EBF5FB", icon: "📊💾", tagline: "Understanding IOPS vs Throughput", analogy: "IOPS = how many times per second the disk can respond to requests (like transactions per second). Throughput = how much data flows per second (like a pipe width). A random-read DB cares about IOPS; a video stream cares about throughput.", keyPoints: ["IOPS (Input/Output Operations Per Second): key for random read/write DBs", "Throughput (MB/s): key for sequential workloads like big data, video", "gp3 lets you tune both independently — unique to gp3 among EBS types", "High IOPS + low throughput need = io2. High throughput + sequential = st1"], when: "Choosing the right EBS volume type for your workload", pricing: "Context: gp3 gives 3k IOPS free; io2 lets you provision up to 256k IOPS at cost." },
      { name: "📁 Shared File Systems", isHeader: true, color: "#C0392B" },
      { name: "EFS", full: "Elastic File System", color: "#C0392B", lightBg: "#FDEDEC", icon: "📁", tagline: "Shared file system for multiple EC2s — NFS, auto-scaling, two performance modes", analogy: "A shared network drive — multiple EC2 instances (even across AZs) can all read and write to it simultaneously.", keyPoints: ["NFS-based — mount on many EC2s at once; auto-scales storage up and down (no provisioning)", "Two storage tiers: Standard and Infrequent Access (IA) — use Lifecycle Management to auto-move cold files to IA", "Performance modes (set at creation, cannot change):", "  • General Purpose (default): lowest per-operation latency — best for latency-sensitive workloads (web servers, CMS, home directories, dev environments); supports up to ~7,000 file operations/second", "  • Max I/O: higher aggregate throughput and IOPS for massively parallel workloads — slightly higher latency per operation; ideal for big data analytics, media processing, genomics, ML training with thousands of EC2s all reading/writing simultaneously", "Throughput modes (can change after creation):", "  • Bursting (default): throughput scales with storage size — 50 MB/s per TB stored, with burst credits for spikes", "  • Provisioned: set a fixed throughput independent of storage size — use when throughput needs exceed what storage size would give you", "  • Elastic (recommended): automatically scales throughput up and down based on workload — pay per GB transferred, not provisioned; simplest choice for unpredictable workloads", "Access control: IAM policies for mount permissions + Security Groups (port 2049 / NFS) for network access"], when: "Shared file access across multiple EC2 instances or containers. Use Max I/O mode for big data / high-parallelism workloads; General Purpose for everything else.", pricing: "Standard ~$0.30/GB/month · IA ~$0.025/GB/month + $0.01/GB access. Elastic throughput: ~$0.03/GB transferred. ~4× more expensive than EBS but shared across many instances." },
      { name: "FSx for Windows", full: "Amazon FSx for Windows File Server", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗂️🪟", tagline: "Fully managed Windows-native file shares — SMB, NTFS, Active Directory integration", analogy: "A managed Windows file server in the cloud. Your Windows apps, users, and scripts use it exactly like an on-prem Windows file share — same drive letter mapping, same ACLs, same AD group policies.", keyPoints: ["Protocol: SMB (Server Message Block) — the native Windows file-sharing protocol; accessible from Windows, Linux (via CIFS), and macOS", "NTFS file system: full NTFS semantics — Windows ACLs (file/folder permissions), alternate data streams, symbolic links", "Active Directory: integrates with AWS Managed Microsoft AD or your on-prem AD (via AD Connector) — users authenticate with their AD credentials, no separate user management", "Multi-AZ: active + standby file server in two AZs — automatic failover in seconds; standby is synchronously replicated via AWS-managed Windows Server replication", "Single-AZ option: lower cost, one AZ, no automatic failover — for non-critical workloads", "DFS Namespaces: supports Distributed File System Namespaces — aggregate multiple FSx shares under one namespace path (\\\\corp\\shares\\project)", "Shadow copies (VSS): built-in Windows Volume Shadow Copy Service for user-accessible file restore without admin involvement", "Throughput: up to 2 GB/s per file system; SSD or HDD storage tiers", "File Gateway vs FSx for Windows: File Gateway = on-prem apps using SMB/NFS backed by S3 (hybrid). FSx for Windows = managed Windows file server IN AWS for EC2/cloud workloads. Both use SMB but serve different locations"], when: "Windows workloads needing SMB file shares: home directories, lift-and-shift Windows apps, .NET apps using UNC paths, SharePoint backend, SQL Server filestream", pricing: "SSD: ~$0.23/GB/month. HDD: ~$0.013/GB/month + $0.013/GB SSD cache. Throughput capacity: ~$2.20/MBps/month. Exam tip: 'SMB', 'NTFS', 'Active Directory file shares', 'Windows file server' → FSx for Windows. Never EFS for Windows-native workloads." },
      { name: "FSx for Lustre", full: "Amazon FSx for Lustre", color: "#C0392B", lightBg: "#FDEDEC", icon: "🗂️⚡", tagline: "High-performance parallel file system for HPC and ML — sub-millisecond latency, hundreds of GB/s", analogy: "A Formula 1 pit lane of file systems. Thousands of compute nodes all read and write simultaneously to the same file system at extreme throughput — like a data motorway with no traffic lights, built specifically for the biggest compute jobs.", keyPoints: ["Protocol: POSIX-compliant parallel file system (Lustre protocol) — accessible from Linux only (EC2 Linux, EKS, ECS, SageMaker, ParallelCluster)", "Performance: sub-millisecond latency; throughput scales linearly — 100s of GB/s and millions of IOPS across many EC2 instances simultaneously", "S3 integration: link a Lustre file system to an S3 bucket — files are lazily loaded from S3 on first access (no pre-copy needed); results can be exported back to S3; effectively turns S3 into a Lustre backend", "Deployment types: Scratch (ephemeral — no replication, highest throughput per $, 2× replication not included — for short-lived jobs); Persistent (replicated within AZ, for longer-running jobs needing durability)", "Scratch file systems: data is NOT preserved if the file system fails — use for short HPC batch jobs where data can be reloaded from S3", "SSD storage: all Lustre deployments use SSD; choose the throughput tier (50/100/200 MB/s per TiB) to tune cost vs performance", "ParallelCluster / HPC: pairs with AWS ParallelCluster for traditional HPC workloads (CFD, genomics, FEA, seismic)"], when: "HPC, CFD, genomics, ML training with large datasets, video rendering, financial modelling — any workload needing the absolute highest aggregate file system throughput from many EC2s at once", pricing: "Scratch: ~$0.14/GB/month. Persistent: ~$0.145/GB/month. Exam tip: 'HPC', 'ML training', 'high-throughput parallel I/O', 'Linux only', 'S3 as data lake backend' → FSx for Lustre. Contrast: EFS = shared NFS for general Linux workloads; Lustre = extreme HPC parallelism." },
      { name: "FSx for OpenZFS", full: "Amazon FSx for OpenZFS", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🗂️🐧", tagline: "Managed OpenZFS file system — NFS for Windows, Linux, and macOS with ZFS features", analogy: "A managed ZFS server in the cloud. If you rely on ZFS snapshots, clones, and compression for your Linux/macOS data management workflows, FSx for OpenZFS gives you all of that without running a ZFS server yourself.", keyPoints: ["Protocol: NFS v3/v4 — accessible from Linux, macOS, and Windows (via NFS client); broad cross-platform compatibility without needing Windows AD", "ZFS features: data compression (LZ4 — often reduces storage by 30–50%), instant snapshots (no downtime, space-efficient), clones from snapshots (copy-on-write, near-zero time), deduplication (optional)", "Performance: SSD-based; up to 1 million IOPS and 12.5 GB/s throughput; low latency (sub-millisecond for in-memory I/O)", "In-memory caching: FSx for OpenZFS has a configurable in-memory read cache — frequently accessed data served at memory speed", "Snapshots: point-in-time snapshots are instant and space-efficient (only changed blocks are stored); users can self-restore files from snapshots via the .zfs/snapshot directory", "Data management: volume hierarchy within the file system — create nested volumes, each with its own quotas, compression settings, and snapshot schedules", "Migration: lift-and-shift from on-prem ZFS/NFS servers without converting data format or changing client mount syntax"], when: "Workloads currently on on-prem ZFS or NFS servers (Linux/macOS/mixed); development environments needing fast clones of data; any NFS workload wanting ZFS data management features (snapshots, compression)", pricing: "SSD storage: ~$0.09/GB/month. Throughput capacity: ~$0.20/MBps/month. Exam tip: 'NFS', 'Windows + Linux + macOS', 'ZFS features', 'snapshots and clones', 'migrate on-prem NFS/ZFS' → FSx for OpenZFS. Contrast with EFS: EFS auto-scales storage, FSx OpenZFS is provisioned; OpenZFS offers ZFS-specific features EFS lacks." },
      { name: "FSx for ONTAP", full: "Amazon FSx for NetApp ONTAP", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🗂️🔀", tagline: "Managed NetApp ONTAP — SMB and NFS simultaneously, Windows + Linux + macOS", analogy: "A cloud-hosted NetApp filer. If your on-prem storage is NetApp ONTAP, FSx for ONTAP is a drop-in replacement — same SnapMirror replication, same ONTAP CLI, same dual-protocol SMB+NFS support.", keyPoints: ["Dual protocol: simultaneously serves both SMB (Windows) and NFS (Linux/macOS) from the same file system and even the same files — the only FSx variant to support both protocols at once", "Cross-platform: Windows, Linux, and macOS clients all access the same data — ideal for mixed-OS environments without separate file servers", "NetApp ONTAP features: SnapMirror (replication to on-prem ONTAP or other FSx ONTAP), FlexClone (instant zero-cost clones), deduplication, compression, tiering to S3 (cold data auto-tiered to S3 storage)", "iSCSI: also supports iSCSI block storage — can serve SAN workloads alongside NAS, from the same system", "Storage tiering: hot data on SSD; cold data automatically tiered to S3 capacity pool (~$0.025/GB vs ~$0.18/GB for SSD) — significant cost saving for large datasets with mixed access patterns", "Multi-AZ: synchronous replication between two AZs with automatic failover; single-AZ option also available", "SnapMirror: replicate data between FSx ONTAP and on-prem NetApp systems bidirectionally — enables hybrid cloud storage or migration path from on-prem", "Migration: use ONTAP's built-in SnapMirror to migrate on-prem NetApp data to FSx ONTAP with zero downtime"], when: "On-prem NetApp ONTAP migration; mixed Windows + Linux environments needing a single shared file system; workloads needing both SMB and NFS simultaneously; SAN + NAS from one system", pricing: "SSD: ~$0.18/GB/month. Capacity pool (S3 tier): ~$0.025/GB/month. Exam tip: 'SMB and NFS simultaneously', 'Windows + Linux + macOS all accessing same data', 'NetApp ONTAP', 'dual-protocol' → FSx for ONTAP. The only FSx option that serves both SMB and NFS from the same file system." },
      { name: "🔌 Backup & Hybrid Storage", isHeader: true, color: "#7D6608" },
      { name: "AWS Backup", full: "AWS Backup", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🛡️", tagline: "Centralised backup across AWS services", analogy: "A single control panel to schedule and manage backups for all your AWS resources.", keyPoints: ["Policy-driven backup schedules", "Covers EC2, RDS, EFS, DynamoDB, EBS & more", "Cross-region & cross-account support"], when: "Compliance, DR, managing backups centrally across services", pricing: "Pay storage rates + $0.01/GB/month for Vault Lock (WORM compliance)." },
      { name: "Storage Gateway", full: "AWS Storage Gateway", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔌", tagline: "Bridge between on-prem and AWS", analogy: "A translator that lets your on-premises servers talk to AWS storage seamlessly.", keyPoints: ["Three modes: File Gateway (NFS/SMB → S3), Volume Gateway (iSCSI block → S3/EBS), Tape Gateway (virtual tape library → S3/Glacier)", "Runs as a VM appliance on-premises or as a hardware appliance", "All modes cache frequently accessed data locally; cold data lives in AWS"], when: "Hybrid cloud — extend on-prem storage to AWS without changing existing protocols", pricing: "~$0.01–$0.03/GB transferred + underlying AWS storage costs (S3, EBS, Glacier)." },
      { name: "File Gateway", full: "AWS Storage Gateway — File Gateway", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔌📁", tagline: "NFS/SMB file shares backed by Amazon S3", analogy: "A local filing cabinet that silently syncs everything to S3 behind the scenes — your apps use NFS as normal, but the files actually live in the cloud.", keyPoints: ["Presents NFS (v3/v4.1) or SMB file shares to on-premises applications — no protocol changes needed", "Files written to the share are stored as objects in S3 — one file = one S3 object", "Local cache on the gateway appliance keeps recently accessed files available at low latency", "Works with all S3 storage classes — pair with S3 Lifecycle policies to auto-tier cold data to IA or Glacier", "Supports S3 versioning, replication, and Object Lock on the underlying bucket", "File Gateway vs FSx for Windows: File Gateway = on-prem appliance bridging local SMB/NFS to S3 (hybrid, data stays in S3). FSx for Windows = a real managed Windows file server running IN AWS for cloud workloads. Key question: where are the apps? On-prem → File Gateway. In AWS → FSx for Windows"], when: "On-prem workloads using NFS/SMB that need cloud scalability + S3 cost tiers without changing app code", pricing: "No gateway fee. Pay S3 storage rates + $0.01–$0.03/GB data written. Local cache on-prem hardware." },
      { name: "Volume Gateway", full: "AWS Storage Gateway — Volume Gateway", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔌💾", tagline: "iSCSI block volumes backed by AWS — cached or stored", analogy: "An iSCSI hard drive your servers see locally, but the data is actually in S3 — like a cloud hard drive mounted on-prem.", keyPoints: ["Presents iSCSI block volumes to on-premises servers — works like an attached disk", "Cached mode: primary data in S3, frequently accessed blocks cached locally (minimises on-prem storage)", "Stored mode: full data set on-premises, asynchronously backed up to S3 as EBS snapshots (full local access, cloud backup)", "EBS snapshots can be restored as EBS volumes in AWS for quick EC2 attachment", "Use stored mode for applications needing low-latency access to the entire dataset; cached mode for everything else"], when: "On-prem block storage workloads (databases, apps using iSCSI) needing cloud backup or gradual migration to EBS", pricing: "~$0.03/GB stored in cached mode · ~$0.03/GB snapshot in stored mode + $0.05/GB/month EBS snapshot." },
      { name: "Tape Gateway", full: "AWS Storage Gateway — Tape Gateway (VTL)", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔌📼", tagline: "Virtual Tape Library backed by S3 and Glacier — replace physical tape without changing backup software", analogy: "A digital cassette player that your old backup software thinks is real tape. The 'tapes' actually live in S3 and archive to Glacier — infinite capacity, no physical media, same interface your backup app already knows.", keyPoints: ["Presents a Virtual Tape Library (VTL) via iSCSI to on-premises backup applications — Veeam, NetBackup, Backup Exec, Veritas all work without modification", "Virtual tapes: created on-demand up to 5 TB each; stored in a virtual tape library (backed by S3) while 'in the drive'; archived to virtual tape shelf (S3 Glacier or Glacier Deep Archive) when 'ejected'", "The backup software has no idea the tapes aren't physical — it uses the same workflows and job schedules it always has", "Tape retrieval: archived tapes must be retrieved from Glacier to the VTL before the backup software can read them (same as physical tape recalls)", "Capacity: no physical tape limit — virtual tape shelf scales automatically; remove the tape rotation/offsite logistics entirely", "Cost: eliminates physical tape hardware, media, shipping, and offsite storage contracts; Glacier Deep Archive is ~$0.00099/GB/month"], when: "Migrating existing tape-based backup environments to cloud without rewriting backup jobs or replacing backup software — keep the process, eliminate the hardware", pricing: "Virtual tape storage in VTL (S3): ~$0.023/GB/month. Archived tape (Glacier Flexible): ~$0.0036/GB/month. Deep Archive: ~$0.00099/GB/month. Exam tip: if the question mentions 'existing backup software', 'tape library', or 'no change to backup process' → Tape Gateway." },
    ]
  },
  databases: {
    tab: "🗃️ Databases",
    items: [
      { name: "🗃️ Relational (SQL)", isHeader: true, color: "#2E86AB" },
      { name: "RDS", full:"Relational Database Service", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗃️", tagline: "Managed SQL databases", analogy: "A rows-and-columns database managed by AWS — backups, patching, failover included.", keyPoints: ["Engines: MySQL, PostgreSQL, Oracle, SQL Server, MariaDB", "Multi-AZ: synchronous replication to a standby in a different AZ within the same region — standby is not readable, exists only for automatic failover; ~1–2 min failover", "Read Replicas: asynchronous replication — can be within the same AZ, cross-AZ, or cross-region; readable and serve SELECT queries; up to 15 replicas",
          "Data transfer charges apply for cross-region replication — cross-region Read Replicas and Aurora Global DB both incur per-GB data transfer fees between regions; within the same region replication (Multi-AZ, same-region Read Replica) has no transfer charge", "Key distinction — the exam framework: Multi-AZ = HA (automatic same-region failover, standby not readable, ~1–2 min RTO). Read Replicas = scalability (offload read traffic, asynchronous, readable). Cross-region Read Replica = DR (readable standby in another region, manually promoted on disaster). Aurora Global Database = multi-region DR (managed, sub-second replication, <1 min RTO)", "Promoting a Read Replica: makes it a standalone DB — breaks replication; used as a DR last resort (possible data lag due to async)", "RDS Custom: for Oracle and SQL Server — gives DBA SSH access to the underlying OS and DB engine for specialised customisations (patches, settings, agents); supports Multi-AZ for HA", "Standard RDS: AWS manages OS + engine fully — you cannot SSH in or modify engine internals", "IAM authentication to RDS: instead of username/password, Lambda generates a short-lived auth token (15 min expiry) using its IAM role credentials via generate-db-auth-token. RDS validates the token against IAM on each connection — no password stored anywhere. Requires: (1) IAM role attached to Lambda with rds-db:connect permission, (2) IAM auth enabled on the RDS instance. Both are required — neither alone is sufficient", "Exam trigger: 'Lambda → RDS, short-lived credentials, improve security' → IAM role on Lambda + IAM authentication enabled on RDS"], when: "Traditional relational data — orders, users, transactions; use RDS Custom for Oracle/SQL Server needing OS or engine customisation", pricing: "On-Demand or Reserved. db.t3.micro ~$0.017/hr. Multi-AZ doubles cost. Storage ~$0.115/GB/month." },
      { name: "Aurora", full: "Amazon Aurora", color: "#E8593C", lightBg: "#FEF0ED", icon: "✨", tagline: "AWS's supercharged SQL database", analogy: "RDS rebuilt from scratch — 5× faster than MySQL, auto-scaling, 6-way replication across 3 AZs.", keyPoints: ["MySQL & PostgreSQL compatible", "6 copies across 3 AZs automatically", "Up to 15 read replicas", "Shared distributed storage: primary and ALL replicas read from the same storage volume — no binlog shipping between instances. This is why Aurora replication lag is typically <100ms (often single-digit ms), vs RDS MySQL replicas which ship binary logs over the network and can fall behind under heavy writes", "RDS MySQL read replica lag problem: each replica is a separate copy; under peak write traffic the binary log queue builds up and replicas fall behind. Aurora eliminates this entirely by design", "Aurora Auto Scaling: automatically adds/removes Aurora Replicas based on CPU or connection metrics — handles traffic spikes without manual intervention", "Exam scenario: RDS MySQL read replicas lagging under peak load + minimal code changes → migrate to Aurora MySQL + Aurora Replicas + Aurora Auto Scaling", "Framework: 6 copies across 3 AZs = built-in HA (no separate Multi-AZ config needed). Aurora Replicas = read scalability (up to 15, same shared storage, <100ms lag). Aurora Global Database = multi-region DR (up to 5 secondary regions, sub-second replication, <1 min RTO on failover)"], when: "High-performance relational workloads; drop-in MySQL/PostgreSQL replacement", pricing: "~$0.10/ACU-hr (Serverless v2). Storage ~$0.10/GB/month." },
      { name: "Aurora Serverless", full: "Amazon Aurora Serverless v2", color: "#9B59B6", lightBg: "#F5EEF8", icon: "✨🗄️", tagline: "Aurora that scales compute capacity automatically — pay per ACU-second", analogy: "A database that breathes — it scales up instantly when queries hit and scales back down to near-zero when idle, so you pay for only the compute you actually use.", keyPoints: ["Aurora Serverless v2: auto-scales in fine-grained increments (0.5 ACU steps) from minimum to maximum ACU range you define — no manual instance resizing", "ACU (Aurora Capacity Unit): ~2 GB RAM + proportional CPU + networking; billing per ACU-second consumed", "Scales in < 1 second — much faster than v1 which had a cold-start pause; suitable for production workloads", "Minimum ACU = 0 (v1 only, can pause to zero); v2 minimum is 0.5 ACU — v2 does NOT pause to zero but near-zero cost when idle", "Supports Multi-AZ, global databases, read replicas — same Aurora features as provisioned, just with auto-scaling compute", "Compatible with Aurora MySQL and Aurora PostgreSQL — drop-in replacement for provisioned Aurora", "Use case fit: variable or unpredictable workloads, dev/test environments, multi-tenant SaaS, new apps with unknown traffic patterns", "v1 vs v2: v1 can scale to zero (pause) but has cold-start latency and scales in larger steps; v2 is faster, finer-grained, and recommended for most workloads"], when: "Variable or spiky workloads where provisioning a fixed instance size wastes money; dev/test environments; any Aurora use case where you want automatic compute scaling", pricing: "~$0.12/ACU-hour (MySQL) or ~$0.12/ACU-hour (PostgreSQL). Storage same as Aurora: $0.10/GB/month. Exam tip: no charge when paused (v1 only); v2 charges minimum 0.5 ACU." },
      { name: "Aurora Global DB", full: "Aurora Global Database", color: "#C0392B", lightBg: "#FDEDEC", icon: "🌍✨", tagline: "Aurora replicated across regions", analogy: "Aurora with a shadow copy in another continent — failover in under a minute.", keyPoints: ["1 primary + up to 5 secondary regions", "Sub-second replication lag", "~1 min RTO for cross-region failover"], when: "Global apps needing low-latency reads; cross-region DR", pricing: "Aurora pricing + ~$0.20/million replicated write I/Os cross-region." },
      { name: "HA / DR", full: "High Availability & Disaster Recovery", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔄", tagline: "Staying up & recovering fast", analogy: "HA = never going down. DR = getting back up fast when something does go wrong.", keyPoints: ["RTO = how fast you recover", "RPO = how much data you can lose", "Multi-AZ = HA; Multi-Region = DR"], when: "Every production system — plan for both", pricing: "Multi-AZ typically doubles DB cost. DR cost scales with standby strategy." },
      { name: "⚡ NoSQL", isHeader: true, color: "#E8983C" },
      { name: "DynamoDB", full: "Amazon DynamoDB", color: "#9B59B6", lightBg: "#F5EEF8", icon: "⚡", tagline: "Serverless NoSQL key-value store", analogy: "A super-fast dictionary — look things up by key, no rigid table structure needed.", keyPoints: ["Single-digit millisecond latency at any scale", "On-demand or provisioned capacity", "TTL: auto-expire items at a set timestamp", "DynamoDB Streams: ordered event feed of all inserts/updates/deletes — triggers Lambda for real-time processing, cross-region replication, or audit logging", "Framework: DynamoDB is Multi-AZ by default within a region = built-in HA. Global Tables = multi-region active-active DR (reads and writes in every region). DAX = read scalability (microsecond cache in front of DynamoDB)"], when: "Massive-scale apps: gaming leaderboards, carts, IoT", pricing: "On-Demand: ~$1.25/million writes, $0.25/million reads. 25GB free forever." },
      { name: "DAX", full: "DynamoDB Accelerator", color: "#E8593C", lightBg: "#FEF0ED", icon: "⚡🗃️", tagline: "In-memory cache for DynamoDB", analogy: "ElastiCache but built specifically for DynamoDB — sits in front of your DynamoDB table and serves hot reads at microsecond speed without changing your app code.", keyPoints: ["Microsecond latency (vs single-digit ms for DynamoDB)", "Fully managed, in-memory caching cluster", "Drop-in compatible — uses same DynamoDB API", "Ideal for read-heavy or bursty workloads"], when: "DynamoDB tables with high read traffic or cost issues from repeated identical queries", pricing: "dax.r5.large ~$0.269/hr per node. Requires multi-node cluster for HA (~$0.538/hr minimum)." },
      { name: "TTL", full: "DynamoDB Time To Live — Auto-Delete Items at a Timestamp", color: "#7D6608", lightBg: "#FEFCE8", icon: "⏱️🗃️", tagline: "Automatically delete DynamoDB items after a Unix timestamp — free, no write capacity consumed", analogy: "A best-before date stamped on every item in the pantry. When the date passes, the item is automatically thrown out without you having to check manually. You set the date when you put the item in — the pantry handles the rest.", keyPoints: ["Enable TTL on a table by designating one attribute as the TTL attribute (e.g. expires_at). Store a Unix epoch timestamp (seconds since Jan 1 1970) in that attribute on each item you want to expire", "DynamoDB scans for expired items in the background and deletes them automatically — typically within 48 hours of the timestamp passing (usually much faster, but not guaranteed to the exact second)", "TTL deletions are free: they do not consume write capacity units (WCUs) and do not count toward your provisioned or on-demand throughput", "Expired items may still be readable: between the TTL timestamp passing and actual deletion, the item can still appear in reads. Filter expired items in your app if precision matters (e.g. WHERE expires_at > now())", "DynamoDB Streams integration: TTL deletions appear in the Streams event log — trigger a Lambda function on item expiry for cleanup, archival, or notifications", "ElastiCache/Redis TTL: set per-key in seconds via EXPIRE or SETEX commands. Different mechanism (in-memory, immediate) vs DynamoDB TTL (eventual, background)", "Use cases: session tokens, temporary auth codes, OTP tokens, rate-limit windows, shopping cart abandonment, event data you only keep for N days, cache items with freshness requirements"], when: "Q asks how to automatically expire or delete DynamoDB items after a period without application code → TTL. 'Session data that expires after 24 hours', 'auto-delete old records', 'reduce table size automatically' → DynamoDB TTL.", pricing: "TTL in DynamoDB is free — no extra cost, no WCU charge for deletions. Exam tip: TTL deletions are NOT immediate (up to 48 hrs). Expired items CAN still be read — always filter client-side if your app is sensitive to stale data." },
      { name: "🔄 In-Memory Caching", isHeader: true, color: "#C0392B" },
      { name: "ElastiCache", full:"Amazon ElastiCache", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🧠", tagline: "In-memory cache in front of any slow or repeated data source", analogy: "A sticky note on your monitor — faster to read than opening a filing cabinet every single time. First request goes to the cabinet (DB), result gets written on the sticky note. Every request after that reads the note. The cabinet barely gets touched.", keyPoints: ["Sits in front of ANY data source — RDS, Aurora, DynamoDB, compute results, external APIs, or data that was never in a DB (sessions, counters, leaderboards)", "How it works: app checks cache first → if found (cache hit) return instantly; if not found (cache miss) fetch from source, store in cache, return result — subsequent requests skip the source entirely", "Two engines: Redis (default choice — rich data types, persistence, Multi-AZ, pub/sub, TTL, sorted sets) vs Memcached (simple key-value only, pure horizontal scaling, no replication or persistence)", "Use Redis for: session storage, leaderboards (sorted sets), rate-limiting counters, pub/sub, anything needing HA or persistence", "Use Memcached for: pure simple cache at massive horizontal scale with no other requirements — rarely chosen on the exam", "NOT only for databases — also stores session state (user login, shopping cart), pre-computed results, config data, API responses, HTML fragments", "NOT a fix for connection exhaustion (use RDS Proxy) and NOT for DynamoDB specifically (use DAX — purpose-built)"], when: "Repeated identical queries slowing DB → cache results. Users losing sessions after Auto Scaling → store sessions in Redis. Microsecond read latency needed → ElastiCache. Reduce DB load → ElastiCache in front. Leaderboard / real-time ranking → Redis sorted sets.", pricing: "cache.t3.micro ~$0.017/hr. Reserved saves up to 55%. Exam tip: ElastiCache = repeated reads / session state / leaderboards. RDS Proxy = connection pooling. DAX = DynamoDB-specific cache. These three are commonly confused." },
      { name: "Redis", full: "Redis (via ElastiCache)", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔴", tagline: "Advanced in-memory data store — single-threaded, rich data types, persistence", analogy: "Memcached is a simple whiteboard with multiple writers working in parallel. Redis is a whiteboard with sorting, pub/sub, persistence, and TTLs — but only one person writes at a time (single-threaded).", keyPoints: ["Rich types: lists, sets, sorted sets, hashes", "Persistence: RDB snapshots or AOF log", "Pub/Sub + native TTL on every key", "Single-threaded: Redis processes commands one at a time on a single core — it does NOT support multi-threading. If a question asks for multi-threaded caching or maximum CPU utilisation across cores, Redis is the wrong answer", "Memcached supports multi-threading: if the use case explicitly requires multi-threaded performance (e.g. CPU-intensive caching, parallel command execution across cores), Memcached is the correct choice over Redis"], when: "Sessions, leaderboards, pub/sub, rate limiting, queues — any workload needing rich data types, HA, or persistence. NOT the right fit when multi-threading or full CPU core utilisation is the explicit requirement.", pricing: "Same ElastiCache pricing. Multi-AZ = 2×. Global Datastore adds cross-region cost. Exam trap: 'multi-threaded' in the question → Memcached, not Redis." },
      { name: "Global Datastore", full: "ElastiCache Global Datastore", color: "#E8983C", lightBg: "#FEF5ED", icon: "🌍🧠", tagline: "Redis replicated across regions — multi-region DR for your cache", analogy: "Aurora Global Database but for Redis — cache mirrored to another region for global reads and failover.", keyPoints: ["1 primary + up to 2 secondary regions", "~1 second replication lag", "Secondaries read-only; promote on failover", "Framework: Redis Multi-AZ = HA (automatic failover to standby replica in another AZ, same region). Redis Global Datastore = multi-region DR (cross-region replication, promote secondary on disaster). Redis read replicas = read scalability (offload reads within a region)"], when: "Global apps needing low-latency cache; Redis DR across regions", pricing: "Standard ElastiCache per cluster + ~$0.20/GB replicated cross-region." },
      { name: "🕸️ Specialized Databases", isHeader: true, color: "#9B59B6" },
      { name: "Neptune", full:"Amazon Neptune", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🕸️", tagline: "Graph database", analogy: "Great at answering 'who is connected to whom' — social networks, fraud rings, knowledge graphs.", keyPoints: ["Supports Gremlin & SPARQL", "Optimised for relationship traversal", "Fully managed, Multi-AZ"], when: "Social networks, fraud detection, knowledge graphs", pricing: "db.r5.large ~$0.35/hr + $0.10/GB/month storage." },
      { name: "RDS Proxy", full: "Amazon RDS Proxy", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔄🗄️", tagline: "Connection pooler between app and RDS/Aurora", analogy: "A hotel concierge desk — instead of every guest calling the kitchen directly (exhausting the chef), all requests go through the desk which queues and batches them efficiently.", keyPoints: ["Maintains a warm pool of DB connections; multiplexes thousands of app connections onto far fewer DB connections", "Critical for Lambda: each invocation would otherwise open a new DB connection — proxy reuses pooled ones", "Credentials stored in Secrets Manager — enforces best practice, supports IAM auth to proxy", "Reduces Multi-AZ failover time by ~66% (proxy absorbs the switchover, app reconnects to proxy only)", "Supports RDS MySQL, PostgreSQL, MariaDB, SQL Server and Aurora"], when: "Lambda or containerised apps hitting RDS/Aurora at high concurrency; reducing 'too many connections' errors", pricing: "Charged per vCPU of the underlying RDS instance per hour. ~$0.015/vCPU-hr." },
    ]
  },
  security: {
    tab: "🔒 Security",
    items: [
      { name: "🔐 Identity & Access", isHeader: true, color: "#C0392B" },
      { name: "IAM", full:"Identity & Access Management", color: "#E8593C", lightBg: "#FEF0ED", icon: "🪪", tagline: "Who can do what in AWS", analogy: "A keycard system — each person/service gets a card that only opens allowed doors.", keyPoints: ["Users, Groups, Roles, Policies (JSON)", "Principle of least privilege", "Roles for services — no hardcoded credentials", "EC2 + IAM role: use an instance profile (not adding the instance ID to the trust policy — EC2 instances are not IAM principals). Trust policy principal must be the service: ec2.amazonaws.com", "Trust policy = who can ASSUME the role. Permission policy = what the role can DO. Both are required"], when: "Always — every AWS account uses IAM", pricing: "Free." },
      { name: "IAM Policies", full: "IAM Policy Types & Evaluation Logic", color: "#E8593C", lightBg: "#FEF0ED", icon: "📜🔑", tagline: "Six policy types — and an explicit Deny always wins", analogy: "Identity-based policies are your staff badge listing what rooms you can enter. Resource-based policies are a sign on the door listing who's allowed in. Permission boundaries are a hard ceiling — even if your badge says yes, the boundary can still say no.", keyPoints: ["Identity-based policies: attached to IAM users, groups, or roles — define what actions the principal can perform. Types: AWS Managed (AWS maintains), Customer Managed (you own), Inline (embedded in one principal, not reusable)", "Resource-based policies: attached directly to a resource (S3 bucket policy, KMS key policy, SQS queue policy, SNS topic policy, Lambda resource policy). Must specify a Principal. Enable cross-account access without role assumption", "Trust policies: a special resource-based policy on an IAM role defining which principal can assume that role (sts:AssumeRole). Required on every IAM role", "Permission boundaries: a managed policy attached to a user or role that caps the maximum permissions — even if identity-based policies grant more, the boundary is the ceiling. Used to safely delegate user/role creation", "Service Control Policies (SCPs): applied at AWS Organizations OU or account level — restrict what any principal in that scope can do regardless of their IAM policies. Cannot grant permissions, only restrict. See Governance tab", "Session policies: passed when calling AssumeRole / AssumeRoleWithWebIdentity — further restrict the temporary session beyond the role's own policies. Effective permissions = intersection of role policy + session policy", "Evaluation order: explicit Deny wins → SCP check → resource-based policy (cross-account) → identity-based policy → permission boundary → session policy. Access only granted if no Deny AND at least one explicit Allow exists", "Cross-account access: requires Allow in both the identity-based policy of the calling principal AND the resource-based policy (or role trust policy) of the target resource/role"], when: "Designing least-privilege access: identity-based for user/role permissions, resource-based for cross-account, SCPs for org guardrails, permission boundaries for delegated admin", pricing: "All IAM policy types are free. No cost to create, attach, or evaluate policies." },
      { name: "SSO / IAM Identity Center", full: "AWS IAM Identity Center (SSO)", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔑👥", tagline: "Single sign-on across AWS accounts and apps", analogy: "A master key — one login gives you access to all your AWS accounts and business apps (Salesforce, Slack, etc.) without re-entering credentials.", keyPoints: ["One login to access multiple AWS accounts in AWS Organizations", "Integrates with Azure AD, Okta, Google Workspace (SAML/OIDC)", "Replaces manually managing IAM users in each account", "Supports permission sets for role-based access"], when: "Multiple AWS accounts, enterprise SSO, replacing per-account IAM users", pricing: "Free for AWS managed accounts. External IdP integrations are free too." },
      { name: "Cognito", full: "Amazon Cognito", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🤝", tagline: "User sign-up, sign-in & auth for web/mobile apps", analogy: "A ready-made login system — registration, passwords, MFA, social login out of the box.", keyPoints: ["Two distinct components: User Pools (authentication) and Identity Pools (AWS authorisation)", "User Pools: a managed user directory — handles sign-up, sign-in, password reset, MFA, email/phone verification, and issues JWT tokens (ID token, access token, refresh token)", "User Pools support: username/password login, social login (Google, Facebook, Apple), SAML/OIDC federation with corporate IdPs", "Identity Pools (Federated Identities): exchange a Cognito User Pool JWT (or any trusted identity) for temporary AWS credentials via STS AssumeRoleWithWebIdentity", "Identity Pools allow unauthenticated guest access (limited IAM role) and authenticated access (different IAM role with broader permissions)", "Common pattern: User Pool authenticates the user → Identity Pool grants that user temporary IAM credentials to call S3, DynamoDB, API Gateway, etc. directly from the client"], when: "User Pool: adding login/auth to web or mobile apps. Identity Pool: granting end users scoped AWS service access (e.g. mobile app uploading to S3 directly)", pricing: "50k MAUs free · $0.0055/MAU after. Identity Pool: 50k sync operations free." },
      { name: "Directory Service", full: "AWS Directory Service (Managed Microsoft AD)", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗂️👥", tagline: "Managed Microsoft Active Directory in AWS — domain join EC2, federate SSO, enable LDAP auth", analogy: "The corporate phonebook and keycard system that Windows-based systems rely on. AWS hosts and manages it so you get all the benefits of AD (domain joining, group policies, Kerberos, LDAP) without running your own domain controllers.", keyPoints: ["Three flavours: (1) AWS Managed Microsoft AD — full AD domain controllers in AWS, trusts on-prem AD, supports MFA; best for cloud-native AD or extending on-prem to cloud. (2) AD Connector — a proxy that forwards authentication requests to your on-prem AD; no users stored in AWS; on-prem AD must be reachable. (3) Simple AD — Samba-based, no real Microsoft AD; cheapest, for small orgs with basic LDAP needs only", "Domain join EC2: instances in your VPC join the AD domain automatically on launch — apply Group Policy, Kerberos auth, centralized patch management", "RDS for SQL Server and Amazon FSx for Windows: both require AWS Managed Microsoft AD for Windows authentication", "AWS Transfer Family: use AD Connector or Managed AD as the identity provider for SFTP users — maps SFTP logins to AD accounts", "AWS IAM Identity Center (SSO) integration: connect to Managed AD or AD Connector so employees use corporate credentials to access AWS Console and apps", "Trust relationship: Managed AD supports one-way and two-way trusts with on-prem AD — enables single sign-on across hybrid environments without syncing passwords to AWS"], when: "Windows workloads on EC2 needing domain join; SQL Server on RDS needing Windows auth; FSx for Windows; Transfer Family SFTP with corporate identities; migrating on-prem Windows apps to AWS", pricing: "Standard edition: ~$0.05/hr per directory (2 DCs). Enterprise edition: ~$0.19/hr. AD Connector: ~$0.05/hr for small. Exam tip: 'domain join EC2', 'Windows auth for RDS/FSx', 'AD-integrated SFTP' → AWS Directory Service Managed AD." },
      { name: "IAM Access Analyzer", full: "AWS IAM Access Analyzer", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔍🔑", tagline: "Who outside my account can access my resources?", analogy: "A security guard who reads every sign on every door in your building and reports back: 'Room 3 has a sign saying anyone from the building next door can walk in — did you mean to do that?' It doesn't lock doors; it just tells you which ones are open to outsiders.", keyPoints: ["Continuously analyses resource-based policies and flags any that grant access to an external principal — an entity outside your defined zone of trust (your account or AWS Organization)", "Zone of trust: you define the boundary. Typically your account (finds cross-account + public access) or your entire AWS Org (finds only truly external access, suppresses known inter-account access)", "Resources it checks: S3 bucket policies, IAM role trust policies, KMS key policies, SQS queue policies, Lambda resource policies, Secrets Manager secrets, SNS topics", "Finding = a specific resource + which external principal + which actions are allowed. Each finding has status: Active (investigate), Archived (expected/approved), Resolved (policy fixed)", "Archive rules: suppress known-good findings — e.g. 'a specific partner account intentionally has access to this S3 bucket' → archive so it doesn't keep reappearing", "Policy validation: checks IAM policies you're writing for syntax errors, typos, and security best practices before you apply them — gives actionable warnings", "Policy generation: analyses CloudTrail logs of what an IAM principal actually did over a time period, then generates a least-privilege policy based on real observed usage — stops over-permissioning", "Does NOT make changes — purely advisory/audit; you decide how to act on findings"], when: "Q asks: 'identify resources unintentionally exposed to the internet or other accounts' → IAM Access Analyzer. 'Generate least-privilege policy from actual usage' → Access Analyzer policy generation. 'Validate IAM policy before applying' → Access Analyzer policy validation.", pricing: "Free for basic findings. Policy generation uses CloudTrail — charges apply for CloudTrail if not already enabled. Exam tip: GuardDuty = active threat detection (suspicious behaviour). Access Analyzer = passive auditing (who has access to what). Inspector = vulnerability scanning (CVEs). These three are commonly confused." },
      { name: "🔑 Encryption & Secrets", isHeader: true, color: "#7D6608" },
      { name: "KMS", full:"Key Management Service", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔐", tagline: "Manage encryption keys", analogy: "A secure bank vault for encryption keys — controls who can use them to lock or unlock data.", keyPoints: ["CMK: you control rotation & policy", "AWS-managed keys: automatic, free", "Envelope encryption: KMS encrypts a data key, data key encrypts your data"], when: "Encrypting S3, EBS, RDS, DynamoDB, Lambda, etc.", pricing: "$1/month per CMK · $0.03/10,000 API calls. AWS-managed keys free." },
      { name: "Secrets Manager", full: "AWS Secrets Manager", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🤫", tagline: "Store & rotate secrets securely", analogy: "A locked vault — apps fetch passwords at runtime instead of hardcoding in source code.", keyPoints: ["DB passwords, API keys, tokens", "Auto-rotates on schedule", "Fine-grained IAM access per secret"], when: "Any secret that shouldn't be in code or env vars", pricing: "$0.40/secret/month · $0.05/10,000 API calls." },
      { name: "CloudHSM", full: "AWS CloudHSM", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔐🏦", tagline: "Dedicated hardware security module — you fully manage the keys, AWS cannot access them", analogy: "KMS is a bank vault you share with other tenants where the bank has a master key. CloudHSM is a safety deposit box in a room only you enter — you hold the only key, even the bank staff can't open it.", keyPoints: ["Single-tenant dedicated HSM hardware in your VPC — the physical device is not shared with other AWS customers (unlike KMS which is a multi-tenant managed service)", "FIPS 140-2 Level 3 certified: the highest level of hardware tamper protection — required by some financial, government, and compliance standards", "AWS has zero access to your keys: you initialise and manage the HSM yourself using industry-standard tools (PKCS#11, JCE, OpenSSL); if you lose your credentials, AWS cannot recover your keys", "Clustered for HA: deploy multiple CloudHSM instances in different AZs — keys are synchronised across the cluster automatically", "Common use cases: Oracle Transparent Data Encryption (TDE) key management; SSL/TLS private key storage (SSL offloading on-premises or with load balancers); regulatory requirements explicitly mandating customer-controlled HSM; signing code or documents", "vs KMS: KMS is FIPS 140-2 Level 2, AWS-managed, simpler and cheaper. CloudHSM is Level 3, customer-managed, more control, higher cost and operational overhead", "Can integrate with KMS: use CloudHSM as a custom key store behind KMS — KMS API calls are backed by your HSM hardware while you still use the familiar KMS interface"], when: "Regulatory requirements demanding FIPS 140-2 Level 3 or customer-exclusive HSM control; Oracle TDE; contractual obligation that the cloud provider cannot access encryption keys", pricing: "~$1.60/hr per HSM instance. Exam tip: 'dedicated HSM', 'FIPS 140-2 Level 3', 'AWS cannot access keys' → CloudHSM. 'Customer manages keys with audit trail' (but AWS-managed hardware is fine) → KMS CMK." },
      { name: "ACM", full: "AWS Certificate Manager", color: "#C0392B", lightBg: "#FDEDEC", icon: "📜", tagline: "Free SSL/TLS certificates", analogy: "A free, auto-renewing padlock for your website — HTTPS without managing cert renewals.", keyPoints: ["Free public certs for CloudFront, ALB, API Gateway", "Auto-renews before expiry", "Private CA for internal certs"], when: "HTTPS on any AWS-hosted website or API", pricing: "Public certs: free. Private CA: $400/month + $0.75/cert." },
      { name: "🛡️ Threat Protection", isHeader: true, color: "#E8983C" },
      { name: "Network Firewall", full: "AWS Network Firewall", color: "#E8983C", lightBg: "#FEF5ED", icon: "🔥🛡️", tagline: "Managed stateful network firewall for VPC — deep packet inspection, IDS/IPS, domain filtering", analogy: "A full-featured checkpoint booth inside your VPC's perimeter. Unlike Security Groups (simple allow/deny on ports) and NACLs (stateless subnet rules), Network Firewall inspects the actual content of packets and can block based on hostnames, signatures, and behaviour patterns.", keyPoints: ["Deployed in a dedicated firewall subnet in each AZ; route tables steer all VPC traffic through it before it reaches subnets or the internet gateway", "Stateful inspection: tracks connection state (like Security Groups) AND inspects packet payloads — can detect and block threats mid-stream", "Stateless rules: fast path for high-volume allow/deny rules before stateful inspection (evaluated first)", "Domain list filtering: block or allow outbound traffic to specific domain names (e.g. block *.malware-c2.com) — works on encrypted traffic via SNI inspection for HTTPS", "Suricata-compatible IDS/IPS rules: bring your own Suricata rule files for advanced threat signatures — same format as open-source Suricata/Snort", "AWS Firewall Manager: centrally deploy and manage Network Firewall rules across multiple accounts in an AWS Organization", "vs WAF: WAF operates at Layer 7 HTTP/HTTPS on ALB/CloudFront/API GW. Network Firewall operates at network layer inside the VPC on all traffic types", "vs Security Groups: SGs are per-resource allow/deny on port/IP. Network Firewall is centralised, stateful, payload-aware inspection for all VPC traffic"], when: "VPC traffic requires deep packet inspection, outbound domain filtering, IDS/IPS signatures, or centralised network-level security policy across many subnets", pricing: "~$0.395/hr per firewall endpoint (per AZ) + $0.065/GB data processed. Exam tip: 'deep packet inspection in VPC', 'block specific domains', 'IDS/IPS rules', 'centralised VPC firewall' → AWS Network Firewall (not WAF, not SGs)." },
      { name: "WAF", full:"AWS Web Application Firewall", color: "#7D6608", lightBg: "#FEFCE8", icon: "🧱", tagline: "Block malicious web traffic", analogy: "A bouncer — inspects HTTP requests and blocks SQLi, XSS, bad bots before they reach your app.", keyPoints: ["Blocks OWASP Top 10: SQLi, XSS, bad IPs", "Attaches to CloudFront, ALB, API Gateway", "Managed rule groups available"], when: "Protecting public web apps from common attacks", pricing: "$5/Web ACL/month + $1/rule/month + $0.60/million requests." },
      { name: "Shield", full: "AWS Shield", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🛡️", tagline: "DDoS protection", analogy: "A flood barrier — absorbs volumetric attacks before they knock your app offline.", keyPoints: ["Standard: free, automatic, always-on", "Advanced: $3,000/month, L7 protection, 24/7 DRT", "Protects EC2, ELB, CloudFront, Route 53"], when: "Standard always on. Advanced for high-value targets.", pricing: "Standard: free. Advanced: $3,000/month per organisation." },
      { name: "Firewall Manager", full: "AWS Firewall Manager", color: "#C0392B", lightBg: "#FDEDEC", icon: "🧱🏢", tagline: "Centrally manage WAF, Shield, Security Groups, and Network Firewall rules across all AWS Organization accounts", analogy: "A head of security for a large corporation with dozens of offices. Instead of each office managing its own door locks, CCTV, and alarm codes separately, the head of security sets the rules once from HQ and they are automatically enforced in every office — including any new ones that open next month.", keyPoints: ["Firewall Manager is a central policy management service — define security rules once, automatically enforce them across all accounts and regions in your AWS Organization", "Resources it manages: (1) AWS WAF Web ACLs (ALB, CloudFront, API Gateway, AppSync), (2) VPC Security Groups (attach, audit, remediate), (3) AWS Shield Advanced (enable protection on ALBs, CloudFront, Route 53, EIPs), (4) AWS Network Firewall (VPC-level traffic inspection), (5) Route 53 Resolver DNS Firewall", "Does NOT manage: NACLs, AWS Config rules, or individual IAM policies — these are outside Firewall Manager's scope", "New accounts auto-enrolled: when a new account joins the AWS Organization, Firewall Manager automatically applies existing policies to it — zero manual setup required per account", "Prerequisite: requires AWS Organizations with all features enabled, and AWS Config must be enabled in all member accounts (Firewall Manager uses Config to discover resources)", "Audit Security Groups: identify overly permissive SG rules (e.g. 0.0.0.0/0 on SSH) across all accounts, report non-compliant resources, and optionally auto-remediate", "Common exam scenario: multi-account company needs consistent WAF rules applied everywhere including new accounts → Firewall Manager + AWS Organizations"], when: "Q describes centrally managing WAF/Shield/Security Group rules across multiple AWS accounts in an Organization, especially with automatic enforcement on new accounts → Firewall Manager", pricing: "~$100/policy/region/month. Exam tip: Firewall Manager manages WAF + Shield Advanced + Security Groups + Network Firewall + DNS Firewall. It does NOT manage NACLs. The key phrase is 'centrally' + 'across Organization accounts' + 'automatically applied'." },
      { name: "GuardDuty", full: "Amazon GuardDuty", color: "#E8983C", lightBg: "#FEF5ED", icon: "🕵️", tagline: "Intelligent threat detection across accounts, workloads, and S3", analogy: "A security camera with AI — watches for suspicious behaviour across your entire AWS environment, no agents needed.", keyPoints: ["Analyses: CloudTrail (API calls), VPC Flow Logs (network), DNS logs, and S3 data events", "S3 protection: detects suspicious S3 access patterns — unusual geo access, known malicious IPs, anomalous API calls against buckets", "Detects: crypto mining, credential theft, port scanning, privilege escalation, data exfiltration", "One-click enable — fully managed, no agents or infrastructure to deploy", "Findings sent to Security Hub, EventBridge — can trigger automated Lambda remediation"], when: "Continuous threat monitoring across AWS accounts, workloads, and S3 data", pricing: "~$4/million CloudTrail events + $0.10–$0.80/GB VPC Flow Logs + $0.20/million S3 events. 30-day free trial." },
      { name: "🔍 Compliance & Audit", isHeader: true, color: "#2E86AB" },
      { name: "Inspector", full:"Amazon Inspector", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔍", tagline: "Automated vulnerability scanning", analogy: "A health inspector — scans for known CVEs and network exposure on EC2 and containers.", keyPoints: ["Scans EC2, Lambda, ECR container images", "Continuous, severity-ranked findings", "No agents for EC2 (uses SSM)"], when: "Finding vulnerabilities before attackers do", pricing: "EC2: ~$0.111/instance/month · ECR: ~$0.09/image/month." },
      { name: "Macie", full: "Amazon Macie", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔎", tagline: "Find sensitive data in S3", analogy: "A data scanner that flags files containing credit cards, SSNs, or passwords in your S3 buckets.", keyPoints: ["ML-powered PII/sensitive data discovery", "Scans S3 for PII, credentials, financials", "Integrates with Security Hub & EventBridge"], when: "GDPR/HIPAA audits, finding accidentally exposed PII", pricing: "$1/bucket/month + $1/GB scanned. 30-day trial." },
      { name: "Security Hub", full: "AWS Security Hub", color: "#E8983C", lightBg: "#FEF5ED", icon: "🏛️", tagline: "Centralised security dashboard", analogy: "One pane of glass — aggregates findings from GuardDuty, Inspector, Macie, WAF into one prioritised view.", keyPoints: ["50+ AWS & partner integrations", "Automated CIS, PCI-DSS, NIST compliance checks", "EventBridge integration for remediation"], when: "Managing security posture across many services or accounts", pricing: "$0.0010/finding/month + $0.0008/security check. 30-day trial." },
      { name: "CloudTrail", full: "AWS CloudTrail", color: "#7D6608", lightBg: "#FEFCE8", icon: "📋", tagline: "Audit log of all AWS API calls", analogy: "A CCTV recording of everything in your account — who did what, when, from where.", keyPoints: ["Logs every API call across all services", "90 days free; send to S3 for long-term", "Essential for compliance & forensics"], when: "Audits, security investigations, 'who deleted that?'", pricing: "One free trail/region. Extra trails: $2/100k management events." },
      { name: "Security Lake", full: "Amazon Security Lake", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏞️🔐", tagline: "Centralised security data lake in your own S3 — OCSF-normalised", analogy: "A single reservoir that collects water (security logs) from dozens of different rivers (AWS services, SaaS tools, on-prem) and converts everything into the same standard measurement units (OCSF) so any analysis tool can work with it without custom plumbing.", keyPoints: ["Automatically collects and normalises security logs from AWS sources: CloudTrail, VPC Flow Logs, Route 53 DNS logs, Security Hub findings, Lambda, S3 data events", "Converts all data to Open Cybersecurity Schema Framework (OCSF) — an open standard so third-party SIEM/analytics tools (Splunk, CrowdStrike, etc.) can query without custom parsers", "Data stored in your own S3 bucket in your account — you own and control it; Security Lake manages the collection, transformation, and partitioning", "Supports custom sources: ingest logs from on-prem systems, other clouds, or SaaS tools and normalise them to OCSF too", "Subscriber model: grant query access to analytics tools (Amazon Athena, third-party SIEMs) via an S3 data access subscriber — they query your lake directly", "Retention management: set lifecycle policies per log type within the lake — hot tier for recent events, archive to Glacier for compliance", "Multi-account & multi-region: a delegated administrator account aggregates logs from all member accounts in an AWS Organization into one lake", "Difference from Security Hub: Security Hub aggregates findings and runs compliance checks; Security Lake is a raw log store for deep forensic queries and long-term retention"], when: "Centralising all AWS + third-party security telemetry for SIEM integration, threat hunting, or compliance archiving — especially in multi-account environments", pricing: "Charged on data ingested: ~$0.35/GB for first 50 TB/month. Storage in your S3 at standard S3 rates. No charge for subscriber query access itself." },
    ]
  },
  messaging: {
    tab: "📨 Messaging",
    items: [
      { name: "📬 Queuing & Pub/Sub", isHeader: true, color: "#E8593C" },
      { name: "SQS", full: "Simple Queue Service", color: "#E8593C", lightBg: "#FEF0ED", icon: "💬", tagline: "Message queue — decouple services", analogy: "A to-do list between services — A drops a task, B picks it up when ready.", keyPoints: ["Pull-based (consumers poll)", "Messages retained up to 14 days", "Standard queue: at-least-once delivery, best-effort ordering, nearly unlimited throughput", "FIFO queue: exactly-once processing, strict message ordering, up to 300 msg/s (single) or 3,000 msg/s with batching (10 messages per batch)", "FIFO batch mode: send/receive up to 10 messages per API call — multiply throughput by batch size (300 × 10 = 3,000 msg/s)", "Visibility timeout: message hidden from other consumers while being processed — prevents double processing", "Dead Letter Queue (DLQ): messages that fail processing N times are moved here for inspection", "Migrating Standard → FIFO checklist: (1) verify peak throughput ≤ 3,000 msg/s with batching — FIFO cannot exceed this limit unlike Standard which is nearly unlimited; (2) you CANNOT convert an existing Standard queue to FIFO — delete it and recreate as FIFO; (3) FIFO queue name MUST end in .fifo suffix (e.g. orders-queue.fifo) — hard AWS requirement enforced at creation"], when: "Decoupling microservices, smoothing traffic spikes, background jobs; use FIFO when order matters or exactly-once is required", pricing: "1M requests/month free · $0.40/million (Standard) · $0.50/million (FIFO). Exam tip: FIFO limit is 300/s bare, 3,000/s with batching of 10." },
      { name: "SNS", full: "Simple Notification Service", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📢", tagline: "Pub/sub notifications — fan out", analogy: "A megaphone — one message broadcast to all subscribers simultaneously.", keyPoints: ["Push-based (AWS delivers)", "One message → many receivers", "Subscribers: Lambda, SQS, HTTP, email, SMS"], when: "Fan-out patterns, alerting multiple systems, notifications", pricing: "1M publishes free · $0.50/million after · SMS ~$0.00645/message." },
      { name: "Amazon MQ", full: "Amazon MQ (ActiveMQ / RabbitMQ)", color: "#C0392B", lightBg: "#FDEDEC", icon: "📮", tagline: "Managed message broker for ActiveMQ and RabbitMQ — lift-and-shift without rewriting messaging code", analogy: "Moving an existing post office (your on-prem ActiveMQ broker) to the cloud without changing any of the mail formats or delivery routes. Your apps keep speaking their protocol (AMQP, STOMP, MQTT) — Amazon MQ just hosts the post office.", keyPoints: ["Managed broker service supporting Apache ActiveMQ and RabbitMQ — AWS handles provisioning, patching, maintenance, and backups", "Supported protocols: AMQP, MQTT, OpenWire, STOMP, WebSocket — the same protocols your on-prem broker already speaks; applications connect without code changes", "Key difference from SQS/SNS: SQS/SNS are AWS-proprietary cloud-native services requiring SDK integration. Amazon MQ is protocol-compatible — existing messaging code works as-is", "Single-instance or active/standby HA: active/standby broker pair in two AZs with automatic failover; passive broker takes over in ~30 seconds on failure", "Storage: ActiveMQ uses Amazon EFS for HA broker data; RabbitMQ uses EBS", "Not infinitely scalable: unlike SQS (which scales to any throughput), MQ broker instances have vCPU/memory limits — scale by choosing larger instance type or partitioned queues", "When NOT to use: building new cloud-native apps → use SQS/SNS (simpler, cheaper, infinitely scalable). Migrating existing apps with broker dependencies → use Amazon MQ"], when: "Migrating on-prem ActiveMQ or RabbitMQ workloads to AWS without rewriting messaging code; applications using AMQP, MQTT, STOMP, or OpenWire protocols that can't switch to SQS/SNS", pricing: "mq.m5.large ~$0.30/hr single-instance. Active/standby: 2× broker cost. Exam tip: 'lift-and-shift messaging', 'existing broker', 'AMQP/MQTT/STOMP' → Amazon MQ. 'New cloud-native app' → SQS/SNS." },
      { name: "SES", full: "Amazon Simple Email Service (SES)", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "📧", tagline: "High-volume transactional and marketing email — send and receive email at scale", analogy: "A professional mail room with a great reputation. Your app hands it letters (emails), it handles deliverability, bounce management, and DKIM signing — like having a full-time postmaster without hiring one.", keyPoints: ["Sending: transactional email (password resets, order confirmations, alerts) and bulk marketing campaigns — millions of emails per day", "Deliverability features: DKIM signing, SPF, DMARC support, dedicated IPs for reputation separation, sandbox mode for testing", "Domain verification: verify your sending domain in SES so emails appear from your domain rather than an AWS address; required before production sending", "Receiving: SES can receive inbound email for your domain and route it to: S3 (store the raw email), Lambda (process it), SNS (fan out notification), or another service — build email-driven workflows", "Bounce and complaint handling: SES tracks bounces and complaints; integrate with SNS to automate removal of bad addresses", "vs SNS email subscription: SNS email is simple plaintext notification for AWS alerts. SES is for rich application-generated HTML email at scale — different tools for different purposes", "Regions: SES is available in specific regions — verify your sending identity in the region you'll send from"], when: "Application sending transactional emails (receipts, alerts, password resets); bulk marketing campaigns; inbound email processing (receive→Lambda→process workflow)", pricing: "First 62,000 emails/month free when sent from EC2. $0.10/1,000 emails after. Dedicated IPs: ~$24.95/month each. Exam tip: 'application sends notification emails', 'inbound email to S3/Lambda' → SES. 'Alert multiple subscribers including email' → SNS." },
      { name: "EventBridge", full: "Amazon EventBridge", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🚌", tagline: "Serverless event bus and scheduler", analogy: "A smart router that listens for events from AWS services or SaaS apps and routes them to targets based on pattern-matching rules — like a programmable traffic controller for your architecture.", keyPoints: ["Event bus: route events by pattern matching on JSON content → Lambda, SQS, SNS, Step Functions, etc.", "Scheduler: cron and rate-based schedules (replaces CloudWatch Events/cron jobs)", "Pipes: point-to-point integration with built-in filtering and transformation between source and target", "SaaS integrations: Salesforce, Zendesk, Shopify → your AWS targets with no custom connector code", "Schema registry: auto-discovers event schemas for discoverability and code binding generation"], when: "Decoupled event-driven architectures; replacing cron jobs; reacting to AWS service events; SaaS → AWS workflows", pricing: "$1.00/million custom events. AWS events free. Pipes: $0.40/million events. Scheduler: $1.00/million scheduled invocations." },
      { name: "📡 Streaming (Kinesis)", isHeader: true, color: "#2E86AB" },
      { name: "Kinesis Data Streams", full:"Amazon Kinesis Data Streams", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌊", tagline: "Real-time stream ingestion — multiple consumers, you manage shards", analogy: "A multi-lane highway you build yourself — you decide how many lanes (shards), producers pour data in, and multiple consumers read their own copy of the stream independently. Each driver (consumer) drives the full length of the highway at their own pace and can rewind to any point.", keyPoints: ["Producers write records; consumers (Lambda, EC2, KDA, Firehose) read from shards independently", "Each shard: 1 MB/s in, 2 MB/s out, up to 1,000 records/s write. Scale by adding shards", "Retention: 24 hrs default, up to 365 days — data stays in the stream and can be REPLAYED from any point. This is a key differentiator from Firehose (which has no replay)", "Multiple independent consumers: each consumer tracks its own position (sequence number) in the stream. Lambda, EC2, KDA, and Firehose can ALL read the same stream simultaneously — each gets its own full copy of the data", "On-demand mode: auto-scales shards based on throughput. Provisioned mode: you set shard count manually (useful for cost predictability)", "Ordering: records within a shard are strictly ordered. Use a partition key to route related records to the same shard for ordered processing", "Latency: sub-second (milliseconds) — records are available to consumers almost immediately after being written", "Management overhead: you are responsible for shard count, consumer checkpointing, and scaling — more control but more work than Firehose", "Use KDS when: need multiple consumers on same data, need replay, need sub-second latency, need custom processing logic per consumer"], when: "Sub-second real-time processing, replay capability needed, multiple consumers reading the same stream simultaneously, custom processing logic per consumer, fan-out to multiple destinations", pricing: "$0.015/shard-hr (provisioned) + $0.014/million records. On-demand: $0.08/GB in + $0.034/GB out. Exam tip: 'multiple consumers', 'replay', 'sub-second latency', 'fan-out' → KDS. 'Deliver to one destination, no code' → Firehose." },
      { name: "Kinesis Firehose", full: "Amazon Kinesis Data Firehose", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🚿", tagline: "Fully managed delivery to ONE destination — zero infrastructure, buffered, near-real-time", analogy: "A self-driving pipe that automatically delivers packages to one specific address. You connect the source and pick the destination — it buffers, compresses, optionally transforms, and delivers automatically. No drivers to hire, no routes to plan. But it only goes to one address per pipe.", keyPoints: ["Fully managed — no shards, no consumers to code, no capacity planning. Auto-scales to any throughput", "Single destination per delivery stream: S3, Redshift, OpenSearch, Splunk, or a custom HTTP endpoint — one at a time per stream. Cannot fan out to multiple destinations natively", "Buffered delivery: Firehose batches records before delivery — triggers on either 60 seconds elapsed OR 1 MB of data (whichever comes first). This means ~60 second minimum latency — NOT sub-second real-time", "No replay: once data is delivered to the destination, it is gone from Firehose. There is no sequence number, no consumer position, no way to re-read old records from the stream itself", "Optional Lambda transform: invoke a Lambda function on each batch to enrich, filter, or reformat records before they land in the destination", "Format conversion: automatically converts JSON to Parquet or ORC before writing to S3 — no extra code needed for columnar storage optimisation", "Source options: Firehose can receive data directly from producers (SDK/agent), or read from a Kinesis Data Streams stream as its source", "Use Firehose when: simple pipeline to one destination, no code to write, near-real-time is acceptable (60s lag OK), want managed ETL/format conversion"], when: "Load streaming data into S3/Redshift/OpenSearch with zero infrastructure management; ETL on ingest (Lambda transform or format conversion); single destination pipeline; 60-second delivery lag is acceptable", pricing: "$0.029/GB ingested (first 500 TB/month). Format conversion extra. Exam tip: 'deliver to S3/Redshift/OpenSearch automatically', 'no shard management', 'near-real-time' → Firehose. 'Multiple consumers', 'replay', 'sub-second' → KDS." },
      { name: "KDS vs Firehose: Decision Guide", full: "Kinesis Data Streams vs Firehose — Full Comparison", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌊⚖️🚿", tagline: "KDS = control + multiple consumers + replay + sub-second. Firehose = managed + single destination + buffered.", analogy: "KDS is a river with multiple boats — each boat (consumer) travels the river independently at its own speed, can go back upstream (replay), and multiple boats run simultaneously. Firehose is a single pipe with a tap — you turn it on and water flows automatically to one tank, no boats, no upstream, no choices.", keyPoints: ["Consumers: KDS supports MULTIPLE independent consumers simultaneously (Lambda + EC2 + KDA + Firehose all reading the same stream at once). Firehose delivers to ONE destination per stream — no fan-out", "Latency: KDS = sub-second (milliseconds from write to read). Firehose = near-real-time (~60 seconds buffer before delivery). If sub-second matters → KDS only", "Replay: KDS retains records for 24 hours to 365 days — consumers can rewind and re-read from any sequence number. Firehose = no replay, data delivered and gone from the stream", "Management: KDS requires you to provision/manage shards, handle consumer checkpointing, and scale manually (or use on-demand). Firehose = fully managed, zero shard management, auto-scales", "Custom processing: KDS consumers can apply any custom logic per record. Firehose = optional Lambda transform (simple enrichment/filter) but not arbitrary processing per consumer", "Format conversion: Firehose natively converts JSON → Parquet/ORC before writing to S3. KDS has no built-in format conversion — consumers handle it", "Sources: both accept data from SDK/agent/CLI. Firehose can also use a KDS stream as its source (KDS → Firehose → S3)", "Pattern: KDS upstream + Firehose as one consumer = fan-out to S3 for archiving AND real-time Lambda processing at the same time — best of both"], when: "Sub-second latency OR replay OR multiple consumers OR fan-out → KDS. Single destination OR no code OR 60s lag OK OR format conversion → Firehose. Both together: KDS as source, Firehose as one of the consumers.", pricing: "Exam decision table: 'real-time alerts + archive to S3' → KDS (Lambda consumer) + Firehose as second consumer. 'Just load data to S3/Redshift' → Firehose alone. 'Replay last 7 days of events' → KDS (Firehose cannot do this). 'Multiple teams consuming same stream' → KDS." },
      { name: "Kinesis Data Analytics", full: "Amazon Kinesis Data Analytics", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📊🌊", tagline: "Run SQL or Flink on a live stream in real time", analogy: "A live scoreboard — instead of waiting for the game to end and analysing the recording, you query the stream as events happen and update the board every second.", keyPoints: ["Two modes: SQL (simple, legacy) or Apache Flink (powerful, stateful)", "Reads from Kinesis Data Streams or Firehose as source", "Outputs to KDS, Firehose, Lambda, or S3", "Use cases: real-time dashboards, anomaly detection, windowed aggregations", "Flink mode supports complex event processing and exactly-once semantics"], when: "Real-time aggregations, windowed metrics, anomaly detection on a live stream — without batch delay", pricing: "Flink: $0.11/KPU-hr (Kinesis Processing Unit = 1 vCPU + 4 GB). SQL: $0.11/KPU-hr." },
    ]
  },
  serverless: {
    tab: "⚡ Serverless",
    items: [
      { name: "🗄️ Data & Orchestration", isHeader: true, color: "#9B59B6" },
      { name: "Step Functions", full: "AWS Step Functions", color: "#E8593C", lightBg: "#FEF0ED", icon: "🪜⚡", tagline: "Orchestrate multi-step serverless workflows", analogy: "A flowchart that executes — each box is an AWS service call (Lambda, DynamoDB, SNS…) and Step Functions handles the arrows: retries, error handling, branching, and waiting.", keyPoints: ["State machine defined in Amazon States Language (JSON)", "Standard Workflows: long-running (up to 1 year), exactly-once, auditable history", "Express Workflows: high-volume, short-duration, at-least-once, cheaper", "Built-in error handling, retries, timeouts, and parallel branches", "200+ SDK integrations — call any AWS service directly without Lambda"], when: "Multi-step processes: order fulfilment, ML pipelines, approval workflows, ETL orchestration", pricing: "Standard: $0.025/1k state transitions. Express: $1/million transitions + $0.00001667/GB-sec." },
    ]
  },
  analytics: {
    tab: "📊 Analytics",
    items: [
      { name: "🔄 ETL & Data Integration", isHeader: true, color: "#2E86AB" },
      { name: "Glue", full:"AWS Glue", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔧📊", tagline: "Serverless ETL and data catalog", analogy: "A data plumber — it discovers what's in your data sources, maps the pipes between them, and runs the transformation jobs to move data where it needs to go.", keyPoints: ["Data Catalog: central metadata store — tables, schemas, partitions used by Athena, Redshift, EMR", "ETL Jobs: serverless Spark jobs to transform and load data", "Glue Crawlers: auto-discover schemas from S3, RDS, DynamoDB and register them in the catalog"], when: "Building data pipelines between S3/RDS/Redshift; powering Athena queries with a schema catalog", pricing: "Crawler: $0.44/DPU-hr. ETL jobs: $0.44/DPU-hr. Catalog: first 1M objects free." },
      { name: "Glue Studio", full: "AWS Glue Studio", color: "#C0392B", lightBg: "#FDEDEC", icon: "🎨🔧", tagline: "Visual ETL job builder for AWS Glue", analogy: "A drag-and-drop flowchart builder for your data pipelines — draw the source, transformations, and destination visually, and Glue Studio generates and runs the Spark code behind the scenes.", keyPoints: ["Visual canvas: drag sources, transforms (join, filter, aggregate), and targets onto a graph", "Generates Glue ETL job code automatically — no Spark knowledge needed", "Monitor job runs, view data previews, and debug visually", "Supports the full Glue job ecosystem: same Data Catalog, same DPU billing"], when: "Teams who want Glue ETL power without writing Spark code; building and monitoring pipelines visually", pricing: "No extra charge for Studio itself. Jobs billed at standard Glue rates: $0.44/DPU-hr." },
      { name: "Glue DataBrew", full: "AWS Glue DataBrew", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🧪📊", tagline: "No-code visual data preparation", analogy: "Excel's data cleaning tools, but serverless and at scale — profile your dataset, spot quality issues, and apply 250+ built-in transformations without writing a single line of code.", keyPoints: ["250+ built-in transformations: normalise, deduplicate, parse dates, redact PII, pivot…", "Data profiling: auto-generates statistics on nulls, distributions, outliers for each column", "Recipes: reusable, versioned sequences of transforms you can apply to new datasets", "Outputs cleaned data to S3 in any format; integrates with Glue Data Catalog"], when: "Analysts and data scientists cleaning raw data before feeding it to Athena, Redshift, or ML models", pricing: "Interactive sessions: $1/session-hr. Recipe jobs: $0.48/DPU-hr." },
      { name: "AppFlow", full: "Amazon AppFlow", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔄🏢", tagline: "No-code SaaS data integration", analogy: "A pre-built highway between SaaS apps and AWS — instead of building a custom API connector to pull Salesforce data into S3, AppFlow does it in minutes with point-and-click configuration.", keyPoints: ["50+ connectors: Salesforce, Slack, ServiceNow, Zendesk, Google Analytics, SAP…", "Flows: define source, optional transformations (filter, map, mask), and destination", "Trigger: on-demand, scheduled, or event-driven (e.g. when a Salesforce record changes)", "Data stays in AWS network — no public internet hop for supported connectors"], when: "Pulling SaaS data (CRM, support, marketing) into S3/Redshift for analytics without building custom integrations", pricing: "Flow runs: $0.001/flow run. Data processing: $0.02/GB. Free tier: 1M records/month." },
      { name: "Lake Formation", full: "AWS Lake Formation", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🏞️🔑", tagline: "Build, secure, and manage a data lake on S3 — fine-grained access control for Athena, Redshift, and EMR", analogy: "Lake Formation is the security guard and property manager for your data lake. Glue is the construction crew that moves and organises data. Lake Formation decides who gets into which room and what they can see once inside — even down to specific columns and rows.", keyPoints: ["Sits on top of S3/Glue: Lake Formation manages permissions for data stored in S3 and catalogued in the AWS Glue Data Catalog — it doesn't store data itself", "Fine-grained permissions: grant table-level, column-level, and row-level (data filter) access to specific IAM users, roles, or groups — controls what data they see when querying via Athena, Redshift Spectrum, or EMR", "Cross-account data sharing: grant access to Glue Data Catalog databases and tables to other AWS accounts without copying data — the recipient queries your S3 data directly with scoped permissions", "Data lake administrator: a designated IAM role/user that manages Lake Formation permissions centrally — separates data governance from data engineering", "Tag-based access control (TBAC): assign LF-Tags (key:value) to data assets, then grant permission on the tag rather than individual tables — scales governance as new tables are created", "Governed Tables: a Lake Formation table type supporting ACID transactions and automatic data compaction on S3 — brings database-like consistency to the data lake", "Blueprints: automated data ingestion workflows (database snapshots, incremental loads) that import data into the lake and register it in the catalog"], when: "Centralising access control for S3-based data lakes; enforcing column/row-level security on Athena queries; multi-team or multi-account data governance where different teams see different slices of the same dataset", pricing: "Lake Formation service itself is free. You pay for underlying Glue, S3, and Athena usage. Governed Tables: $0.10/GB for compaction. Exam tip: 'column-level permissions on S3 data', 'data lake governance', 'row-level security for Athena' → Lake Formation." },
      { name: "🏭 Data Warehousing & Query", isHeader: true, color: "#E8983C" },
      { name: "Redshift", full:"Amazon Redshift", color: "#C0392B", lightBg: "#FDEDEC", icon: "🏛️📊", tagline: "Petabyte-scale data warehouse", analogy: "A giant spreadsheet optimised for analysis — not live transactions, but crunching billions of rows with SQL in seconds.", keyPoints: ["Columnar storage — fast for aggregations (SUM, GROUP BY)", "Redshift Spectrum: query S3 directly from Redshift without loading", "Serverless option: pay per query, no cluster management", "Integrates with Tableau, QuickSight, dbt"], when: "BI dashboards, historical reporting, joining large datasets for analytics", pricing: "dc2.large ~$0.25/hr. Reserved 1yr saves ~40%. Serverless ~$0.375/RPU-hr." },
      { name: "Athena", full: "Amazon Athena", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔍📊", tagline: "Query S3 data with SQL — no servers", analogy: "A SQL terminal pointed at your S3 bucket. No database to set up — just write a query and pay per TB scanned.", keyPoints: ["Serverless — no infrastructure to manage", "Standard SQL (Presto/Trino under the hood)", "Pay per query: $5/TB scanned — use partitioning and columnar formats to cut costs", "Works with Glue Data Catalog for schema management"], when: "Ad-hoc analysis of logs, events, or data lake files in S3; cost-effective alternative to loading into Redshift", pricing: "$5/TB data scanned. Use Parquet/ORC + partitioning to reduce scan size by 10–100×." },
      { name: "EMR", full: "Amazon EMR", color: "#9B59B6", lightBg: "#F5EEF8", icon: "⚡📊", tagline: "Managed Hadoop / Spark clusters", analogy: "A rented supercomputer cluster — spin up 100 nodes of Spark or Hadoop, crunch petabytes of data, then tear it all down and stop paying.", keyPoints: ["Supports Spark, Hadoop, Hive, Presto, Flink, HBase", "Runs on EC2 (spot instances for 90% cost savings) or EKS/Serverless", "Persistent cluster or transient (spin up, run job, terminate)", "Integrates with S3 as storage layer (decouple compute from storage)"], when: "Large-scale batch processing, ML feature engineering, log analysis, custom big data frameworks", pricing: "EMR fee on top of EC2: ~$0.048–$0.27/hr per node depending on type. Use Spot for big savings." },
      { name: "🔍 Search & Visualization", isHeader: true, color: "#9B59B6" },
      { name: "OpenSearch", full:"Amazon OpenSearch Service", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔍🗂️", tagline: "Search and log analytics engine", analogy: "A Google-style search engine you host yourself — index anything, search it in milliseconds, and build dashboards over it.", keyPoints: ["Fork of Elasticsearch — full-text search, fuzzy matching, aggregations", "OpenSearch Dashboards (Kibana) included for visualisation", "Common pattern: ingest logs via Kinesis Firehose or CloudWatch → OpenSearch → dashboards", "Multi-AZ with automated snapshots"], when: "Application search, log analytics (ELK-style stack), clickstream analysis, security event monitoring", pricing: "t3.small.search ~$0.036/hr. Storage ~$0.135/GB/month. No free tier." },
      { name: "QuickSight", full: "Amazon QuickSight", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "📈🖥️", tagline: "Serverless BI and dashboarding", analogy: "AWS's version of Tableau or Power BI — connect to your data sources, drag and drop charts, share dashboards with anyone in the company.", keyPoints: ["SPICE: in-memory engine for fast dashboard queries", "Connects to Athena, Redshift, RDS, S3, Salesforce, and more", "ML Insights: anomaly detection and forecasting built-in", "Pay-per-session pricing — no per-seat licence"], when: "Self-service BI for non-technical users; embedding dashboards in apps; sharing reports across the org", pricing: "Authors: $18/month. Readers: $0.30/session (max $5/month per reader)." },
      { name: "QuickSight SPICE", full: "SPICE — Super-fast Parallel In-memory Calculation Engine", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "📈⚡", tagline: "QuickSight's in-memory data store — import data once, query at speed without hitting your database", analogy: "Instead of sending a waiter to the kitchen (your database) every time a guest asks a question, you pre-load a buffet table (SPICE) in the dining room. Every question is answered instantly from the buffet — the kitchen only gets involved when the buffet needs restocking (data refresh).", keyPoints: ["SPICE is QuickSight's proprietary in-memory columnar data engine. When you import a dataset into SPICE, QuickSight stores it internally — dashboard queries run against SPICE, not your source database", "Columnar storage: SPICE stores data column-by-column (like Parquet). When a query aggregates 3 columns from a 100-column dataset, only those 3 columns are read from memory — much faster than row-based storage which reads entire rows", "Columnar filtering: filter and aggregate operations on SPICE are extremely fast because the engine scans only the relevant columns in memory rather than fetching from disk or network", "Performance benefit: dashboards load faster because SPICE queries are in-memory vs network round-trips to Redshift, Athena, RDS, or S3. Eliminates repeated query load on source systems", "Data freshness: SPICE is a snapshot — data must be refreshed to stay current. Schedule automatic refreshes (hourly, daily) or trigger manual refreshes. NOT real-time by default", "Direct Query mode (alternative): QuickSight can query your data source live on every dashboard load instead of using SPICE. Slower but always current. Use for real-time requirements; use SPICE for performance", "Capacity: each QuickSight author gets 10 GB of SPICE free. Additional SPICE capacity purchased separately (~$0.25/GB/month)"], when: "Q asks why QuickSight dashboards are slow → import data into SPICE. Q asks how to reduce query load on Redshift/RDS from BI tools → use SPICE. Q asks about 'columnar filtering' or 'in-memory BI engine' → SPICE. Real-time data requirement → Direct Query mode instead.", pricing: "Exam tip: SPICE = fast + not real-time (needs refresh). Direct Query = real-time + slower. 'Columnar' means it reads only the columns needed by a query — the source of its speed advantage. 10 GB free per author; paid beyond that." },
    ]
  },
  ai: {
    tab: "🤖 AI / ML",
    items: [
      { name: "🤖 Machine Learning Platform", isHeader: true, color: "#9B59B6" },
      { name: "SageMaker", full:"Amazon SageMaker", color: "#E8593C", lightBg: "#FEF0ED", icon: "🧠⚙️", tagline: "End-to-end ML platform", analogy: "A fully equipped ML lab — data prep benches, training rigs, and a deployment line all in one building. You bring the idea, AWS provides every tool.", keyPoints: ["Build, train, and deploy ML models at scale", "Managed Jupyter notebooks (Studio) for experimentation", "Built-in algorithms + bring your own; auto-scales training jobs", "SageMaker Endpoint for real-time inference; Batch Transform for bulk scoring"], when: "Any ML workload: training custom models, running inference endpoints, AutoML with Autopilot", pricing: "ml.t3.medium notebook ~$0.05/hr. Training: ml.m5.xlarge ~$0.23/hr. Inference endpoint varies by instance." },
      { name: "🧠 AI Services (Pre-built)", isHeader: true, color: "#2E86AB" },
      { name: "Comprehend", full:"Amazon Comprehend", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📖🤖", tagline: "NLP — understand text at scale", analogy: "A speed-reader with a psychology degree — it scans text and tells you the sentiment, who/what is mentioned, what language it's in, and what topics it covers.", keyPoints: ["Sentiment analysis: positive / negative / neutral / mixed", "Entity recognition: people, places, dates, organisations", "Key phrase extraction, language detection, topic modelling", "Custom classifiers and entity recognisers you can train on your data"], when: "Analysing customer reviews, support tickets, social media; document classification; compliance scanning", pricing: "NLP units (100 chars = 1 unit). Sentiment: $0.0001/unit. Custom model training: $3/hr." },
      { name: "Transcribe", full: "Amazon Transcribe", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🎙️📝", tagline: "Speech to text — audio/video → transcript", analogy: "A tireless court reporter — feed it an audio file or a live stream and it types out everything said, with speaker labels and timestamps.", keyPoints: ["Batch transcription (upload file) or streaming (real-time)", "Speaker diarisation: labels who said what", "Custom vocabulary for domain-specific jargon", "Redaction: auto-removes PII like card numbers from transcripts"], when: "Call centre analytics, meeting transcription, video captioning, voice-driven search", pricing: "Standard: $0.024/minute. Streaming: $0.018/minute. First 60 min/month free for 12 months." },
      { name: "Rekognition", full: "Amazon Rekognition", color: "#C0392B", lightBg: "#FDEDEC", icon: "👁️🤖", tagline: "Computer vision — detect objects, faces, text, and unsafe content in images and video", analogy: "A tireless pair of AI eyes that can scan millions of images per second, telling you 'this photo has two people, one appears to be 30–40 years old, wearing glasses, and there is a dog in the background' — instantly and at any scale.", keyPoints: ["Object and scene detection: identify thousands of object categories, scenes, and activities in images (car, beach, wedding, sports) — returns labels with confidence scores", "Facial analysis: detect faces, estimate age range, detect emotions (happy, sad, surprised), head pose, facial landmarks, gender — does NOT identify who the person is unless using a custom collection", "Face search (recognition): compare a face against a pre-built collection of known faces (e.g. employee database) — returns matches with similarity scores; used for identity verification, access control", "Celebrity recognition: identify well-known public figures in images and video", "Content moderation: detect explicit, violent, or otherwise unsafe content in images and video — returns a confidence score and moderation label (nudity, violence, drugs, etc.); used to review UGC before publishing", "Text in images (OCR): extract printed or handwritten text from images — licence plates, street signs, product labels", "Video analysis: process stored video files (S3) or live streams (Kinesis Video Streams) — detect activities, people, scenes, and unsafe content over time", "Custom Labels: train a custom model on your own images to detect domain-specific objects (e.g. defects on a production line, specific product SKUs)"], when: "Content moderation for user-generated content; identity verification in mobile apps; media asset tagging at scale; security camera facial search; document text extraction", pricing: "Images: ~$0.001/image (first 1M/month). Video: ~$0.10/minute. Custom Labels: $1/hr training + $4/hr inference. Exam tip: 'detect unsafe content in images', 'facial analysis', 'video content moderation', 'object detection' → Amazon Rekognition." },
      { name: "Lookout", full: "Amazon Lookout for Metrics", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔭📉", tagline: "Automated anomaly detection for metrics", analogy: "A tireless analyst watching all your dashboards — it learns what 'normal' looks like for each metric and pages you the moment something unusual happens, with a root-cause explanation.", keyPoints: ["Ingests metrics from S3, Redshift, RDS, CloudWatch, or SaaS (Salesforce, Marketo…)", "No ML expertise needed — fully automated model training and updating", "Groups related anomalies together and ranks by severity", "Variants: Lookout for Vision (image defects), Lookout for Equipment (sensor data)"], when: "Business metric monitoring (revenue drops, ad CTR spikes), IoT sensor anomaly detection, image quality inspection", pricing: "Up to 5 metrics free/month. Additional: $0.30/metric/month up to 5k, lower tiers after." },
    ]
  },
  monitoring: {
    tab: "📡 Monitoring",
    items: [
      { name: "📊 Metrics & Alarms", isHeader: true, color: "#E8593C" },
      { name: "CloudWatch", full:"Amazon CloudWatch", color: "#E8593C", lightBg: "#FEF0ED", icon: "📊", tagline: "Metrics, logs & alarms for AWS", analogy: "Your infrastructure dashboard — metrics, logs, and alert system in one place.", keyPoints: ["Collects metrics from all AWS services", "Custom metrics from your own apps", "Alarms trigger SNS, Lambda, Auto Scaling"], when: "Monitoring resource health, threshold alerts, dashboards", pricing: "Basic (5-min) free. Detailed (1-min): ~$0.01/metric/month. Custom: $0.30/metric/month." },
      { name: "CloudWatch Logs", full: "CloudWatch Logs", color: "#C0392B", lightBg: "#FDEDEC", icon: "📝", tagline: "Centralised log storage & search", analogy: "A searchable filing cabinet for all your logs — no more SSHing into servers.", keyPoints: ["Ingests from EC2, Lambda, ECS, RDS, etc.", "Log Insights: SQL-like queries", "Metric filters: log patterns → CloudWatch alarms"], when: "Centralising logs, debugging errors, compliance retention", pricing: "$0.50/GB ingested · $0.03/GB stored/month." },
      { name: "CloudWatch Agent", full: "CloudWatch Agent", color: "#E8983C", lightBg: "#FEF5ED", icon: "🦾📊", tagline: "Collect OS-level metrics from EC2", analogy: "A reporter on your EC2 — sends memory, disk, and process metrics CloudWatch can't see by default.", keyPoints: ["Memory, disk, swap — not in default EC2 metrics", "Collects OS log files too", "Deploy via SSM or manually"], when: "Need memory/disk metrics from EC2 instances", pricing: "Agent free. Custom metrics sent: $0.30/metric/month." },
      { name: "🔍 Distributed Tracing", isHeader: true, color: "#9B59B6" },
      { name: "X-Ray", full:"AWS X-Ray", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔬", tagline: "Distributed tracing for apps", analogy: "An X-ray machine for microservices — shows exactly where a request went and where it broke.", keyPoints: ["Traces across Lambda, EC2, API Gateway, SQS", "Visual service dependency map", "Identifies latency and error sources"], when: "Debugging latency and errors in distributed systems", pricing: "100k traces/month free · $5/million recorded." },
    ]
  },
  devops: {
    tab: "🏗️ IaC & DevOps",
    items: [
      { name: "🏗️ Infrastructure as Code", isHeader: true, color: "#E8593C" },
      { name: "CloudFormation", full:"AWS CloudFormation", color: "#E8593C", lightBg: "#FEF0ED", icon: "📐🏗️", tagline: "Infrastructure as Code — deploy AWS resources from templates", analogy: "An architect's blueprint — write your infrastructure once as a YAML/JSON template and CloudFormation builds it exactly the same way every time, in any account or region.", keyPoints: ["Templates declare resources: EC2, RDS, VPC, Lambda, etc.", "Stacks: a deployed instance of a template — update or delete as a unit", "Change Sets: preview what will change before applying", "Drift detection: spot manual changes made outside CloudFormation", "StackSets: deploy across multiple accounts and regions at once"], when: "Any repeatable infrastructure: environments (dev/staging/prod), disaster recovery, account vending", pricing: "Free. You pay only for the resources it creates." },
      { name: "CDK", full: "AWS Cloud Development Kit", color: "#9B59B6", lightBg: "#F5EEF8", icon: "👨‍💻🏗️", tagline: "Define infrastructure with real code", analogy: "CloudFormation but in Python, TypeScript, or Java — write a class, call methods, and CDK synthesises the CloudFormation template for you.", keyPoints: ["Write infrastructure in TypeScript, Python, Java, Go, C#", "Synthesises to CloudFormation under the hood", "Constructs: reusable, shareable infrastructure components (like npm packages)", "L1 (raw CFN), L2 (opinionated defaults), L3 (patterns like a full API stack)"], when: "Teams who prefer code over YAML; building reusable infra libraries; complex conditional logic", pricing: "Free. Uses CloudFormation to deploy." },
      { name: "⚙️ Configuration & Operations", isHeader: true, color: "#7D6608" },
      { name: "Systems Manager", full:"AWS Systems Manager (SSM)", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🖥️🔧", tagline: "Manage and operate EC2 at scale", analogy: "A remote control panel for your entire fleet — run commands, push patches, open shells, and store configs without ever opening a firewall port or using SSH keys.", keyPoints: ["Session Manager: browser/CLI shell to EC2 with no SSH or bastion needed", "Run Command: execute scripts across hundreds of instances at once", "Patch Manager: automate OS patching on a schedule", "SSM Agent pre-installed on most AWS AMIs", "Default Host Management Configuration (DHMC) via Quick Setup: one-click setup that automatically attaches the required IAM instance profile (AmazonSSMManagedInstanceCore) to all EC2 instances in the account/region — no manual IAM role modification needed. Also enables Patch Manager, Session Manager, and inventory collection automatically", "DHMC exam scenario: EC2 fleet with existing custom IAM roles needs centralized patching with least admin overhead → enable DHMC. It handles SSM permissions separately without touching existing roles"], when: "Running commands on EC2 fleets, replacing bastion hosts, automated patching, compliance", pricing: "Session Manager and Run Command: free. Advanced features ~$0.00695/instance/month." },
      { name: "Parameter Store", full: "SSM Parameter Store", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🗝️📋", tagline: "Store config and secrets — free tier", analogy: "A key-value config store built into AWS — apps fetch their database URLs, feature flags, and non-sensitive secrets at runtime without hardcoding anything.", keyPoints: ["Standard tier: free — great for config values and non-sensitive strings", "SecureString: encrypted with KMS for sensitive values", "Versioned: roll back to previous values", "vs Secrets Manager: Parameter Store is cheaper but no auto-rotation"], when: "Storing app config, environment variables, non-rotating secrets; budget-conscious alternative to Secrets Manager", pricing: "Standard params: free. Advanced params: $0.05/param/month. GetParameter API: $0.05/10k calls." },
      { name: "🚀 CI/CD", isHeader: true, color: "#1A7A4A" },
      { name: "CodeBuild", full: "AWS CodeBuild", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔨🏗️", tagline: "Fully managed CI build service — compile, test, and package code without managing build servers", analogy: "A on-demand build robot. Instead of keeping a Jenkins server running 24/7, CodeBuild spins up a fresh environment for each build, runs your build script, then disappears — you pay only for the minutes it took.", keyPoints: ["Managed service: no build servers to provision, patch, or maintain — AWS handles the underlying compute", "buildspec.yml: a YAML file in your repo's root that defines the build phases: install (dependencies), pre_build (setup), build (compile/test), post_build (package/push artifacts)", "Build environments: pre-built Docker images for common runtimes (Java, Node, Python, Go, .NET, etc.) or bring your own Docker image for custom build environments", "Artifacts: build output is uploaded to S3; integrates directly with CodePipeline as the 'Build' stage between source checkout and deployment", "Parallel builds: multiple builds can run simultaneously — no queue on a shared Jenkins master", "Cache: cache dependencies (npm, Maven, pip) in S3 to speed up repeat builds significantly", "Environment variables and Secrets Manager: inject build-time secrets and config without hardcoding in buildspec.yml; pull from Parameter Store or Secrets Manager", "CI/CD trilogy: CodePipeline (orchestrates) → CodeBuild (builds/tests) → CodeDeploy (deploys). Most exam questions about CI/CD on AWS involve this trio"], when: "Building, testing, and packaging code as part of a CI/CD pipeline; the managed alternative to self-hosted Jenkins/CircleCI build servers", pricing: "~$0.005/build minute for general1.small. No charge when idle. Exam tip: the full AWS CI/CD pipeline is CodeCommit/GitHub → CodePipeline → CodeBuild → CodeDeploy. 'Managed build server', 'compile and test code' → CodeBuild." },
      { name: "CodePipeline", full:"AWS CodePipeline", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔄🏗️", tagline: "Fully managed CI/CD pipeline", analogy: "An assembly line for your code — every commit automatically flows through source → build → test → deploy stages without manual intervention.", keyPoints: ["Orchestrates source (CodeCommit/GitHub), build (CodeBuild), deploy (CodeDeploy/ECS/Lambda)", "Visual pipeline editor in the console", "Parallel execution and manual approval gates", "Integrates with third-party tools: Jenkins, GitHub Actions"], when: "Automating deployments to EC2, ECS, Lambda, or Elastic Beanstalk", pricing: "$1/active pipeline/month. First pipeline free. Action executions beyond 1/month: $0.005 each." },
      { name: "CodeDeploy", full: "AWS CodeDeploy", color: "#7D6608", lightBg: "#FEFCE8", icon: "🚀📦", tagline: "Automated application deployments", analogy: "A careful moving crew — deploys new code to your fleet with configurable rollout strategies (all-at-once, half-at-a-time, one-at-a-time) and auto-rolls back if health checks fail.", keyPoints: ["Supports EC2, Lambda, and ECS targets", "Deployment strategies: In-place, Blue/Green", "AppSpec file defines lifecycle hooks (before install, after install, validate)", "Integrates with Auto Scaling groups for fleet deployments"], when: "Zero-downtime deployments to EC2 fleets or Lambda; blue/green ECS deployments", pricing: "EC2/on-prem: free. Lambda/ECS deployments: $0.02 per deployment." },
    ]
  },
  migration: {
    tab: "🔁 Migration",
    items: [
      { name: "🔄 Data & Database Migration", isHeader: true, color: "#2E86AB" },
      { name: "Migration Hub", full:"AWS Migration Hub", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🗺️🔄", tagline: "Central dashboard to track migrations", analogy: "The project management board for your cloud migration — all your servers, databases, and applications in one place showing what's been assessed, what's in flight, and what's done.", keyPoints: ["Aggregates progress from DMS, Server Migration Service, CloudEndure", "Discovery: integrates with Application Discovery Service to map dependencies", "Tracks migration status: not started, in progress, completed", "No migration work happens here — it's purely tracking"], when: "Large-scale migrations with many servers and databases to track across teams", pricing: "Free. Pay for the underlying migration tools used." },
      { name: "DataSync", full: "AWS DataSync", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔄💾", tagline: "Fast online data transfer to AWS", analogy: "A high-speed courier service over the internet — automatically handles scheduling, monitoring, encryption, and retry of file transfers between on-prem and AWS storage.", keyPoints: ["Transfers between on-prem NFS/SMB/HDFS and S3, EFS, FSx", "Up to 10× faster than open-source tools via parallel transfers", "Scheduled or one-time transfers; incremental on repeat runs", "Also moves data between AWS storage services"], when: "Online migration of file shares or NAS to S3/EFS; ongoing sync between on-prem and AWS", pricing: "$0.0125/GB transferred." },
      { name: "DMS", full: "AWS Database Migration Service", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🗃️🔄", tagline: "Migrate databases with minimal downtime", analogy: "A live translator between databases — copies your data while the source keeps running, then switches over with seconds of downtime.", keyPoints: ["Homogeneous (MySQL → RDS MySQL) or heterogeneous (Oracle → Aurora) migrations", "Continuous replication: CDC (Change Data Capture) keeps target in sync during migration", "Schema Conversion Tool (SCT): auto-converts stored procedures, views from one engine to another", "Source can stay running the whole time", "DMS is not just for databases — it also supports S3 as a source and Kinesis Data Streams as a target. Use DMS to stream existing S3 files (full load) + ongoing new/updated files (CDC) directly into Kinesis — no custom code needed", "Exam trick: 'stream S3 data to Kinesis fastest/least effort' → DMS as a bridge. Sounds wrong because DMS = migrations, but S3 → Kinesis is a valid DMS source/target combination"], when: "Migrating on-prem or cloud databases to RDS/Aurora; replicating for DR or analytics; streaming S3 files to Kinesis Data Streams", pricing: "t3.micro ~$0.018/hr for replication instance + storage ~$0.10/GB/month." },
      { name: "SCT", full: "AWS Schema Conversion Tool", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔄🗂️", tagline: "Convert database schema from one engine to another", analogy: "A translator for your database blueprints — converts Oracle or SQL Server DDL (tables, views, stored procs, triggers) into PostgreSQL or Aurora syntax before you move the data.", keyPoints: ["Free desktop application — not a managed AWS service", "Used for heterogeneous migrations: different source and target engines (e.g. Oracle → Aurora PostgreSQL, SQL Server → MySQL)", "Automatically converts schema objects: tables, indexes, views, stored procedures, functions, triggers", "Flags anything it can't auto-convert so you can fix manually — shows a conversion assessment report with complexity rating", "NOT needed for homogeneous migrations (same engine, e.g. MySQL → RDS MySQL) — schema is already compatible", "SCT + DMS together: SCT converts the schema (structure); DMS migrates the data (rows) with CDC for live replication. Apply SCT output to target first, then run DMS", "Exam rule: 'Oracle/SQL Server → Aurora/PostgreSQL' = SCT (schema) + DMS (data). Same engine = DMS only"], when: "Heterogeneous database migrations where source and target engines differ", pricing: "Free tool. No AWS charges for SCT itself." },
      { name: "🖥️ Server & Application Migration", isHeader: true, color: "#9B59B6" },
      { name: "MGN", full: "AWS Application Migration Service (MGN)", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🖥️🔄", tagline: "Lift-and-shift VM migration — replicate servers to AWS with no refactoring", analogy: "A live photocopier for your servers. MGN continuously copies every block of your on-prem VM to AWS in the background while the source keeps running. When you're ready, you flip the switch — the AWS copy becomes production in minutes.", keyPoints: ["Agent-based continuous block-level replication: install the AWS Replication Agent on source VMs → data is continuously replicated to a staging area in AWS (using a lightweight replication server)", "Lift-and-shift: no OS or application changes required — the VM is replicated exactly as-is, including OS, drivers, and installed software", "Three-phase process: (1) Install agent + start replication, (2) Launch test instances to validate, (3) Launch cutover instance when ready — source VM can keep running throughout phases 1 and 2", "Test before cutover: launch test EC2 instances from the replicated data at any point — isolated, non-disruptive, source VM unaffected. Validate networking, application health, and performance before committing", "Cutover: once testing passes and replication is confirmed up-to-date, launch the cutover instance — the final production EC2 that replaces the on-prem VM. Then decommission the source.", "Supports any OS (Windows, Linux) and any application — no compatibility requirements beyond installing the agent", "Replaces legacy tools: AWS MGN is the successor to CloudEndure Migration and AWS Server Migration Service (SMS) — both deprecated. MGN is the current recommended tool", "vs DMS: DMS migrates databases only. MGN migrates entire VMs (OS + apps + data). Use both together for full-stack migrations"], when: "Lift-and-shift VM migration from on-prem or other clouds to AWS with no refactoring; tight deadlines; any OS/application; need to test before cutover", pricing: "Free for the first 90 days per server. After 90 days: ~$0.042/hr per replicated server. Exam tip: 'lift-and-shift', 'no refactoring', 'migrate VMs', 'tight deadlines' → AWS MGN (not DMS, not Snowball, not SMS which is deprecated)." },
      { name: "❄️ Physical Data Transfer", isHeader: true, color: "#5D6D7E" },
      { name: "Snowball Edge", full:"AWS Snowball Edge", color: "#2E86AB", lightBg: "#EBF5FB", icon: "📦❄️", tagline: "Physical device to move petabytes offline", analogy: "A ruggedised hard drive the size of a suitcase — AWS ships it to you, you load your data, ship it back, and AWS uploads it. No internet required.", keyPoints: ["80TB usable storage per device (Storage Optimised)", "Snowball Edge Compute: runs EC2 and Lambda at the edge too", "Order multiple devices for parallel transfers", "Encrypted at rest and in transit"], when: "Migrating large datasets (>10TB) where network transfer would take weeks; remote/edge locations with no internet", pricing: "~$300 per job (10-day use). Extra days ~$30/day. Data transfer into AWS free." },
      { name: "AWS Transfer Family", full: "AWS Transfer Family (SFTP/FTPS/FTP/AS2)", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔄📁", tagline: "Fully managed SFTP/FTP/FTPS endpoint that stores files directly in S3 or EFS", analogy: "A translator booth at the UN — legacy vendors speak SFTP (an old language), AWS speaks S3 (the modern language). Transfer Family sits in the middle and translates in real time, so both sides work without changing anything.", keyPoints: ["Provides fully managed SFTP (SSH), FTPS (FTP over TLS), FTP, and AS2 endpoints — no infrastructure to provision or manage", "Files uploaded via SFTP are stored directly in an S3 bucket or EFS file system — no conversion or intermediate storage needed", "Identity providers: AWS managed (built-in user store), Active Directory (via AWS Directory Service), LDAP, Cognito (for federated identities), or a custom identity provider via API Gateway + Lambda", "IAM role per user: each Transfer Family user is mapped to an IAM role that scopes their access to a specific S3 bucket or prefix — enforces isolation between vendors/users", "Home directory: each user is assigned a logical home directory (mapped to an S3 prefix) — they see only their folder, not the whole bucket", "VPC endpoint option: deploy the Transfer Family endpoint inside a VPC with a Security Group for private/internal access or internet-facing with Elastic IP", "AS2 support: EDI (Electronic Data Interchange) protocol used in supply chain/logistics — Transfer Family supports AS2 for B2B file exchange"], when: "Legacy vendors/partners requiring SFTP access to upload files directly to S3 without changing their workflows — fully managed, no EC2 SFTP server to run", pricing: "~$0.30/hr per enabled protocol endpoint + $0.04/GB uploaded + $0.04/GB downloaded. Exam tip: if the question mentions SFTP + S3 + fully managed + no infrastructure → AWS Transfer Family." },
      { name: "Snowmobile", full:"AWS Snowmobile", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🚛❄️", tagline: "100 petabyte data transfer in a truck", analogy: "A literal shipping container of storage on a semi-truck — AWS drives it to your data centre, you fill 100PB of data, they drive it back.", keyPoints: ["100PB per Snowmobile", "GPS-tracked, 24/7 security escort, tamper-resistant", "Use when Snowball would require 10+ devices", "Typically for entire data centre migrations"], when: "Exabyte-scale migrations where even dozens of Snowball devices would be impractical", pricing: "Custom pricing. Contact AWS." },
    ]
  },
  pricing: {
    tab: "💰 Pricing",
    items: [
      { name: "💸 EC2 Purchasing Options", isHeader: true, color: "#E8593C" },
      { name: "On-Demand", full:"On-Demand Pricing", color: "#E8593C", lightBg: "#FEF0ED", icon: "💳", tagline: "Pay as you go, no commitment", analogy: "Renting a car by the hour — maximum flexibility, highest price per unit.", keyPoints: ["No upfront cost, no commitment", "Pay per second/hour", "Best for unpredictable or short-term workloads"], when: "Starting out, unpredictable traffic, anything you can't commit to", pricing: "Full price. Baseline to compare all models against." },
      { name: "Savings Plans", full: "AWS Savings Plans — Three Types", color: "#2E86AB", lightBg: "#EBF5FB", icon: "💰", tagline: "Flexible $/hr commitment — three plan types covering compute, EC2, and SageMaker", analogy: "A monthly phone plan — commit to $X/hr of spend, get a discount across whatever you use within that plan's scope. Pick the plan that matches how broadly or narrowly you want to commit.", keyPoints: ["Compute Savings Plans: most flexible — covers EC2 (any family, size, region, OS, tenancy), Lambda, and Fargate. Discount automatically applies as you shift between any of these services. Up to 66% off", "EC2 Instance Savings Plans: locked to a specific EC2 instance family + region (e.g. m5 in us-east-1), but any size or OS within that family. Bigger discount than Compute Plans (up to 72%) in exchange for less flexibility", "SageMaker Savings Plans: covers SageMaker ML compute — training jobs, inference endpoints, Studio notebooks. Completely separate from the compute plans — applies only to SageMaker usage. Up to 64% off", "All plans: 1-year or 3-year term. Payment options: All Upfront, Partial Upfront, No Upfront (All Upfront = biggest discount)", "Savings Plans vs Reserved Instances: RIs are service-specific (EC2 RI, RDS RI, etc.) and less flexible across services. Savings Plans (Compute) are the most flexible discount mechanism across EC2 + Lambda + Fargate", "Savings Plans do NOT cover RDS, Redshift, ElastiCache — those still use Reserved Instances for commitment discounts"], when: "Q mentions flexible commitment discount across EC2/Lambda/Fargate → Compute Savings Plans. Locked to one instance family for max discount → EC2 Instance Savings Plans. SageMaker workload cost reduction → SageMaker Savings Plans.", pricing: "Compute SP: up to 66% off. EC2 Instance SP: up to 72% off. SageMaker SP: up to 64% off. Exam tip: Savings Plans do NOT replace RIs for RDS/Redshift/ElastiCache — only compute-layer services." },
      { name: "Dedicated Instances vs Hosts", full: "EC2 Dedicated Instances vs Dedicated Hosts", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🏠🖥️", tagline: "Both run on physical hardware for you alone — but Hosts give you visibility and control over the server itself", analogy: "Dedicated Instance = renting a private room in a hotel (no neighbours, but the hotel manages everything). Dedicated Host = leasing the entire floor of the hotel and getting the floor plan — you know every room number and can decide which guests go where.", keyPoints: ["Dedicated Instance: your EC2 runs on physical hardware that is not shared with other AWS accounts — but you don't see or control which specific server it's on; AWS may move it to different hardware at stop/start", "Dedicated Host: you get a specific, named physical server allocated to you — you see the socket/core count, can control instance placement on that host, and the host stays yours (persistent)", "Why Dedicated Host? Per-socket or per-core software licensing: Windows Server, SQL Server, RHEL, SUSE — BYOL (Bring Your Own License) requires a Dedicated Host because licenses are tied to physical cores/sockets", "Why Dedicated Instance? Compliance or regulatory requirements that prohibit shared hardware (but don't need BYOL licensing); simpler to use than Hosts", "Cost: Dedicated Host is generally more expensive (you pay for the whole host); Dedicated Instance has a per-instance charge + per-region fee (~$2/region/hr)", "Dedicated Host can be shared across accounts in an AWS Organization via RAM (Resource Access Manager)", "On-Demand or Reserved pricing available for both"], when: "BYOL software licensing (SQL Server, Oracle, Windows) → Dedicated Host. Hardware isolation for compliance without BYOL → Dedicated Instance.", pricing: "Exam tip: the BYOL question always points to Dedicated Host (not Dedicated Instance) because BYOL requires knowing the physical core/socket count — only Dedicated Hosts provide this." },
      { name: "VPC & Instance Tenancy", full: "EC2 Tenancy: VPC-level vs Instance-level", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🏠⚙️", tagline: "VPC tenancy is a floor — it can only force dedicated, never force shared", analogy: "VPC tenancy is a building policy. A 'dedicated' building means everyone inside gets a private room — even if a tenant asks for shared, the building rules win. A 'default' building has no opinion — tenants decide for themselves.", keyPoints: [
        "Two places to set tenancy: (1) VPC level — default or dedicated; (2) Instance level (Launch Template) — default (shared), dedicated, or host",
        "VPC = default: no override — the instance-level setting is used as-is. Instances can be shared or dedicated depending on what the LT says",
        "VPC = dedicated: overrides everything — ALL instances launched into this VPC become dedicated, regardless of what the Launch Template specifies",
        "The asymmetry: a Launch Template can upgrade an instance to dedicated inside a default VPC (instance setting wins), but it cannot downgrade an instance to shared inside a dedicated VPC (VPC always wins)",
        "Interaction table: VPC default + LT default → shared. VPC default + LT dedicated → dedicated. VPC dedicated + LT default → dedicated (VPC overrides). VPC dedicated + LT dedicated → dedicated",
        "Exam scenario: LT1 (instance=dedicated) + VPC1 (vpc=default) → dedicated. LT2 (instance=default) + VPC2 (vpc=dedicated) → dedicated. Both dedicated — for different reasons",
        "Changing VPC tenancy: you can change a VPC from dedicated back to default, but existing running instances are not affected — only new launches pick up the change"
      ], when: "Any question pairing a Launch Template's tenancy setting with a VPC's tenancy setting — always check the VPC setting first; if VPC=dedicated, the instance setting is irrelevant", pricing: "Exam tip: VPC tenancy = dedicated is a hard override. No instance-level setting can make an instance shared inside a dedicated VPC." },
      { name: "Reserved", full:"Reserved Instances", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📅", tagline: "1–3 year commitment, up to 72% off", analogy: "Signing a 1–3 year lease — cheaper per month but you're locked in.", keyPoints: ["1yr or 3yr term (3yr = bigger discount)", "All Upfront > Partial > No Upfront", "Applies to EC2, RDS, ElastiCache, Redshift"], when: "Steady-state production workloads running 1+ years", pricing: "Up to 72% off (3yr All Upfront). Convertible RIs: up to 54%, more flexible." },
      { name: "Spot Instances", full: "EC2 Spot Instances", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🎲", tagline: "Spare EC2 capacity — up to 90% off", analogy: "Standby airline seats — incredibly cheap but you can be bumped with 2 minutes notice.", keyPoints: ["Up to 90% below On-Demand", "2-min interruption warning", "Best for fault-tolerant, stateless workloads"], when: "Batch jobs, big data, ML training — anything interruptible", pricing: "Fluctuates with demand. Typically 70–90% below On-Demand." },
      { name: "Free Tier", full: "AWS Free Tier", color: "#7D6608", lightBg: "#FEFCE8", icon: "🎁", tagline: "Free usage to learn and experiment", analogy: "A free sample — meaningful amounts of each service to try before you buy.", keyPoints: ["Always Free: 1M Lambda req/mo, 25GB DynamoDB — forever", "12 Months: 750hrs EC2 t2.micro, 5GB S3, 750hrs RDS", "Trial: 30-day trials for newer services"], when: "Learning, prototyping, small personal projects", pricing: "Free within limits. Overage billed at standard rates — watch carefully." },
      { name: "📊 Cost Management Tools", isHeader: true, color: "#1A7A4A" },
      { name: "Data Transfer", full:"Data Transfer Pricing", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔄", tagline: "Moving data costs money — know the rules", analogy: "Storing water is cheap, but piping it outside your house costs money — especially internationally.", keyPoints: ["FREE: data IN from internet; within same AZ via private IPs", "CHARGED: out to internet (~$0.09/GB); cross-AZ (~$0.01/GB); cross-region (~$0.02/GB)", "CloudFront reduces egress significantly vs direct EC2/S3"], when: "Architecture decisions — use same AZ, VPC Endpoints, CloudFront to minimise egress", pricing: "Internet out: $0.09/GB. Cross-AZ: $0.01/GB each way. Cross-region: ~$0.02/GB." },
      { name: "AWS Budgets", full: "AWS Budgets", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "💸", tagline: "Set spending alerts and guardrails", analogy: "A smoke alarm for your bill — alerts before the surprise arrives, and can automatically stop resources if budget is blown.", keyPoints: ["Alert at 80%, 100% of budget via email or SNS", "Budget Actions: auto-apply IAM deny policy or stop EC2/RDS", "Track actual vs forecast spend", "First 2 budgets free"], when: "Preventing surprise bills; per-team or per-project cost control", pricing: "First 2 budgets free. $0.02/day per budget after." },
      { name: "Cost Explorer", full: "AWS Cost Explorer", color: "#E8983C", lightBg: "#FEF5ED", icon: "📈", tagline: "Visualise and analyse your actual spend", analogy: "Your AWS bank statement with graphs — see where money is going and spot waste.", keyPoints: ["Visualise by service, region, tag, account", "EC2 rightsizing recommendations", "Forecasts future spend from trends"], when: "Reviewing your bill, finding waste, RI purchase decisions", pricing: "$0.01/API request (console use free)." },
      { name: "Compute Optimizer", full: "AWS Compute Optimizer", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "📊⚙️", tagline: "ML-based rightsizing recommendations for EC2, Lambda, ECS, and EBS — reduce cost and improve performance", analogy: "A fitness coach for your AWS resources. It watches how hard each resource is actually working (CPU, memory, network) and tells you 'this EC2 is doing the work of a lightweight runner but you've given it the body of a weightlifter — downsize and save money'.", keyPoints: ["Analyses CloudWatch metrics over the past 14 days (or up to 93 days with enhanced infrastructure metrics) and uses ML to model resource utilisation patterns", "Supported resources: EC2 instances, EC2 Auto Scaling groups, Lambda functions, ECS services on Fargate, EBS volumes", "Recommendations: over-provisioned (downsize → save cost), under-provisioned (upsize → improve performance), optimised (good fit), not enough data (insufficient history)", "Finding risk: each recommendation shows estimated cost savings AND performance risk of the change — low risk = high confidence the new size handles the workload", "Enhanced infrastructure metrics (paid add-on): extends the lookback period to 93 days and improves recommendation accuracy for seasonal/cyclical workloads; ~$0.0003360/resource/hr", "Opt-in service: no automatic changes — recommendations are advisory only; you decide when and whether to act", "vs Trusted Advisor: TA shows general cost/performance checks. Compute Optimizer is deeper — ML-driven, per-resource rightsizing with specific replacement recommendations", "Integration: findings exportable to S3; viewable in AWS Console; API for programmatic access"], when: "After migration when correct sizing is unknown; regular cost review to eliminate over-provisioning; Lambda memory optimisation (higher memory can actually reduce cost by finishing faster)", pricing: "Basic: free. Enhanced metrics: ~$0.0003360/resource/hr. Exam tip: 'rightsizing EC2 or Lambda', 'ML-based recommendations', 'reduce over-provisioning' → Compute Optimizer. Generic cost checks → Trusted Advisor." },
      { name: "Cost Optimization Hub", full: "AWS Cost Optimization Hub", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🎯💰", tagline: "Single pane of glass for all cost-saving recommendations across your org", analogy: "Compute Optimizer is a personal trainer who analyses each individual athlete (resource). Cost Optimization Hub is the sports director who collects reports from all trainers AND the finance team, ranks every suggestion by dollar savings, and hands you one prioritised action list.", keyPoints: ["Aggregates recommendations from multiple sources into one view: AWS Compute Optimizer (rightsizing), Cost Explorer (Reserved Instance and Savings Plans purchase recommendations), and more", "Shows estimated monthly savings for each recommendation — lets you sort and filter by savings amount, resource type, account, region, or tag across your entire AWS Organization", "Covers more recommendation types than Compute Optimizer alone: EC2 rightsizing, Lambda rightsizing, ECS Fargate rightsizing, Savings Plans, Reserved Instances, EBS volume rightsizing, RDS rightsizing", "Available in AWS Billing & Cost Management console — no separate service to enable; free to use", "Does NOT make changes automatically — all recommendations are advisory; you act on them manually or via automation", "vs Compute Optimizer: Compute Optimizer = deep ML analysis of resource utilisation for rightsizing only. Cost Optimization Hub = broader aggregation of ALL recommendation types (rightsizing + commitment discounts) in one ranked list", "vs Trusted Advisor: TA has basic cost checks (underutilised EC2, idle load balancers). Cost Optimization Hub is more comprehensive, integrates Compute Optimizer-quality recommendations, and is org-wide"], when: "Q asks for a single view of all cost optimisation opportunities across an AWS Organisation, or asks to identify the highest-dollar savings recommendations in one place → Cost Optimization Hub", pricing: "Free. Exam tip: distinguish three tools — Trusted Advisor (broad best-practice checks, basic cost), Compute Optimizer (deep ML rightsizing for specific resource types), Cost Optimization Hub (aggregated savings dashboard across all recommendation types org-wide)." },
      { name: "Pricing Calculator", full: "AWS Pricing Calculator", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🧮", tagline: "Estimate your bill before you build", analogy: "A quote tool — configure planned services, get a monthly estimate before committing.", keyPoints: ["Free at calculator.aws", "Estimate service by service", "Export and share with team"], when: "Planning projects, justifying spend, comparing architectures", pricing: "Free to use at calculator.aws" },
    ]
  },
  governance: {
    tab: "🏢 Governance",
    items: [
      { name: "🏢 Multi-Account Management", isHeader: true, color: "#2E86AB" },
      { name: "Organizations", full:"AWS Organizations", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏢🗂️", tagline: "Manage multiple AWS accounts centrally", analogy: "A company org chart for AWS — group accounts into OUs (like departments), apply policies centrally, and consolidate billing into one invoice.", keyPoints: ["Management account owns the org; member accounts are child accounts", "OUs (Organisational Units): group accounts for policy inheritance", "Consolidated billing: volume discounts across all accounts", "Enables SCPs, Control Tower, RAM, and cross-account roles"], when: "Any organisation with more than one AWS account; enforcing guardrails; consolidated billing", pricing: "Free." },
      { name: "SCPs", full: "Service Control Policies", color: "#E8593C", lightBg: "#FEF0ED", icon: "🚫🏢", tagline: "Guardrails for entire accounts or OUs", analogy: "A constitution for your AWS accounts — even the root user can't violate an SCP. It defines the maximum permissions any identity in the account can ever have.", keyPoints: ["Applied to OUs or individual accounts in AWS Organizations", "SCP doesn't grant permissions — it restricts what IAM policies can allow", "Allow list vs deny list strategies", "Example: 'No account in the Prod OU may delete CloudTrail logs'", "Does NOT affect the management account", "If a user/role has an IAM permission that grants an action NOT allowed or explicitly denied by SCP → the action is BLOCKED (SCP wins)", "SCP affects ALL users and roles in member accounts, INCLUDING the root user of member accounts", "SCP does NOT affect service-linked roles (they bypass SCPs)"], when: "Enforcing region restrictions, preventing resource deletion, blocking risky services across all accounts", pricing: "Free (requires AWS Organizations)." },
      { name: "Control Tower", full: "AWS Control Tower", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🗼🏢", tagline: "Set up and govern a multi-account AWS environment", analogy: "A move-in-ready apartment complex — Control Tower builds out a landing zone with accounts, networking, logging, and guardrails pre-configured so you start on solid ground.", keyPoints: ["Landing Zone: baseline multi-account setup (Log Archive, Audit, Sandbox accounts)", "Guardrails: pre-built SCPs and Config rules (preventive + detective)", "Account Factory: vend new accounts with standard config in minutes", "Dashboard: compliance status across all accounts at a glance"], when: "Starting a multi-account AWS strategy; standardising new account provisioning; compliance at scale", pricing: "Free. Pay for underlying services (Config, CloudTrail, SNS, etc.)." },
      { name: "🔍 Resource Management & Compliance", isHeader: true, color: "#1A7A4A" },
      { name: "RAM", full:"AWS Resource Access Manager", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🤝🏢", tagline: "Share AWS resources across accounts", analogy: "A shared office floor — one account owns the infrastructure (a Transit Gateway, subnet, or License Manager config) and RAM lets other accounts use it without duplicating it.", keyPoints: ["Share: VPC subnets, Transit Gateways, Route 53 Resolver rules, License Manager configs", "Recipients can use shared resources as if they owned them", "No data copying — the resource stays in the owner's account", "Works within an Organisation or with individual accounts"], when: "Sharing a central Transit Gateway across accounts; VPC subnets across teams; centralising software licences", pricing: "Free. You pay for the shared resources." },
      { name: "Config", full: "AWS Config", color: "#7D6608", lightBg: "#FEFCE8", icon: "📐🏢", tagline: "Continuous compliance and config tracking", analogy: "A time machine for your AWS settings — see what every resource looked like at any point, and get alerted when something drifts from your standards.", keyPoints: ["Records configuration history of every AWS resource", "Config Rules: evaluate resources against compliance policies (managed or custom Lambda)", "Conformance Packs: bundle of rules for CIS, PCI-DSS, NIST standards", "Aggregator: view compliance across multiple accounts and regions"], when: "Compliance audits, detecting drift from approved baselines, investigating 'what changed?'", pricing: "$0.003/config item recorded · $1/active rule/month · $0.0012/conformance pack rule." },
      { name: "Trusted Advisor", full: "AWS Trusted Advisor", color: "#C0392B", lightBg: "#FDEDEC", icon: "💡🏢", tagline: "Automated best-practice checks across your account", analogy: "A consultant who runs through a checklist of your account every day — flagging open S3 buckets, idle resources, security gaps, and service limit risks.", keyPoints: ["5 pillars: Cost, Performance, Security, Fault Tolerance, Service Limits", "~7 core checks free for all accounts", "Full 400+ checks require Business or Enterprise Support", "Integrates with CloudWatch for automated alerts"], when: "Regular account hygiene; spotting over-provisioned resources; pre-audit security checks", pricing: "Core checks free. Full access: Business Support from $100/month." },
    ]
  },
  scenarios: {
    tab: "🎯 Exam Scenarios",
    items: [
      { name: "🔒 Design Secure Architectures", isHeader: true, color: "#6B48C8" },
      { name: "ALB + EC2 + RDS Security Groups", full: "Three-Tier Security Group Rules: ALB → EC2 → RDS", color: "#E8593C", lightBg: "#FEF0ED", icon: "🧱🔗🗄️", tagline: "Each tier's SG references the upstream tier's SG — never hardcode IPs or open wide CIDR ranges", keyPoints: ["ALB Security Group: inbound port 443 from 0.0.0.0/0 (public HTTPS from internet); no inbound on port 80 directly — ALB terminates HTTPS and forwards HTTP internally", "EC2 Security Group: inbound port 80 from ALB Security Group only — ALB decrypts HTTPS and forwards plain HTTP to instances; never expose port 80 to the internet", "RDS Security Group: inbound port 5432 (PostgreSQL) from EC2 Security Group only — DB layer never exposed to ALB or internet", "Why reference SGs not CIDRs? Instances launch/terminate and IPs change — SG references always point to the right set of instances regardless", "HTTPS termination at ALB: ALB handles TLS certificates (ACM), decrypts at the ALB, forwards HTTP to EC2; EC2 doesn't need HTTPS cert management", "Outbound rules: by default AWS allows all outbound — only restrict inbound; leave outbound open unless the question specifies otherwise"], when: "Q asks how to configure SGs for an ALB + EC2 ASG + RDS stack → ALB: 443 from internet; EC2: 80 from ALB SG; RDS: 5432 from EC2 SG", pricing: "Exam tip: the exam commonly asks 'select three' for three-tier SG config. The three answers are always: (1) ALB inbound 443 from 0.0.0.0/0, (2) EC2 inbound 80 from ALB SG, (3) RDS inbound DB port from EC2 SG." },
      { name: "Security Group Chaining", full: "Tiered Security Groups: Reference SG Instead of CIDR", color: "#E8593C", lightBg: "#FEF0ED", icon: "🧱🔗", tagline: "Reference a security group as the source/destination — locks traffic to that exact tier", analogy: "Instead of writing 'anyone wearing a blue badge can enter', you write 'only people holding a Security Group A badge can enter' — as instances come and go, the rule always points at the right group, not a brittle IP range.", keyPoints: ["Security groups can reference other security groups as inbound/outbound rules — no need for IP ranges", "Web tier (SG A): inbound port 443 from 0.0.0.0/0 (public HTTPS); outbound port 1433 to SG B (DB tier only)", "DB tier (SG B): inbound port 1433 from SG A only — rejects anything not originating from web tier, regardless of IP", "Why not CIDR? IPs of EC2 instances change on stop/start or scale-out — SG references stay valid automatically", "Principle: never open DB security groups to IP ranges or 0.0.0.0/0 — always reference the upstream application SG", "Multi-tier extension: API tier (SG C) references web SG (SG A); DB tier references API SG (SG C) — clean trust chain with no hardcoded IPs"], when: "Q describes web + DB tier needing secure connectivity — most secure answer is always SG B inbound from SG A (not IP range, not 0.0.0.0/0)", pricing: "Exam tip: if options include both 'allow from 10.0.0.0/16' and 'allow from security group A', the SG reference is always MORE secure — it scopes to exactly those instances, not the whole subnet." },
      { name: "EFS Access Control", full: "EFS: IAM vs Security Groups", color: "#6B48C8", lightBg: "#EEE8FF", icon: "📁🔐", tagline: "Two layers: who can mount vs who can reach it", analogy: "A building with a keycard system (IAM) AND a locked door (Security Group) — the keycard controls who's allowed in, the door controls who can physically get to it.", keyPoints: ["IAM policy controls mount permissions — which EC2 identities can call efs:ClientMount", "Security Groups control network traffic — which EC2s can reach the EFS mount target on port 2049 (NFS)", "Both must allow access; either one being restrictive blocks access", "Attach the IAM policy to the EC2 instance role, not the user"], when: "Q asks: 'only permitted EC2 instances can access EFS' — answer uses BOTH IAM + SG", pricing: "Exam tip: 'access control' questions on EFS almost always require BOTH layers as the correct answer." },
      { name: "S3 + CloudFront OAC", full: "S3: Block Direct Access via OAC", color: "#E8983C", lightBg: "#FEF5ED", icon: "🪣🔒", tagline: "CloudFront in front — S3 bucket stays private", analogy: "A museum that only lets people in through the front lobby (CloudFront). Side doors (direct S3 URLs) are locked. The lobby checks tickets before anyone gets in.", keyPoints: ["Make bucket private (block all public access)", "Create CloudFront distribution with S3 as origin", "Enable OAC (Origin Access Control) — the modern replacement for OAI", "S3 bucket policy allows only the CloudFront distribution principal to GetObject", "Users access via CloudFront URL only; direct S3 URLs return 403"], when: "Q says 'serve content securely via CloudFront, prevent direct S3 access' — answer: OAC + private bucket", pricing: "Exam trap: OAI (old) vs OAC (new) — AWS recommends OAC for all new distributions." },
      { name: "Encryption Layers", full: "Encryption: At Rest vs In Transit vs Client-Side", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔐📦", tagline: "Where is the data encrypted — disk, wire, or client?", analogy: "At rest = locked in a safe. In transit = armoured van. Client-side = you brought your own safe and only you have the key.", keyPoints: ["At rest (S3): SSE-S3 (AWS manages keys), SSE-KMS (you control keys in KMS), SSE-C (you provide key per request)", "At rest (EBS/RDS): enable encryption at creation — uses KMS CMK under the hood", "In transit: enforce HTTPS/TLS — use bucket policy with aws:SecureTransport condition to deny HTTP", "Client-side: encrypt before sending to AWS — you manage keys entirely, AWS never sees plaintext", "KMS CMK: Customer Managed Key — you control rotation, access policy, and audit via CloudTrail"], when: "Q says 'company manages its own encryption keys' → SSE-KMS with CMK. 'Prevent unencrypted uploads' → bucket policy + SSE enforcement", pricing: "Exam tip: SSE-KMS gives key usage audit trails via CloudTrail. SSE-S3 does not — use SSE-KMS for compliance." },
      { name: "S3 Retention Config", full: "S3 Object Lock: How Retention Periods Actually Work", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔒📅", tagline: "Retention rules not working? Check how and where they're set", analogy: "A lease agreement — you can set a standard lease length for all tenants (bucket default), but any individual tenant can sign a different-length lease (object-level override). The individual lease wins.", keyPoints: ["Retention can be set two ways: (1) bucket default — applies to all new objects unless overridden; (2) explicitly on an object version — you set a specific Retain Until Date for that version", "Object-level explicit retention overrides the bucket default for that specific version", "Different versions of the same object can have different retention modes (Governance vs Compliance) and different Retain Until Dates", "Governance mode: users with s3:BypassGovernanceRetention permission can delete. Compliance mode: nobody can — including root", "Legal Hold is separate — no expiry date, toggled on/off, blocks deletion regardless of retention period", "Common misconfiguration: bucket default set correctly but individual object versions were explicitly set to shorter periods — the explicit setting wins and overrides the default"], when: "Q says 'retention rules not working as expected' or asks which statements about S3 retention are valid — know both bucket-level and object-version-level config", pricing: "Exam tip: two correct statements to remember — (1) explicit retention on an object version sets a Retain Until Date; (2) different versions of the same object CAN have different retention modes and periods." },
      { name: "API Call Monitoring", full: "CloudTrail → CloudWatch Metric Filter → Alarm → SNS", color: "#E67E22", lightBg: "#FEF9EC", icon: "🔍📡", tagline: "Detect unusual API activity and alert in near-real time", analogy: "A CCTV system with a motion sensor — CloudTrail records everything on tape (logs), the metric filter watches for a specific pattern (suspicious movement), and CloudWatch Alarm + SNS triggers the security guard's pager when it spots one.", keyPoints: ["CloudTrail logs every API call to CloudWatch Logs (enable trail → send to CW Logs log group)", "CloudWatch Metric Filter: scans log events for a pattern (e.g. error codes like AccessDenied, UnauthorizedOperation) and increments a custom metric counter each time it matches", "CloudWatch Alarm: fires when the metric crosses a threshold (e.g. >10 errors in 5 minutes)", "Alarm action: publish to SNS topic → notifies team via email, SMS, or triggers Lambda", "The chain: CloudTrail → CW Logs → Metric Filter → CW Alarm → SNS"], when: "Q asks: 'automated near-real-time alerting for unusual/illegal API calls' — this 4-step chain is the answer", pricing: "Exam tip: GuardDuty also detects API anomalies but is ML-based and less targeted. Metric Filter gives you precise control over which error codes to track. If the Q mentions 'specific error codes' or 'metric-based alarm' → CloudWatch Metric Filter, not GuardDuty." },
      { name: "Geo Content Restriction", full: "Restricting Content by Country: CloudFront + Route 53", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌍🚫", tagline: "Two independent tools to enforce geographic content licensing", analogy: "CloudFront geo restriction is a bouncer at the door checking passports and turning people away at the CDN edge. Route 53 geolocation routing is a switchboard that routes callers to different numbers — or to a dead end — based on where they're calling from.", keyPoints: ["CloudFront geo restriction: whitelist (only allowed countries) or blacklist (blocked countries get 403 Forbidden) — enforced at CloudFront edge, built-in, free", "Route 53 geolocation routing: route DNS queries to different endpoints based on continent, country, or US state — can point blocked countries to an 'access denied' page or return no record", "Both are valid and complementary — CloudFront blocks at delivery layer; Route 53 blocks at DNS layer", "CloudFront geo restriction uses CloudFront's own IP-geolocation database — no third-party service needed", "Route 53 geolocation is more flexible (e.g. route UK users to EU servers, US users to US servers) but requires separate endpoints per region"], when: "Q asks about enforcing distribution rights or licensing restrictions by country (select two) → CloudFront geo restriction AND Route 53 geolocation routing", pricing: "Exam tip: the two correct answers for geo-restriction questions are almost always CloudFront georestriction + Route 53 geolocation routing — they operate at different layers (CDN vs DNS) and can both be used together." },
      { name: "EKS IRSA", full: "EKS: Pod-Level IAM Access Control with IRSA", color: "#2E86AB", lightBg: "#EBF5FB", icon: "⎈🔑", tagline: "Give each group of Pods its own IAM role — not the whole node's role", analogy: "Without IRSA, every app running on an EC2 node shares the node's one IAM badge — anyone on the node gets the same access. IRSA gives each app its own individual badge, so the UI app only opens the DynamoDB door and the data app only opens the S3 door.", keyPoints: ["Pod: the smallest unit in Kubernetes — one or more containers running together, sharing a network and storage; the thing that IRSA assigns an IAM identity to", "Problem: EKS worker node has one EC2 IAM role — all Pods on that node inherit it, violating least privilege", "IRSA solution: create a Kubernetes service account → annotate it with an IAM role ARN → assign service account to Pod spec → Pod gets scoped credentials via OIDC token", "AWS OIDC provider: you register your EKS cluster's OIDC endpoint with IAM — this is how AWS trusts the Kubernetes service account token", "Each service account maps to one IAM role — UI service account → DynamoDB-only role; data service account → S3-only role", "Credentials are short-lived and automatically rotated — more secure than storing access keys in env vars"], when: "Q asks about least-privilege IAM access for specific EKS workloads/Pods → IRSA (IAM Roles for Service Accounts)", pricing: "Exam tip: IRSA is the AWS-native solution for per-workload IAM in EKS. Wrong answers: using a single node IAM role (too broad); storing access keys in Kubernetes secrets (insecure)." },
      { name: "Encrypt Existing RDS", full: "Encrypting an Unencrypted RDS Database", color: "#C0392B", lightBg: "#FDEDEC", icon: "🗄️🔐", tagline: "You cannot enable encryption on a running RDS — you must snapshot, copy encrypted, restore", analogy: "You can't seal a document that's already open. You have to photocopy it into a sealed envelope, then shred the original.", keyPoints: ["AWS does NOT allow enabling encryption on an existing unencrypted RDS instance directly — there is no toggle", "Required steps: (1) Take a snapshot of the unencrypted DB, (2) Copy the snapshot with encryption enabled (choose a KMS CMK during copy), (3) Restore a new DB instance from the encrypted snapshot, (4) Update app connection string to new endpoint, (5) Terminate the old unencrypted instance", "Encrypted snapshots can be shared cross-account (the recipient must have access to the KMS key)", "Read Replicas and Multi-AZ standbys of an encrypted instance are automatically encrypted with the same key", "Cross-region copy: you can copy an encrypted snapshot to another region — AWS re-encrypts it with a KMS key in the destination region"], when: "Q asks how to encrypt an existing unencrypted RDS database → snapshot → copy as encrypted → restore → terminate original", pricing: "Exam trap: 'modify the instance to enable encryption' is NOT possible. The only path is the snapshot copy workflow." },
      { name: "S3 Access Points per Service", full: "S3 Access Points for Per-Prefix Access Control", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🚪", tagline: "Multiple services sharing one bucket — each gets its own access point scoped to its prefix", analogy: "A shared office building (S3 bucket) with separate key-card doors for each team (access points) — the analytics team's card only opens the analytics room, the finance team's card only opens the finance room, even though they're all in the same building.", keyPoints: ["Problem: multiple services writing to dedicated prefixes in one bucket — a single bucket policy either becomes enormous or grants too-broad access", "Solution: create one S3 Access Point per service, each with a policy that restricts access to only that service's prefix (s3:prefix condition)", "Each access point has its own ARN — the service uses the access point ARN instead of the bucket ARN in its SDK calls", "Bucket policy delegation: add a bucket policy statement granting s3:* to any access point ARN in the account — this delegates access control to the individual access point policies", "Scalability: bucket policy stays simple and stable; new services just get a new access point — no need to touch the shared bucket policy", "Network isolation: optionally restrict each access point to a specific VPC so only that service's infrastructure can use it", "Wrong answers: a single complex bucket policy (hard to maintain, 20 KB limit), separate buckets per service (operational overhead, no shared prefix model), IAM policies alone (can't restrict to prefix without duplicating across many roles)"], when: "Q describes multiple services/teams sharing one S3 bucket needing isolation by prefix with minimal ops overhead → S3 Access Points with per-service policies", pricing: "Exam tip: Access Points are the AWS-recommended pattern for scaling per-client bucket access. Key phrase: 'each service accesses only its own prefix' + 'scalable and maintainable' → Access Points." },
      { name: "Transfer Family SFTP to S3", full: "AWS Transfer Family: SFTP-to-S3 with Per-Vendor IAM Roles", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔄🔒", tagline: "Legacy SFTP vendors upload directly to S3 — Transfer Family + IAM roles per vendor + Cognito federation", keyPoints: ["Deploy AWS Transfer Family with SFTP protocol enabled — provides a managed SFTP endpoint that stores files directly in S3; no EC2 SFTP server to manage", "IAM role per vendor: each SFTP user maps to an IAM role scoped to a specific S3 bucket/prefix — vendors can only read/write their own prefix", "Identity federation: use Cognito (or custom IdP via API Gateway + Lambda) as the Transfer Family identity provider — internal teams manage vendor identities centrally", "Home directory mapping: each vendor's SFTP home directory points to their dedicated S3 prefix — they cannot traverse outside it", "Why not self-managed EC2 SFTP: requires patching, scaling, HA configuration — not fully managed", "Why not pre-signed URLs: SFTP vendors cannot use HTTP-based mechanisms — they need native SFTP protocol"], when: "Q describes legacy SFTP vendors needing to upload to S3, fully managed, no infra management, identity federation → AWS Transfer Family + IAM roles per vendor + Cognito/custom IdP", pricing: "Exam tip: 'SFTP' + 'S3' + 'fully managed' + 'no infrastructure' = AWS Transfer Family. Two correct answers: (1) Transfer Family SFTP endpoint → S3 + IAM roles per vendor, (2) S3 bucket policies + Cognito identity federation for fine-grained access." },
      { name: "API GW IP Allowlist", full: "API Gateway: Restrict Access to Trusted IPs via Resource Policy", color: "#C0392B", lightBg: "#FDEDEC", icon: "🚪📋", tagline: "Resource policy with explicit Deny for all IPs except the allowlist — least operational complexity", analogy: "A bouncer with a guest list — everyone who's not explicitly on the list gets turned away at the door, with no extra infrastructure needed.", keyPoints: ["API Gateway resource policy: a JSON IAM policy attached directly to the API — controls which principals, IP ranges, or VPCs can invoke it", "For IP-based restriction: add an explicit Deny on execute-api:Invoke for all source IPs (NotIpAddress condition) except the trusted ranges (aws:SourceIp)", "Least operational complexity: no WAF, no VPC Link, no Lambda authorizer needed — resource policy is a single config on the API itself", "Structure: one Deny statement with condition NotIpAddress aws:SourceIp [trusted IPs] + one Allow statement for the trusted range", "Private API (alternative): make the API private (VPC endpoint only) — only accessible within a VPC; stricter than IP allowlisting but requires VPC connectivity", "WAF IP-based rules can also restrict access but add cost and complexity — resource policy is simpler for a fixed IP allowlist", "Resource policy applies before the request reaches Lambda/backend — rejected requests are never forwarded"], when: "Q asks for least-complexity way to restrict API Gateway to specific IP ranges → resource policy with explicit Deny for non-allowlisted IPs", pricing: "Exam tip: resource policy = zero extra cost, single config. WAF = extra charge. Lambda authorizer = extra invocation cost + code to maintain. For 'least operational complexity' + 'IP allowlist' → resource policy." },
      { name: "Cross-Account EFS Lambda", full: "EFS Cross-Account Access from Lambda", color: "#2E86AB", lightBg: "#EBF5FB", icon: "📁🔗λ", tagline: "Lambda in Account A mounts EFS in Account B via VPC peering + EFS resource policy", analogy: "Two office buildings (accounts) connected by a covered walkway (VPC peering). The file room in building B (EFS) puts a sign on the door listing who from building A is allowed in (resource policy). Lambda in building A walks over, shows ID, and gets access.", keyPoints: ["EFS resource-based policy: grant cross-account access by specifying the principal AWS account/role allowed to mount the file system", "Networking: Lambda in Account A must reach the EFS mount target in Account B — requires VPC peering (or Transit Gateway) between the two VPCs; Lambda must be in the peered VPC", "EFS access point: scopes access to a specific directory within the file system — attach the access point ARN in Lambda's file system config", "Lambda file system config: specify the EFS access point ARN + local mount path (e.g. /mnt/data)", "Why not DataSync or S3 copy: those add latency and ops overhead — direct EFS mount is the lowest-overhead, most scalable approach for ongoing access"], when: "Q describes Lambda needing direct access to EFS in another account with minimal ops overhead → EFS resource policy + VPC peering + EFS access point", pricing: "Exam tip: EFS resource policies enable cross-account filesystem access the same way S3 bucket policies do for objects — it's the EFS equivalent of a bucket policy." },
      { name: "🔁 Design Resilient Architectures", isHeader: true, color: "#2E86AB" },
      { name: "Multi-AZ vs Read Replica", full: "RDS: HA vs Read Scaling", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗄️↔️", tagline: "Multi-AZ = stay up; Read Replica = go faster", analogy: "Multi-AZ is a spare tyre in the boot — there if you need it, invisible otherwise. Read Replica is an extra cashier lane — real customers use it to reduce queue length.", keyPoints: ["Multi-AZ: synchronous standby in another AZ, auto-failover in ~1–2 min — no reads from standby", "Read Replica: async copy, can serve SELECT queries — does NOT auto-failover for writes", "Multi-AZ = High Availability / DR. Read Replica = Performance / read scaling", "You can promote a Read Replica to a standalone DB (DR last resort, but with data lag risk)", "Aurora: replicas double as failover targets — best of both worlds"], when: "Q says 'minimize downtime' or 'survive AZ failure' → Multi-AZ. Q says 'offload reads' or 'scale read traffic' → Read Replica", pricing: "Exam trap: 'improve read performance' never means Multi-AZ. 'High availability' never means Read Replica." },
      { name: "DR Strategies", full: "Disaster Recovery: 4 Strategies Ranked by RTO/Cost", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔁⏱️", tagline: "Scaled-down but fully functional always-on = Warm Standby", analogy: "Backup & Restore = rebuilding from blueprints (cheap, hours). Pilot Light = a gas igniter ready to spark — just the DB core is on, app servers are off (minutes). Warm Standby = a skeleton crew running the full app at low capacity — everything works but small (minutes, minimum RTO). Multi-site = two full offices both open simultaneously (seconds, maximum cost).", keyPoints: ["Backup & Restore: cheapest, highest RTO (hours) — data in S3/Glacier, nothing running; restore from scratch on disaster", "Pilot Light: only the critical core (DB with live replication) stays on in AWS; app servers are OFF (stopped AMIs) and must be launched on disaster — NOT fully functional until disaster strikes", "Warm Standby: scaled-down but FULLY FUNCTIONAL version of the environment runs continuously — all tiers (web, app, DB) at minimum capacity; scale up resources on disaster; lowest RTO without going full Active-Active", "Multi-site / Active-Active: full-scale production running simultaneously in both on-prem and AWS; near-zero RTO/RPO; most expensive", "RTO/Cost order (fastest RTO & most expensive → slowest & cheapest): Multi-site > Warm Standby > Pilot Light > Backup & Restore", "RPO = how much data loss is acceptable. RTO = how long until you're back online. Lower RTO/RPO always costs more"], when: "Q says 'scaled-down version of a fully functional environment always running' + 'recovery time kept to minimum' → Warm Standby. 'Only critical core (DB) running, app servers off' → Pilot Light. 'Nothing running, just backups' → Backup & Restore. 'Full environment in both sites simultaneously' → Multi-site", pricing: "Exam trap: Pilot Light ≠ Warm Standby. Pilot Light = only the DB/data core is live; app tier is OFF and must be started. Warm Standby = ALL tiers running at reduced capacity — it IS functional right now. The phrase 'fully functional environment' is the Warm Standby fingerprint." },
      { name: "HA Min Cost 3 AZs", full: "High Availability Minimum Cost: Spread Across 3 AZs", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏗️💰", tagline: "Need N instances always running? Spread across AZs so any one AZ failure still meets N", keyPoints: ["Requirement: at least 4 instances must always be available; survive any single AZ failure", "If 4 instances in 1 AZ: one AZ failure = all 4 gone — no HA at all", "If 2 instances in each of 2 AZs (4 total): one AZ failure = 2 instances remain — fails the '4 always available' requirement", "If 2 instances in each of 3 AZs (6 total): one AZ failure = 4 instances remain in the other 2 AZs — meets the requirement with minimum instances", "Why not 4 AZs with 1 each? Losing one AZ leaves only 3 instances — still fails the requirement", "Why not 3 AZs with 4 each (12 total)? Meets requirement but 12 instances vs 6 is not minimum cost", "Formula: instances_per_AZ × (total_AZs - 1) ≥ minimum_required. Here: 2 × (3-1) = 4 ✓"], when: "Q asks for minimum number of instances across AZs to always maintain N instances after any single AZ failure → use ceiling(N / (AZ_count - 1)) instances per AZ across at least 3 AZs", pricing: "Exam tip: the magic pattern is '3 AZs, 2 in each' for a 4-instance minimum. Any fewer total instances fails HA; any more fails the minimum-cost constraint." },
      { name: "ASG Unhealthy Instance Not Terminated", full: "Auto Scaling: Why ASG Isn't Terminating an Unhealthy Instance", color: "#E8593C", lightBg: "#FEF0ED", icon: "📈⚠️", tagline: "Three common reasons ASG skips termination of an unhealthy instance", keyPoints: ["Reason 1 — Health check grace period not expired: after launch, ASG waits a configurable grace period (default 300s) before applying health checks — protects against terminating an instance that's still bootstrapping", "Reason 2 — Instance in Impaired status: EC2 status check shows 'Impaired' (hardware issue) but is not the same as a failed health check; ASG only acts on its own health check results, not all EC2 statuses", "Reason 3 — Failed ELB health check, but ASG health check type is EC2 (not ELB): ASG has two health check types: EC2 (default, checks instance status only) and ELB (checks application-level response); if using EC2 health check type, a failed ELB health check is ignored by ASG", "Fix for Reason 3: change ASG health check type to ELB so it acts on application-level health failures", "Other causes: instance is in a state that prevents termination (e.g. lifecycle hook in progress, connection draining active)", "Lifecycle hooks: if a Termination lifecycle hook is configured, the instance stays in 'Terminating:Wait' state until the hook action completes or times out"], when: "Q asks why ASG is not replacing/terminating an unhealthy instance → check grace period, impaired status, and whether health check type matches (EC2 vs ELB)", pricing: "Exam tip: select-three question. The three answers are: grace period not expired, instance in Impaired status, failed ELB health check (when ASG uses EC2 health check type)." },
      { name: "RDS Multi-AZ Maintenance", full: "RDS Multi-AZ: Engine Upgrades Cause Downtime", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗄️🔧", tagline: "OS patches: rolling (no downtime). Engine upgrades: simultaneous — brief downtime", analogy: "Painting the two wings of a building: OS patching paints one wing at a time so the building stays open. An engine upgrade is like replacing the foundation — both wings close together for the duration.", keyPoints: ["Multi-AZ keeps a synchronous standby in another AZ — normally provides automatic failover with ~1–2 min downtime", "OS / minor patches: AWS applies to standby first, fails over, then patches the former primary — minimal downtime", "DB engine version upgrades (major or minor): applied to BOTH primary and standby simultaneously — causes downtime for the duration of the upgrade; Multi-AZ does NOT protect against this", "Maintenance window: scheduled weekly window (30 min default) during which AWS applies pending maintenance — you can defer but not avoid", "To reduce risk: schedule the maintenance window during off-peak hours; test the upgrade in a non-production environment first", "Aurora difference: Aurora engine upgrades also require a brief outage but can be faster due to the shared storage architecture"], when: "Q asks what happens to a Multi-AZ RDS during a DB engine upgrade → both instances are upgraded simultaneously, causing downtime — Multi-AZ does NOT prevent engine upgrade downtime", pricing: "Exam trap: Multi-AZ is NOT a zero-downtime solution for engine upgrades. It only provides automatic failover for instance/AZ failures and OS-level patching." },
      { name: "SNS Lambda Concurrency", full: "SNS → Lambda: Account Concurrency Quota", color: "#E8983C", lightBg: "#FEF5ED", icon: "📢λ", tagline: "Notifications dropping at peak? Lambda hit its concurrency limit", analogy: "Lambda concurrency is like a restaurant's maximum seating. When SNS tries to deliver 5,000 messages at once but the restaurant only seats 1,000, the overflow gets turned away — messages are dropped.", keyPoints: ["Lambda has a regional account-level concurrency quota (default 1,000 concurrent executions)", "SNS invokes Lambda synchronously — if invocations exceed the concurrency limit, SNS receives a throttle error and drops notifications", "Fix: request an AWS Support quota increase for Lambda concurrent executions for the account/region", "Alternative mitigations: use SQS between SNS and Lambda as a buffer (fan-out pattern) so Lambda processes at its own rate without dropping messages", "Reserved concurrency: set aside a quota for a specific function — protects it from being starved but doesn't increase the account total"], when: "Q describes SNS → Lambda notifications failing or dropping at peak traffic — answer: contact AWS support to raise Lambda account concurrency limit", pricing: "Exam tip: the key phrase is 'account concurrency quota' — this is a hard per-account, per-region limit that requires a support request to raise, not a config change." },
      { name: "Route 53 Resolver Hybrid", full: "Route 53 Resolver: Hybrid DNS Between VPC and On-Prem", color: "#E8593C", lightBg: "#FEF0ED", icon: "🌍🔀", tagline: "Outbound = VPC queries on-prem DNS. Inbound = on-prem queries AWS DNS.", analogy: "Think of endpoints as phone lines: Outbound endpoint = a phone line from your VPC office that lets you call the on-prem switchboard. Inbound endpoint = a phone line installed in your VPC that lets the on-prem office call and ask 'what's the IP of that RDS instance?'", keyPoints: ["Default VPC Resolver (at VPC+2): answers queries for Route 53 private hosted zones and public DNS — but cannot forward to on-prem or accept queries from on-prem", "Outbound endpoint: ENI created in your VPC; you attach forwarding rules that say 'send all *.corp.internal queries to 10.1.1.53 (on-prem DNS)'; traffic flows VPC → on-prem over VPN/DX", "Inbound endpoint: ENI in your VPC that on-prem DNS servers forward queries TO; on-prem gets AWS private DNS answers back over VPN/DX", "Forwarding rule: specifies a domain suffix + target DNS IP — e.g. domain: corp.internal, target: 10.1.1.53; must be associated with a VPC to apply", "For this scenario (VPC resolves on-prem): Outbound endpoint + forwarding rule for the on-prem domain → on-prem DNS server IP", "For the reverse (on-prem resolves AWS): on-prem DNS conditionally forwards to Inbound endpoint IP"], when: "Q describes hybrid DNS where VPC needs to resolve on-prem hostnames → Outbound endpoint + forwarding rule. On-prem needs to resolve AWS DNS → Inbound endpoint.", pricing: "Exam tip: Outbound = traffic leaves AWS toward on-prem DNS. Inbound = traffic arrives from on-prem into AWS DNS. The direction is from the resolver's perspective, not the query origin." },
      { name: "⚡ Design High-Performing Architectures", isHeader: true, color: "#E8593C" },
      { name: "SQS vs SNS vs EventBridge", full: "Messaging: Queue vs Fan-out vs Event Routing", color: "#9B59B6", lightBg: "#F5EEF8", icon: "📨🔀", tagline: "Pick the right message pattern for the question", analogy: "SQS = a to-do tray (one worker picks up each task). SNS = a PA announcement (everyone hears it). EventBridge = a smart switchboard (routes calls based on content).", keyPoints: ["SQS: one consumer processes each message; decouples producer from consumer; retries via visibility timeout", "SNS: fan-out — one message to many subscribers simultaneously (Lambda, SQS, email, HTTP)", "SNS → SQS fan-out: SNS publishes to multiple SQS queues for parallel processing with durability", "EventBridge: rule-based routing from AWS services, SaaS, or custom apps; replaces CloudWatch Events", "EventBridge Scheduler: replaces cron/scheduled Lambda triggers"], when: "Q asks 'process orders one at a time' → SQS. 'Notify 5 systems at once' → SNS. 'Route based on event type' → EventBridge", pricing: "Exam trap: SQS does NOT push — consumers poll. SNS pushes but doesn't guarantee order or dedup (use SQS FIFO for that)." },
      { name: "Stateless vs Stateful Scale", full: "Architecture: Where to Store Session State", color: "#E8983C", lightBg: "#FEF5ED", icon: "🏗️📊", tagline: "Make your app stateless so it can scale horizontally", analogy: "A stateful app is like a sticky note pinned to one cashier's desk — if that cashier is replaced, the note is gone. A stateless app stores notes in a shared board (ElastiCache/DynamoDB) anyone can read.", keyPoints: ["Problem: EC2 behind ALB stores session in local memory → user hits different instance → session lost", "Solution: externalise session state to ElastiCache (Redis) or DynamoDB — any instance can serve any request", "ALB sticky sessions (session affinity) can work but prevent true horizontal scaling — avoid for stateless design", "Use S3 for shared file state; EFS for shared filesystem across instances", "Stateless instances can be terminated/replaced safely — essential for Auto Scaling to work well"], when: "Q says 'users losing sessions when new instances are added' → store state in ElastiCache or DynamoDB", pricing: "Exam tip: any question about 'scaling EC2 with user sessions' = answer involves ElastiCache Redis or DynamoDB for session store." },
      { name: "RDS Proxy Scenario", full: "RDS Proxy: Lambda + Database Connection Exhaustion", color: "#E8593C", lightBg: "#FEF0ED", icon: "🔄🗄️", tagline: "Lambda + RDS = connection exhaustion without a proxy", analogy: "Without a proxy, 1,000 Lambda invocations = 1,000 DB connections hammering the kitchen at once. With RDS Proxy, all 1,000 go through a concierge that holds just 20 standing orders with the kitchen.", keyPoints: ["Problem: Lambda is stateless — each cold start opens a new DB connection; RDS has a hard connection limit", "Symptom in question: 'Lambda functions timing out', 'too many connections error', 'database becoming unavailable under load'", "Solution: add RDS Proxy between Lambda and RDS/Aurora — proxy pools and reuses connections", "Bonus: credentials moved to Secrets Manager; IAM auth to proxy; ~66% faster Multi-AZ failover", "Wrong answers to watch for: increasing RDS instance size (doesn't fix connection count), ElastiCache (wrong layer — caching not connections)"], when: "Any Q with Lambda + RDS performance/availability issues at scale → RDS Proxy is the answer", pricing: "Exam tip: RDS Proxy is almost always the answer when Lambda + RDS + high concurrency appear together in the same question." },
      { name: "S3 Event → SQS → Lambda", full: "Near Real-Time S3 Processing: S3 → SQS → Lambda", color: "#E8983C", lightBg: "#FEF5ED", icon: "🪣📬λ", tagline: "Replace nightly batch EC2 with event-driven Lambda triggered via SQS", analogy: "Instead of a night-shift crew processing everything at midnight, each photo rings a bell the instant it arrives and a worker appears immediately to handle just that one photo.", keyPoints: ["S3 event notification → SQS queue → Lambda polls queue and processes each image", "Why SQS between S3 and Lambda? Decouples upload bursts from processing — SQS buffers images so Lambda processes at a controlled rate; if Lambda fails, the message stays in the queue and retries (no lost events)", "Why not direct S3 → Lambda trigger? No built-in retry buffer — a Lambda failure loses the event; SQS adds at-least-once delivery guarantee", "Each Lambda invocation handles one image: ~3 min / 512 MB — within Lambda limits (max 15 min, up to 10 GB memory)", "Cost advantage: Lambda charges per invocation/ms — zero cost when nothing is uploading. EC2 Spot fleet runs all night regardless of actual volume", "Auto-scaling: Lambda scales concurrency automatically as SQS queue depth grows — no capacity planning"], when: "Q says 'process immediately on upload', 'near real-time', 'replace nightly batch', 'minimize cost' → S3 event notification + SQS + Lambda", pricing: "Exam tip: SQS as a buffer between S3 events and Lambda is the canonical serverless pattern — adds retry, durability, and rate control. Without SQS, failed Lambdas lose the event." },
      { name: "API GW + Lambda + Fargate", full: "Serverless Intake + Long-Running Backend: API GW → Lambda → ECS Fargate", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🚪λ🚀", tagline: "Lambda for fast validation, Fargate for long-running compute — fully managed end to end", analogy: "A restaurant: API Gateway is the front door, Lambda is the host who quickly checks your reservation (lightweight validation), and ECS Fargate is the kitchen doing the actual cooking (heavy compute) — no one on the team manages any servers.", keyPoints: ["API Gateway: receives mobile client requests over HTTPS; handles auth, throttling, routing — no servers", "Lambda: ideal for short-lived lightweight tasks (validation, transformation) — max 15 min timeout", "ECS Fargate: fully managed containers for long-running, compute/memory-intensive workloads; no EC2 provisioning or patching; scales automatically", "Lambda triggers ECS task or publishes to SQS → ECS Fargate consumes — decoupled and scalable", "Why not Lambda for backend: Lambda has 15-min max timeout and 10 GB memory cap — not suitable for long-running heavy processing", "Why not EC2: requires provisioning, patching, capacity planning — higher operational overhead than Fargate"], when: "Q describes lightweight intake/validation + long-running backend + fully managed + no server management → API GW + Lambda + ECS Fargate", pricing: "Exam tip: the phrase 'longer duration than typical short-lived tasks' + 'no server management' = ECS Fargate, not Lambda. Lambda for the fast bit, Fargate for the heavy bit." },
      { name: "S3 Prefix Scaling", full: "S3 Performance: Spread Load with Prefixes", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🪣📂", tagline: "S3 throttles per prefix — spread files across prefixes to scale", analogy: "A single checkout lane (one prefix) can only serve so many customers per second. Adding more lanes (more prefixes) multiplies the throughput — same store, no extra cost.", keyPoints: ["S3 baseline: 3,500 PUT/COPY/POST/DELETE and 5,500 GET/HEAD requests per second **per prefix**", "A prefix is any string before the object key — e.g. /customer1/, /2024/01/, /region-us/", "Single bucket with all files at one prefix = all requests share one 5,500 req/s limit → throttling at scale", "Solution: add customer-specific or date-based prefixes — each prefix gets its own independent 5,500 req/s limit", "10 prefixes = 55,000 GET req/s; 100 prefixes = 550,000 GET req/s — at zero extra cost"], when: "Q mentions S3 request throttling, 503 errors, or >5,000 req/s failures → answer is customer-specific prefixes", pricing: "Exam tip: this is free — just a key naming convention change. Wrong answers: switching to multiple buckets (unnecessary), using S3 Transfer Acceleration (speeds up uploads over distance, doesn't fix request rate limits), adding CloudFront (helps with read caching but not the root cause)." },
      { name: "S3 Upload Speed", full: "S3 Upload Performance: Transfer Acceleration + Multipart", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🪣🚀", tagline: "Two independent fixes for slow S3 uploads — both can be used together", analogy: "Transfer Acceleration = switching from country roads to a motorway (AWS backbone). Multipart uploads = splitting a truck's load into smaller vans that travel in parallel — both get things there faster, and together they're faster still.", keyPoints: ["S3 Transfer Acceleration: files enter via nearest CloudFront edge PoP and travel AWS backbone to the S3 bucket — biggest gain for users far from the bucket's region", "Multipart Upload: split large files into parts (recommended >100 MB, required >5 GB) and upload parts in parallel — saturates available bandwidth and allows resuming failed parts", "Both are independent optimisations and can be combined — TA routes the connection, multipart maximises throughput over that connection", "Transfer Acceleration has a test tool — AWS only charges if the upload is actually faster than a direct upload", "Wrong answers: CloudFront (for downloads/distribution, not uploads); increasing S3 bucket size (S3 is unlimited, not the bottleneck)"], when: "Q describes users in distant regions with slow large-file uploads to S3 — answer is TA + multipart (select two)", pricing: "Exam tip: when a question asks for TWO answers to improve S3 upload speed from overseas — S3 Transfer Acceleration AND multipart uploads are the correct pair." },
      { name: "EC2 Placement Groups", full: "EC2 Placement Groups: Cluster vs Spread vs Partition", color: "#C0392B", lightBg: "#FDEDEC", icon: "🖥️🗂️", tagline: "Control where EC2 instances are physically placed", analogy: "Cluster = pack all the servers in the same rack for maximum speed between them. Spread = put each server in a different rack so one rack failure can't take out more than one. Partition = divide into groups where racks aren't shared between groups.", keyPoints: ["Cluster: all instances in same AZ, same rack — lowest latency, highest throughput (10 Gbps between instances); single point of failure for the group", "Spread: each instance on distinct underlying hardware — max 7 instances per AZ per group; best for small sets of critical instances that must survive hardware failure", "Partition: instances divided into logical partitions, each partition on separate racks — scales to 100s of instances; used by Hadoop, Cassandra, Kafka", "HPC workloads → always Cluster placement group (low latency + high throughput between nodes)", "Cluster groups work best with enhanced networking (ENA) and instance types that support it"], when: "Q mentions HPC, tightly coupled compute, MPI workloads, or 'low latency between instances' → Cluster placement group", pricing: "Exam tip: Cluster = performance. Spread = fault isolation for small critical sets. Partition = large distributed systems (Hadoop/Kafka). Don't confuse Spread (max 7/AZ) with Partition (100s of instances)." },
      { name: "SQS FIFO Throughput", full: "SQS FIFO: Ordered Processing at Scale with Batching", color: "#E8593C", lightBg: "#FEF0ED", icon: "💬🔢", tagline: "FIFO = ordered + exactly-once; batching unlocks higher throughput", analogy: "A single checkout lane (FIFO) processes one customer at a time — 300/s bare. Batching is like processing 10 items per customer instead of one — same lane speed, 10× the work per transaction.", keyPoints: ["SQS Standard: best-effort ordering, at-least-once delivery, virtually unlimited throughput — use when order doesn't matter", "SQS FIFO: strict ordering (first-in first-out), exactly-once processing, but limited to 300 msg/s without batching", "Batch mode: send/receive up to 10 messages per API call → 300 × 10 = 3,000 msg/s effective throughput", "For 1,000 msg/s ordered: FIFO + batch mode (4 messages/operation is sufficient: 300 × 4 = 1,200 msg/s — exceeds 1,000)", "Message Group ID: within FIFO, messages with same Group ID are ordered; different Group IDs can be processed in parallel"], when: "Q requires ordered processing + high throughput → SQS FIFO in batch mode. For 1,000 msg/s ordered → FIFO + batching (not Standard queue)", pricing: "Exam tip: SQS Standard cannot guarantee order. Only FIFO guarantees order. FIFO bare limit is 300/s; batch of 10 gives 3,000/s. A batch of 4 gives 1,200/s — enough for 1,000 msg/s." },
      { name: "EBS Boot Volumes", full: "EC2 Boot Volumes: Which EBS Types Are Allowed", color: "#7D6608", lightBg: "#FEFCE8", icon: "💾🖥️", tagline: "Only SSD types can be boot volumes — HDD types cannot", analogy: "A boot volume is the hard drive your computer needs to turn on — it holds the operating system. You can't boot a computer from a slow tape drive or a cold-storage archive disk, only from fast SSD-type drives.", keyPoints: ["Boot volume (root device): the EBS volume that holds the OS; EC2 attaches it as /dev/xvda or /dev/sda1 and boots from it", "CAN be boot volumes: gp3, gp2 (SSD), io1, io2 (Provisioned IOPS SSD) — all SSD types", "CANNOT be boot volumes: st1 (Throughput Optimized HDD) and sc1 (Cold HDD) — HDD types only", "Additional (non-boot) volumes of any type can be attached after launch — st1/sc1 are fine as data volumes", "Reason: HDD types are optimised for sequential large-block I/O, not the random small-block I/O the OS needs during boot"], when: "Q asks which volume types cannot be used as boot volumes → st1 and sc1 (both HDD). All SSD types (gp2, gp3, io1, io2) can be boot volumes", pricing: "Exam tip: the two HDD types (st1, sc1) are the answer whenever a question asks about boot volume restrictions." },
      { name: "RDS Custom Oracle", full: "RDS Custom: Managed DB with OS + Engine Access", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗃️🔧", tagline: "Managed RDS but you can still access the OS and DB engine", analogy: "Standard RDS is a vending machine — AWS controls everything inside. RDS Custom is a leased kitchen — AWS still manages the building (hardware, backups, HA) but you have the keys to the kitchen and can install your own equipment.", keyPoints: ["RDS Custom is available for Oracle and SQL Server only", "Gives DBA SSH access to the underlying EC2 host and OS — can install agents, custom patches, third-party tools", "Supports Multi-AZ for high availability — unlike running Oracle on self-managed EC2", "Use when: legacy Oracle app requires OS-level customisation, custom DB patches, or specialised engine settings not possible in standard RDS", "Standard RDS: AWS fully manages OS + engine — no SSH, no engine internals access; simpler but less flexible", "EC2 self-managed Oracle: full control but you manage all patching, backups, HA yourself — highest operational overhead"], when: "Q mentions Oracle/SQL Server + OS-level access + HA + minimise operational overhead → RDS Custom Multi-AZ", pricing: "Exam tip: the three-way choice is EC2 (full control, high ops) vs RDS Custom (control + AWS-managed HA) vs RDS (fully managed, no access). 'Specialised OS/engine customisation' = RDS Custom." },
      { name: "EC2 User Data Facts", full: "EC2 User Data: Behavior and Execution Rules", color: "#E8593C", lightBg: "#FEF0ED", icon: "🖥️📝", tagline: "User data scripts run as root, only on first launch by default", keyPoints: ["User data: a script (shell, cloud-init) you provide at instance launch to automate bootstrap tasks — install packages, configure settings, start services", "Runs as root: user data scripts execute with root (sudo) privileges by default — no sudo needed inside the script", "Runs once by default: user data executes only during the first boot cycle after launch; subsequent stop/start cycles do NOT re-run it", "Re-running on every boot: possible but requires configuring cloud-init to run on every boot (change cloud-init module frequency) — not default behaviour", "Base64 encoded: user data is base64-encoded when passed to the instance; the instance decodes it automatically", "Size limit: user data is limited to 16 KB in size", "Viewing user data: accessible from within the instance at the instance metadata endpoint http://169.254.169.254/latest/user-data", "Scripts vs cloud-init directives: shell scripts start with #!/bin/bash; cloud-init directives start with #cloud-config — both are valid user data formats"], when: "Q asks about EC2 user data behaviour → root privileges + runs only on first launch (by default)", pricing: "Exam tip: two key facts that appear in select-two questions: (1) executes with root privileges, (2) runs only during first boot cycle by default." },
      { name: "EventBridge + Fargate Cron", full: "EventBridge Scheduler + ECS Fargate: Containerized Cron Jobs", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "⏰🚀", tagline: "Replace Linux cron with EventBridge Scheduler → ECS Fargate — serverless, scalable, no infra", analogy: "Your cron job is a script that runs on a schedule. Instead of a server that wakes up at midnight and runs it, EventBridge is an alarm clock that fires on schedule and Fargate is a disposable runner that appears, executes the container, and disappears.", keyPoints: ["EventBridge Scheduler: fully managed scheduler that triggers targets on cron or rate expressions — no EC2, no cron daemon to manage", "ECS Fargate task as target: EventBridge Scheduler directly invokes a Fargate task (ECS RunTask) — container starts, runs the script, exits; Fargate manages compute entirely", "Up to 30-minute execution: Fargate tasks have no time limit (unlike Lambda's 15-min max) — suitable for scripts running up to 30 minutes or longer", "Minimal refactoring: containerize the existing scripts (Dockerfile + CMD) — no code changes needed, just package into a container image", "Event-driven future extension: ECS tasks can also be triggered by EventBridge rules (not just scheduler) — supports event-based invocation with no architecture change", "vs Lambda: Lambda max 15 min timeout rules it out for 30-min scripts; Fargate has no timeout limit", "vs EC2 with cron: requires instance management, patching, HA — higher ops overhead than fully serverless", "Infrastructure management: zero — no EC2, no clusters to size; Fargate manages compute capacity automatically"], when: "Q describes cron scripts + up to 30 min runtime + containerized + serverless + minimal infra + event-driven future needs → EventBridge Scheduler + ECS Fargate", pricing: "Exam tip: Lambda timeout (15 min) disqualifies Lambda for scripts > 15 min. Fargate + EventBridge Scheduler is the canonical answer for containerized scheduled jobs with variable/long runtimes." },
      { name: "EFS Max I/O Mode", full: "EFS Performance Mode: Max I/O for Big Data Workloads", color: "#C0392B", lightBg: "#FDEDEC", icon: "📁⚡", tagline: "Max I/O = highest aggregate IOPS for thousands of parallel clients — at the cost of slightly higher latency", analogy: "General Purpose is a fast specialist who handles each request with minimal delay. Max I/O is a massive warehouse loading dock — it can process thousands of trucks simultaneously, but each individual truck waits a little longer to get through.", keyPoints: ["EFS has two performance modes set at file system creation (cannot be changed later):", "General Purpose (default): lowest per-operation latency (~1ms); best for latency-sensitive apps; soft limit of ~7,000 ops/sec", "Max I/O: designed for workloads with thousands of EC2 instances accessing the file system simultaneously; higher aggregate throughput and IOPS; slightly higher latency per operation (~a few ms)", "When to choose Max I/O: big data analytics (Hadoop, Spark), media processing pipelines, genomics/bioinformatics, ML training with large datasets, any highly parallelized workload with many concurrent clients", "When to choose General Purpose: web serving, content management, home directories, dev environments — anything that prioritises low latency over raw aggregate throughput", "Max I/O is only available with Bursting or Provisioned throughput modes — not compatible with Elastic throughput mode (use General Purpose + Elastic for most new workloads)", "Exam signal words: 'big data', 'analytics', 'thousands of instances', 'improve performance of parallel workloads' → Max I/O"], when: "Q asks which EFS performance mode suits big data / analytics / high-parallelism → Max I/O. Q asks about general web/app workloads needing low latency → General Purpose", pricing: "Exam tip: Max I/O and General Purpose cost the same per GB — the choice is purely about workload characteristics, not cost." },
      { name: "EC2 Hibernate", full: "EC2 Instance Hibernate: Preserve RAM to Disk on Stop", color: "#7D6608", lightBg: "#FEFCE8", icon: "🖥️💤", tagline: "Hibernate saves RAM to EBS on stop — resumes exactly where it left off, skipping bootstrap", analogy: "Closing a laptop lid (hibernate) vs turning it off (stop). Lid close saves everything in memory to disk — when you open it again, all your windows and applications are exactly as you left them. Full shutdown clears memory — everything has to reload from scratch.", keyPoints: ["EC2 Hibernate: when an instance is stopped, RAM contents are written to the root EBS volume (encrypted); on start, RAM is restored and the OS resumes instantly", "Eliminates bootstrap time: applications that need minutes of initialization (loading caches, starting services, warming JVM) pick up exactly where they left off", "Requirements: instance must have an encrypted root EBS volume; RAM must be < 150 GB; supported instance families (not all types); not supported for bare metal", "Instance storage: RAM dump adds to the root EBS volume size requirement — must have enough free space", "Hibernation time limit: an instance cannot be hibernated for more than 60 days", "vs Stop: Stop clears RAM and the instance must re-run all startup scripts on next launch. Hibernate preserves RAM state — no re-initialization", "vs Stop + Start billing: hibernate still incurs EBS storage charges during hibernation; EC2 compute charge stops (same as regular Stop)"], when: "Q describes long application boot/initialization time + need to stop/start the instance → EC2 Hibernate", pricing: "Exam tip: key phrases are 'bootstrap time', 'initialization', 'application takes minutes to start', 'stop and resume' → Hibernate. Not the same as Stop — Hibernate preserves in-memory state." },
      { name: "AS Default Termination Policy", full: "Auto Scaling Default Termination Policy: Which Instance Gets Terminated", color: "#E8593C", lightBg: "#FEF0ED", icon: "📈❌", tagline: "AWS picks the AZ with most instances, then applies a fixed priority order to choose which one", analogy: "A staffing agency reducing headcount: first pick the office with the most staff (balance AZs), then within that office fire whoever has the oldest contract type, then whoever has the oldest contract, then whoever's shift ends soonest.", keyPoints: ["Step 1 — Balance AZs: terminate from the AZ with the most instances (or AZ where on-demand/spot balance is off)", "Step 2 — Oldest launch template or configuration: within that AZ, terminate the instance using the oldest launch template or configuration first", "Launch configuration vs launch template: launch configurations are the legacy type; launch templates are the modern replacement. If a mix exists, launch configuration is treated as 'older' than any launch template", "Step 3 — Oldest launch configuration: if there are multiple with old configs, pick the one with the oldest launch configuration", "Step 4 — Closest to next billing hour: if still tied, terminate the instance closest to completing the next full billing hour (to maximize use of already-paid time)", "Applying to the scenario: AZ us-east-1a has most instances → Step 2: oldest launch template = Instance A vs oldest launch configuration = Instance B. Launch configurations are legacy/older type than launch templates → Instance B is terminated first", "Custom termination policies: you can override with policies like OldestInstance, NewestInstance, AllocationStrategy, or a Lambda-based custom policy"], when: "Q asks which EC2 instance Auto Scaling terminates during scale-in → apply the 4-step default policy: most-instances AZ → oldest launch config/template → closest to billing hour", pricing: "Exam tip: the key distinction is launch configuration (legacy) vs launch template (modern) — the default policy treats launch configurations as older than launch templates. Instance B (oldest launch config) is terminated before Instance A (oldest launch template)." },
      { name: "DataSync NFS → EFS via Direct Connect", full: "DataSync: Replicate On-Prem NFS to EFS Over Direct Connect", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔄📁", tagline: "DataSync agent on-prem → Direct Connect private VIF → PrivateLink VPC endpoint → EFS", analogy: "DataSync is a scheduled courier that picks up new files from your on-prem filing cabinet (NFS), drives down a private motorway (Direct Connect), and drops them in the cloud filing cabinet (EFS) — fully managed, no scripting, no servers.", keyPoints: ["DataSync agent: installed on-prem as a VM — detects changed/new files in the NFS mount and sends them to AWS", "Direct Connect private VIF: dedicated private connection — traffic never traverses the public internet; required for compliance and bandwidth predictability", "PrivateLink interface VPC endpoint for EFS: traffic enters the VPC privately from the Direct Connect connection without needing an Internet Gateway or NAT", "Scheduled task: DataSync runs on a defined schedule (hourly, daily) — only transfers new/changed files (incremental), not the full dataset every run", "Why not rsync/cron? Manual scripts require maintenance and error handling; DataSync is fully managed with retry, checksums, and CloudWatch monitoring built in", "Why not Storage Gateway File Gateway? File Gateway provides an ongoing NFS mount backed by S3 — DataSync is for replicating data to EFS specifically", "Transfer path: on-prem NFS → DataSync agent → Direct Connect (private VIF) → PrivateLink EFS endpoint → EFS mount target → EFS"], when: "Q describes on-prem NFS files needing regular replication to EFS over Direct Connect with minimal ops overhead → DataSync agent + private VIF + PrivateLink VPC endpoint for EFS", pricing: "Exam tip: DataSync is the managed answer for scheduled NFS→EFS (or S3) replication. Private VIF + PrivateLink keeps traffic off the public internet — required whenever Direct Connect and 'private' appear together." },
      { name: "RDS Storage Auto-Scaling", full: "RDS Storage Auto-Scaling: Zero-Effort Capacity Management", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🗄️📈", tagline: "Enable once — RDS expands storage automatically when it runs low", analogy: "A smart filing cabinet that orders more drawers automatically when it gets full — you never have to think about storage capacity again.", keyPoints: ["RDS Storage Auto Scaling monitors free storage and automatically increases allocated storage when it falls below a threshold", "Triggers when: free storage < 10% of allocated AND low-storage condition lasts ≥ 5 minutes AND 6 hours have passed since last modification", "You set a Maximum Storage Threshold — RDS will never exceed it; acts as a safety cap on cost", "Supported engines: MySQL, PostgreSQL, MariaDB, Oracle, SQL Server (all RDS engines)", "Zero application changes required — no connection interruption, no instance restart; scaling happens transparently", "Minimum development effort: enable with one checkbox in RDS console or one API call — no custom scripts, no Lambda, no CloudWatch alarms needed", "Wrong answers: provisioning a larger instance (fixes compute, not storage); manual snapshot + restore (high ops overhead); read replicas (read scaling, not storage capacity)"], when: "Q asks for minimum-effort solution to RDS running out of storage or unpredictable storage growth → enable RDS Storage Auto Scaling with a max threshold", pricing: "Exam tip: the phrase 'minimum development effort' or 'minimum operational overhead' + 'rapidly increasing database storage' = RDS Storage Auto Scaling. You pay standard RDS storage rates for the auto-added storage." },
      { name: "🌍 CloudFront vs Global Accelerator", isHeader: true, color: "#5D6D7E" },
      { name: "→ Use CloudFront", full: "CloudFront Scenario Triggers", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌩✅", tagline: "CloudFront = Layer 7, HTTP-aware, caches at edge, terminates SSL", analogy: "CloudFront is the smart post office that opens parcels, stamps them, and caches popular ones near customers. If the task involves reading, transforming, or securing HTTP content — CloudFront.", keyPoints: ["Static website / images / videos on S3 → CloudFront caches at 200+ edge locations, reduces origin load", "Speed up downloads for global users → content cached near the user, not fetched from origin each time", "Custom domain + HTTPS → CloudFront terminates SSL at the edge (ACM cert must be in us-east-1)", "Restrict S3 to CloudFront only → OAC/OAI integration; S3 bucket stays private", "Block malicious web traffic → WAF attaches directly to CloudFront distributions", "Run code at the edge (auth, URL rewrites, A/B testing, header injection) → Lambda@Edge or CloudFront Functions", "Reduce origin load from repeated requests → cache hit ratio means origin barely gets touched", "Geo-restriction / block specific countries → built-in CloudFront geo restriction (whitelist or blacklist)", "S3 static site + minimal changes needed → DNS config only, zero app redesign", "Speed up S3 uploads AND downloads (combined) → CloudFront (downloads) + S3 Transfer Acceleration (uploads)"], when: "Any of: caching, custom domain HTTPS, OAC, WAF, Lambda@Edge, geo-restriction, static S3 site, reduce origin load → CloudFront", pricing: "Deciding question: does the solution need to understand HTTP — cache it, inspect it, terminate SSL, sign requests? YES → CloudFront." },
      { name: "→ Use Global Accelerator", full: "Global Accelerator Scenario Triggers", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🏎️✅", tagline: "Global Accelerator = Layer 3/4, protocol-agnostic, routes through AWS backbone, 2 static IPs", analogy: "Global Accelerator is the private motorway on-ramp — it doesn't know or care what's in your vehicle (TCP/UDP packet). It just gets you onto the fast AWS backbone immediately and routes you to the right destination.", keyPoints: ["Gaming, VoIP, video calls, real-time multiplayer → TCP/UDP protocols that CloudFront can't handle", "Static IPs required for firewall whitelisting → 2 fixed anycast IPs that work globally (CloudFront has no static IPs)", "Non-HTTP protocol (anything TCP/UDP) → Layer 3/4, protocol-agnostic, no HTTP awareness needed", "Multi-region failover in under 30 seconds → health checks + instant rerouting to next healthy region", "Consistent low latency for dynamic content that cannot be cached → routes over AWS backbone without HTTP termination overhead", "IoT device communication, custom TCP protocols → Global Accelerator handles any TCP/UDP workload", "Client can't change DNS easily → 2 static anycast IPs means firewall rules never change even if backend regions change"], when: "Any of: gaming/VoIP/IoT, static IPs, non-HTTP protocol, multi-region TCP failover, real-time dynamic apps → Global Accelerator", pricing: "Deciding question: does the solution need to understand HTTP? NO, just route fast → Global Accelerator. $0.025/hr per accelerator + $0.015/GB." },
      { name: "CF vs GA: Common Traps", full: "CloudFront vs Global Accelerator: Wrong Answer Patterns", color: "#C0392B", lightBg: "#FDEDEC", icon: "⚠️🌍", tagline: "Global Accelerator is often the tempting wrong answer for S3 and upload scenarios", keyPoints: ["❌ 'Speed up S3 uploads from global users' → NOT Global Accelerator. Use S3 Transfer Acceleration. GA has no native S3 integration and requires app redesign", "❌ 'Speed up S3 downloads + uploads with minimal changes' → NOT Global Accelerator. Use CloudFront (downloads) + S3TA (uploads). Both have native S3 integration with no redesign", "❌ 'Custom domain + HTTPS for global site' → NOT Global Accelerator. It's Layer 3/4 — can't terminate SSL or handle custom domains. Use CloudFront", "❌ 'Block countries from accessing content' → NOT Global Accelerator. Use CloudFront geo restriction", "❌ 'Protect against SQLi / XSS' → NOT Global Accelerator. Use CloudFront + WAF",
                "❌ 'CloudFront with OAC for signed S3 requests' → NOT Global Accelerator. GA is Layer 3/4 — it cannot terminate connections, inspect HTTP, or sign requests. Only CloudFront can apply OAC to authenticate S3 origin requests", "✅ The only time GA beats CloudFront: non-HTTP protocol, static IPs needed, or gaming/VoIP/real-time TCP apps", "Memory hook: if S3 is in the question and the options include Global Accelerator → Global Accelerator is almost certainly wrong"], when: "Elimination strategy: see Global Accelerator as an option for an S3, HTTP, caching, or custom domain question → eliminate it. GA is correct only for non-HTTP / static IP / gaming / real-time TCP scenarios.", pricing: "Exam tip: the single deciding question — does the edge need to understand HTTP? YES → CloudFront. NO → Global Accelerator. For S3 specifically → always CloudFront + S3TA, never Global Accelerator." },
      { name: "💰 Design Cost-Optimized Architectures", isHeader: true, color: "#1A7A4A" },
      { name: "Instance Types", full: "EC2 Purchasing: When to Use Which", color: "#7D6608", lightBg: "#FEFCE8", icon: "🖥️💳", tagline: "Match purchase model to workload predictability", analogy: "On-Demand = taxi (expensive but available now). Reserved = annual bus pass (cheap if you commit). Spot = standby flight (dirt cheap, may get bumped). Savings Plans = a gym membership (flexible commitment).", keyPoints: ["On-Demand: unpredictable, short-term, or spiky — no commitment, full price", "Reserved (1 or 3 yr): steady-state, predictable — up to 72% off; Standard RI or Convertible RI", "Spot Instances: fault-tolerant batch, big data, CI jobs — up to 90% off; 2-min interruption notice", "Savings Plans: Compute (most flexible, covers Lambda + Fargate) or EC2 (specific family) — up to 72%", "Dedicated Hosts: per-socket/per-core licensing (Windows Server, SQL Server), compliance requirements"], when: "Q says 'minimum cost for steady 24/7 workload' → Reserved. 'Cheapest for batch that can restart' → Spot. 'Flexible discount' → Savings Plans", pricing: "Exam trap: Spot is NOT for anything that can't handle interruption. Never use Spot for a database or web tier." },
      { name: "Cross-Account VPC Sharing via RAM", full: "Cheapest Cross-Account Private Connectivity: Shared VPC via RAM", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌐💰", tagline: "Share subnets from one VPC to other AWS accounts — EC2s communicate privately with no peering or Transit Gateway cost", keyPoints: ["AWS Resource Access Manager (RAM): share a VPC's subnets with other accounts in the same AWS Organization — those accounts can launch EC2 instances directly into the shared subnets", "All instances in the shared subnets communicate using private IPs within the same VPC — no VPC peering, no Transit Gateway, no data transfer charges between accounts", "Cost advantage: VPC peering has data transfer charges ($0.01/GB for inter-AZ); Transit Gateway charges per attachment ($0.05/hr) and per GB ($0.02/GB) — shared VPC has neither", "The subnet owner account controls routing and security; participant accounts control their own instances and security groups within those subnets", "vs VPC Peering: peering connects two separate VPCs and incurs data transfer fees; shared VPC puts all instances in the same VPC with free intra-VPC communication", "vs Transit Gateway: TGW is for hub-and-spoke routing across many VPCs/accounts — higher cost; overkill if the only need is private instance-to-instance communication", "Limitation: participant accounts cannot modify the shared subnets, route tables, or VPC itself — they can only launch resources into them"], when: "Q asks for cheapest way for EC2 instances across multiple accounts in same region to communicate privately → shared VPC subnets via RAM (not VPC peering, not TGW)", pricing: "Exam tip: RAM subnet sharing = zero extra networking cost. VPC peering = data transfer charges. Transit Gateway = per-attachment + per-GB charges. 'Cheapest' + 'private communication' + 'multiple accounts' → RAM." },
      { name: "VPC Endpoint vs NAT", full: "Accessing AWS Services: Free vs Costly", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔌💰", tagline: "Gateway Endpoint for S3/DynamoDB — free, no NAT needed", analogy: "A staff shortcut corridor (VPC Endpoint) that bypasses the main exit (NAT Gateway). Staff go direct; customers still use the lobby. The corridor is free; the lobby charges per visit.", keyPoints: ["Gateway Endpoints: S3 and DynamoDB only — free, no data transfer cost, add a route in route table", "Interface Endpoints (PrivateLink): all other AWS services (SQS, SNS, KMS, etc.) — charged per hour + per GB", "NAT Gateway: needed for general internet traffic from private subnet — $0.045/hr + $0.045/GB", "Best practice: use Gateway Endpoint for S3/DynamoDB; avoids NAT cost AND keeps traffic off internet", "Lambda / EC2 in private subnet calling S3 → Gateway Endpoint, not NAT"], when: "Q says 'EC2 in private subnet needs to access S3 without internet' → Gateway VPC Endpoint (free)", pricing: "Exam tip: Gateway Endpoints = zero cost. NAT Gateway to S3 = unnecessary charge. This distinction appears frequently." },
      { name: "Gateway Endpoints: S3 + DynamoDB", full: "Private VPC Access to S3 and DynamoDB: Two Separate Gateway Endpoints + Route Table Entries", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔌🪣⚡", tagline: "S3 and DynamoDB each need their own gateway endpoint — two endpoints, two route table entries, free", analogy: "Two private staff corridors in a building — one leads directly to the file room (S3), one leads directly to the records office (DynamoDB). Neither corridor touches the public street. Each door needs its own keycard reader (separate endpoint). Staff can walk to both rooms without ever going outside.", keyPoints: ["Gateway Endpoints are the only free VPC endpoint type — available exclusively for S3 and DynamoDB; all other services use Interface Endpoints (PrivateLink) which cost ~$0.01/hr per AZ", "How they work: creating a Gateway Endpoint adds a new entry to the VPC route table. Destination = the S3 or DynamoDB managed prefix list (pl-xxxxxxxx). Target = the gateway endpoint ID. Traffic matching the prefix is routed privately through the endpoint instead of the internet", "Two separate endpoints: S3 and DynamoDB are different services — you must create one endpoint for S3 and a separate endpoint for DynamoDB. Each adds its own route table entry. You cannot combine them", "Route table association: the endpoint must be associated with the route tables of the subnets that contain the EC2 instances. If your instances are in a private subnet, associate the gateway endpoint with that subnet's route table", "No NAT Gateway needed: without a gateway endpoint, private subnet EC2s need a NAT Gateway to reach S3/DynamoDB (because they're public AWS services). Gateway Endpoints eliminate that need — and the NAT cost — entirely", "Bucket/table policy enforcement: add aws:SourceVpce condition to the S3 bucket policy or DynamoDB resource policy to restrict access to only requests coming through the endpoint — prevents access from outside the VPC even with valid credentials", "Same-VPC only: Gateway Endpoints are scoped to the VPC they are created in — resources in a peered VPC or Transit Gateway-connected VPC CANNOT use another VPC's gateway endpoint. Each VPC needs its own gateway endpoint for S3/DynamoDB", "Same-region only: the gateway endpoint only routes to S3/DynamoDB in the same region as the VPC. Cross-region S3 access from a private subnet still requires a NAT Gateway or Interface Endpoint"], when: "Q describes EC2 in private subnet + needs to access S3 and/or DynamoDB + no internet + cost-effective → Gateway VPC Endpoints (free). One endpoint per service. Each adds one route table entry to the private subnet's route table.", pricing: "Exam tip: Gateway Endpoints = free, S3 and DynamoDB only, route table entry. Interface Endpoints = paid, all other AWS services, ENI in subnet. If the question asks 'how many route entries' and services are S3 + DynamoDB → answer is two (one per service)." },
      { name: "S3 vs EBS vs EFS Cost", full: "Storage Cost Comparison: S3 vs EBS vs EFS", color: "#2E86AB", lightBg: "#EBF5FB", icon: "💰📦", tagline: "EBS charges for provisioned GB — even if mostly empty", analogy: "S3 and EFS are like a pay-as-you-eat buffet — you pay for what you put on your plate. EBS is like renting a fixed-size container — you pay for the whole 100-litre box even if you only fill 1 litre.", keyPoints: ["S3 Standard: charged per GB actually stored — 1 GB file = ~$0.023/month", "EFS Standard: charged per GB actually used — 1 GB file = ~$0.30/month", "EBS (gp2, 100 GB provisioned): charged per GB provisioned regardless of usage — 100 GB × $0.10 = ~$10/month even though only 1 GB of data is stored", "Cost order for this scenario: S3 (~$0.023) < EFS (~$0.30) < EBS (~$10)", "Key insight: EBS cost is dominated by provisioned capacity, not actual data. If you provision 100 GB but use 1 GB, you still pay for 100 GB", "EFS and S3 both scale to actual usage — no over-provisioning cost"], when: "Q compares S3 vs EBS vs EFS cost for the same file size — always check if EBS is over-provisioned; provisioned GB drives the EBS bill", pricing: "Exam tip: S3 < EFS < EBS is the typical order when EBS is provisioned at multiples of actual data size. If EBS were provisioned at exactly 1 GB it would cost ~$0.10 — but you can't provision less than 1 GB and real workloads always provision more than they use." },
      { name: "Storage Tiering by Use Case", full: "Storage Selection: Process vs Store vs Archive", color: "#7D6608", lightBg: "#FEFCE8", icon: "💾📦🏔️", tagline: "Match storage to access pattern — Instance Store → S3 → Glacier", analogy: "A kitchen (Instance Store — fast, right there, gone when you leave), a pantry (S3 — durable, always accessible, scales forever), and a off-site warehouse (Glacier — very cheap, but takes time to retrieve).", keyPoints: ["Instance Store: physically attached to the EC2 host — maximum possible I/O, lowest latency, no network overhead. Use for processing-only workloads (large video files, HPC scratch, temp data). Data is lost when the instance stops — ephemeral by design", "Amazon S3: 11 nines (99.999999999%) durability, no practical size limit, pay per GB stored. Use for durable object storage at any scale — media libraries, backups, data lakes. Standard tier for frequently accessed data", "Amazon S3 Glacier: archival tier at ~$0.004/GB/month vs S3 Standard ~$0.023/GB. Retrieval takes minutes to hours. Use for data you keep but rarely access — legacy archives, compliance retention, cold backups", "Why not EBS for max performance? EBS is network-attached — fast, but Instance Store is physically local and faster for raw throughput. 'Maximum possible I/O' = Instance Store", "Why not EFS for durable storage? EFS is a shared NFS file system — more expensive than S3, not designed for large-scale object storage at 450 TB", "Why not S3-IA for archival? S3-IA is for infrequently accessed data you still need quickly. Glacier is for true cold archives where retrieval delay is acceptable and cost savings are the priority"], when: "Q gives three tiers of storage needs: high-performance processing + large durable storage + cheap archival → Instance Store (process) + S3 (store) + Glacier (archive)", pricing: "Exam tip: the three-tier pattern is a classic SAA question. Memory hook: Process → Instance Store (fastest, ephemeral). Store → S3 (durable, scalable). Archive → Glacier (cheapest, slow retrieval). Match the access pattern, not just the size." },
      { name: "EC2 + Aurora HA Modernisation", full: "Rearchitecting for HA: ASG+ALB for App Layer + Aurora for DB Layer", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🏗️🗄️", tagline: "Aurora replaces the database, not the application — both layers need separate HA fixes", analogy: "A restaurant with one chef doing everything in one room. Modernising means: (1) hire more chefs and open multiple kitchens across the building so one kitchen fire doesn't shut everything down (ASG+ALB), AND (2) upgrade from a notepad order system to a proper database with backups and multiple reading stations (Aurora + read replicas). The chefs still exist — they just have a better kitchen.", keyPoints: ["The app has two independent layers: web/app layer (EC2 running application code) and database layer (MySQL storing data). A single EC2 hosting both is a single point of failure at both layers", "ASG + ALB fix the web/app layer: Auto Scaling Group launches EC2s across multiple AZs — survive AZ failure; ALB distributes traffic across healthy instances — horizontal scalability. Route 53 points to the ALB", "Aurora MySQL fix the database layer: Aurora gives automated Multi-AZ replication and failover. Read replicas offload read traffic via the reader endpoint — directly addresses the 'improve read performance' requirement", "Aurora does NOT replace EC2: Aurora is a managed database service — it runs SQL queries, not application code. The EC2 instances still run the web/app logic (PHP, Node, Python, etc.) and connect to Aurora as their database", "After modernisation: Users → Route 53 → ALB → EC2 fleet (ASG, multi-AZ) → Aurora primary (writes) + Aurora replicas (reads)", "Why not Lambda instead of EC2? Lambda would also work for the app layer but the question asks about modernising an existing EC2-based app for HA — not going fully serverless. The two answers fix HA at each layer within the existing EC2 paradigm"], when: "Q describes a monolithic single EC2 hosting both app and DB, asks for HA + scalability + read performance → ASG+ALB (app layer HA) + Aurora with read replicas (DB layer HA + read scaling). These fix different layers and must both be selected.", pricing: "Exam tip: always decompose the architecture into layers. HA for the app layer (EC2/ASG/ALB) and HA for the DB layer (Aurora Multi-AZ + replicas) are independent problems requiring independent solutions. Aurora does not make EC2 redundant." },
      { name: "Aurora Read/Write Separation", full: "Aurora: Offload Reads to Replica to Free Primary for Writes", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🗄️↔️", tagline: "Single Aurora instance overwhelmed? Add a replica and split reads from writes", analogy: "One cashier handling both payments AND returns at peak hour — the queue backs up and some orders get lost. The fix: open a second till dedicated to returns (reads), so the first cashier only handles payments (writes) and nothing gets dropped.", keyPoints: ["Root cause: a single Aurora instance handling both reads AND writes means read traffic competes with writes for CPU and memory — at peak load, writes (Lambda order inserts) get dropped", "Fix part 1 — Add an Aurora replica in another AZ: the replica stays in sync with the primary automatically; it also serves as a failover target, improving availability", "Fix part 2 — Route all reads to the reader endpoint: Aurora's reader endpoint load-balances read-only connections across all available replicas; the primary then handles writes only", "Result: primary CPU/memory pressure drops → Lambda writes stop being missed. Replica absorbs all read traffic. AZ failure triggers automatic failover to the replica", "Aurora endpoints to know: Writer endpoint = always the primary (use for INSERT/UPDATE/DELETE). Reader endpoint = load-balances across replicas (use for SELECT). Without a replica, the reader endpoint still points to the primary — no benefit", "Why the two answers must be combined: replica alone = reads still hit primary. Reader endpoint alone = without a replica it still routes to primary. Both together = true read/write separation", "Wrong answers: increasing Lambda concurrency makes it worse (more writes to an overwhelmed DB). RDS Proxy fixes connection exhaustion, not CPU/memory read-write contention. Aurora Multi-Master is deprecated and overkill."], when: "Q describes Lambda writes being dropped/missed + high CPU/memory on Aurora + want to improve availability → Add Aurora replica in another AZ + route reads to reader endpoint (select two)", pricing: "Exam tip: 'writes being missed under load' on a single Aurora instance = reads are stealing CPU from writes. The two-part fix is always: (1) add a replica for HA + read capacity, (2) use the reader endpoint to actually offload those reads." },
      { name: "NLB Public Subnet + EC2 ASG Private Subnet", full: "Secure Public Web App: NLB (TLS Termination) in Public Subnet + EC2 ASG in Private Subnet", color: "#2E86AB", lightBg: "#EBF5FB", icon: "⚖️🔒🖥️", tagline: "NLB terminates TLS in public subnet — EC2 instances stay private and never handle encryption", analogy: "A hotel concierge desk (NLB) in the public lobby that handles all guest ID checks (TLS handshake) before escorting guests to the private floors (EC2 in private subnet). Guests never interact directly with the private floors — only the concierge does. The concierge absorbs all the security formalities so the rooms don't have to.", keyPoints: ["Architecture: Internet → NLB (public subnet, TLS listener) → EC2 ASG (private subnet, plain TCP). EC2 instances have no public IPs and no direct internet route", "TLS termination at NLB: configure the NLB listener as TLS (not TCP). NLB uses an ACM certificate to perform the TLS handshake. Traffic forwarded to EC2 targets as plain TCP — EC2 never sees or processes encryption, satisfying the 'offload TLS' requirement", "Most secure: EC2 in a private subnet has no public IP and no internet gateway route — even if a security group is misconfigured, there is no internet path to the instance. The NLB is the single controlled entry point (defence in depth)", "NLB vs ALB for TLS offload: both can terminate TLS. NLB (Layer 4) is chosen when static Elastic IPs per AZ are needed (for client firewall allowlisting), when preserving client source IP natively is required, or when handling non-HTTP TCP protocols. ALB (Layer 7) is preferred when HTTP routing features are needed (path/host-based routing, HTTP headers)", "Static IPs: NLB provides one static Elastic IP per AZ — clients and firewalls can allowlist a fixed IP. ALB does not support static IPs", "Client IP preservation: NLB passes the original client IP to EC2 targets natively. ALB requires X-Forwarded-For header or Proxy Protocol for the same", "Wrong answers eliminated: (1) EC2 in public subnet = less secure, directly internet-reachable. (2) NLB in private subnet = cannot receive internet traffic. (3) EC2 handles TLS = violates offload requirement. (4) No load balancer = no HA and no TLS offloading"], when: "Q describes public web app + TLS offload from EC2 + most secure + static IPs or Layer 4 → NLB with TLS listener in public subnet + EC2 ASG in private subnet. If HTTP routing/WAF integration needed instead → ALB with HTTPS listener + same private subnet pattern.", pricing: "Exam tip: NLB supports TLS termination via TLS listener (not just TCP passthrough) — this surprises many people. The private subnet placement is the 'most secure' element. NLB = static IPs + Layer 4 + client IP preservation. ALB = Layer 7 + HTTP routing + WAF integration." },
      { name: "File Gateway Migration", full: "File Gateway: On-Prem NFS → S3 with Auto-Tiering", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🔌📁", tagline: "NFS compatibility + S3 scalability + automatic cost tiering — no app changes", analogy: "Swapping out a filling filing cabinet for one with a magic back panel — everything goes in through the same NFS drawer your apps already use, but files silently live in S3 and automatically migrate to cheaper shelves when nobody's touched them for a while.", keyPoints: ["Requirement: NFS compatibility → File Gateway (presents NFS share on-prem, backs to S3)", "Requirement: auto-tier rarely accessed data → S3 Lifecycle policy (Standard → IA → Glacier)", "Requirement: no app changes → File Gateway is transparent; apps keep using NFS mount as before", "Requirement: cost-effective → S3 storage is far cheaper than on-prem NAS at scale; Lifecycle policies cut costs further", "Wrong answers: DataSync (online transfer/migration tool, not an ongoing NFS mount); EFS (cloud-native NFS, but requires VPN/Direct Connect and doesn't auto-tier to S3 classes); FSx (Windows/Lustre, not general NFS-to-S3)"], when: "Q describes on-prem NFS workload + scalability issues + cloud migration + cost tiering → File Gateway + S3 Lifecycle", pricing: "Exam tip: the phrase 'NFS-compatible' + 'automated tiering to lower-cost storage' is the exact fingerprint for File Gateway + S3 Lifecycle. DataSync is for one-time or scheduled sync, not an ongoing protocol-compatible mount." },
      { name: "Snowball → Glacier Deep Archive", full: "Snowball to Long-Term Archive: Target S3 + Same-Day Lifecycle to Glacier Deep Archive", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "❄️🏔️", tagline: "Move on-prem backups to Glacier Deep Archive via Snowball — transition on day 0", analogy: "Ship a hard drive to AWS via courier (Snowball), land it in S3, then immediately move everything into the deepest freezer (Deep Archive) the same day so you're not paying Standard rates for even one day.", keyPoints: ["Snowball ingests data into an S3 bucket — it cannot write directly to Glacier", "Once in S3, a lifecycle policy transitions objects to the cheapest storage class", "Glacier Deep Archive: ~$0.00099/GB/month — cheapest storage in AWS; 12-hr retrieval", "Set lifecycle transition to day 0 (same day as creation) — objects move to Deep Archive immediately after Snowball upload, avoiding Standard storage charges entirely", "Why not day 30? S3 Standard is ~$0.023/GB/month — for large datasets you'd pay unnecessarily for 30 days before the transition fires", "Minimum duration for Deep Archive: 180 days — factor this into retrieval cost calculations"], when: "Q asks cheapest way to move on-prem backups to AWS long-term storage via Snowball → S3 bucket + lifecycle policy to Glacier Deep Archive on day 0", pricing: "Exam tip: Snowball always lands in S3 first. 'Same day' lifecycle transition (day 0) is the key cost optimization — do not wait 30 days if the data is archival from day one." },
      { name: "EBS vs S3 for Multi-AZ Document Store", full: "Multi-AZ EC2 Document Storage: EBS Root + S3 Document Store", color: "#2E86AB", lightBg: "#EBF5FB", icon: "💾🪣🌍", tagline: "EBS is AZ-locked — use S3 for shared document storage across multiple AZs", analogy: "EBS is a USB drive plugged into one specific computer — another computer in a different room can't access it. S3 is a shared network file server in the cloud — every computer in every room accesses the same files simultaneously. Keep the USB drive for the OS, put the shared documents on the network server.", keyPoints: ["EBS is AZ-scoped: a volume lives in one AZ and can only attach to instances in that AZ. EC2 instances in AZ-B cannot read from an EBS volume in AZ-A — documents uploaded via AZ-A instances are invisible to AZ-B instances", "S3 is regional: a single S3 bucket is accessible from ALL EC2 instances across ALL AZs in the region simultaneously — no replication to manage, no sync lag, no AZ boundary", "S3 durability: 11 nines (99.999999999%) durability, data automatically replicated across ≥3 AZs within the region — inherently more HA than a single-AZ EBS volume", "S3 Standard = immediate retrieval: millisecond latency for GET requests — satisfies 'documents returned immediately' requirement. No Glacier or IA tier needed here", "Keep EBS as root volume: EC2 always needs a block storage root volume for the OS. This is correct and unchanged — EBS is the right tool for root volumes, just not for shared application data", "Why not EFS: EFS is also multi-AZ and shared, but it's a filesystem (POSIX, NFS) — suited for apps needing directory structure, file locking, or POSIX permissions. For document objects retrieved by key/URL, S3 is simpler and cheaper", "The pattern: EBS = OS root volume. S3 = shared object/document store. EFS = shared POSIX filesystem. RDS = relational data. These four cover almost all EC2 storage architecture questions"], when: "Q describes EC2 in multiple AZs + shared documents/objects + highly available + immediately accessible → S3 as document store (not EBS). EBS stays as root volume only. If the question says 'filesystem' or 'POSIX' or 'file locking' → EFS instead of S3.", pricing: "Exam trap: EBS seems like the obvious storage choice since the instances already use it — but EBS is AZ-locked. The moment you see 'multiple AZs' + 'shared data' + 'all instances access same content', EBS is wrong for the shared data. S3 or EFS are the answers." },
      { name: "EC2 User Data: Run Scripts Once on Launch", full: "EC2 User Data: Automatic One-Time Bootstrap Script on First Boot", color: "#E8593C", lightBg: "#FEF0ED", icon: "🖥️📜", tagline: "User data runs automatically once on first boot — no extra services, no scheduling needed", analogy: "Moving into a new house and leaving a note for yourself on the front door: 'when you walk in for the first time, do these things — install the WiFi, unpack the kitchen, set up the TV'. It runs the checklist once when you first arrive and never again.", keyPoints: ["User data is a script attached to an EC2 instance at launch — cloud-init executes it automatically on first boot as root, then never runs it again", "Runs exactly once by default: if the instance is stopped and restarted, user data does NOT re-run. This makes it perfect for one-time bootstrap tasks", "Minimal effort: configure directly in the EC2 launch wizard, Launch Template, or CloudFormation — no extra services, agents, or scheduling infrastructure needed", "Runs as root: no sudo needed inside the script. Full system access from the first line", "Size limit: 16 KB maximum. For larger scripts, put the script in S3 and use user data to download and execute it: curl -o /tmp/setup.sh s3://bucket/setup.sh && bash /tmp/setup.sh", "Common tasks: install packages (yum/apt), configure application settings, pull code from S3, register with Config Management, set environment variables, start services", "Accessible via instance metadata: the user data script can be read at http://169.254.169.254/latest/user-data from inside the instance — useful for debugging", "Why not SSM Run Command: Run Command is on-demand (triggered by a human or automation), not automatic on launch — requires additional setup and doesn't satisfy 'only once on launch' without extra orchestration", "Why not cron: cron runs on a schedule (repeatedly) — contradicts the 'only once' requirement"], when: "Q says 'run custom scripts once during launch' OR 'bootstrap software on new EC2 instances' OR 'configure instances automatically at launch with minimal effort' → EC2 User Data. Not SSM Run Command (on-demand), not cron (recurring).", pricing: "Exam tip: user data = free, automatic, one-time, root access, 16 KB limit. The phrase 'minimal effort' + 'only once at launch' is the user data fingerprint. If scripts need to run repeatedly on schedule → use cron or SSM. If scripts need to run on-demand → SSM Run Command." },
      { name: "CFN Security Group Ingress", full: "CloudFormation SecurityGroupIngress: Reading Inbound Rules", color: "#7D6608", lightBg: "#FEFCE8", icon: "🧱📋", tagline: "SecurityGroupIngress = inbound rules only; each entry is one allow rule", analogy: "A bouncer's whitelist — each entry says 'let these people in through this door'. If you're not on the list, you're blocked by default.", keyPoints: ["SecurityGroupIngress defines INBOUND rules — traffic coming INTO the instance. Outbound rules use SecurityGroupEgress", "Each list entry is one independent allow rule: protocol + port range + source CIDR", "IpProtocol: tcp/udp/icmp or -1 (all traffic). FromPort/ToPort define the port range — for a single port, set both to the same value", "CidrIp: 0.0.0.0/0 = allow from any IPv4 address (open to the internet). /32 = exactly one IP address", "Example rule 1: IpProtocol tcp, port 80, CidrIp 0.0.0.0/0 → allow HTTP from anywhere", "Example rule 2: IpProtocol tcp, port 22, CidrIp 192.168.1.1/32 → allow SSH from one specific IP only", "Security Groups are stateful — if inbound is allowed, the response is automatically allowed outbound; no matching egress rule needed"], when: "Q shows a CloudFormation SecurityGroupIngress block and asks what it does (select three) → (1) configures inbound rules, (2) allows HTTP from any IP, (3) allows SSH from one specific IP", pricing: "Exam tip: always check whether the question says Ingress (inbound) or Egress (outbound). /32 = single host. 0.0.0.0/0 = open to all. Security Groups are stateful — no separate egress rule needed for responses." },
      { name: "ELB All Instances Unhealthy", full: "ALB: All Targets Unhealthy but EC2 Accessible Directly", color: "#C0392B", lightBg: "#FDEDEC", icon: "⚖️❌", tagline: "Direct IP access works → the EC2 is fine; the problem is between ALB and EC2", analogy: "The fire alarm (ALB health check) keeps going off even though you can walk into the building fine through the side door (direct IP). The alarm is either checking the wrong room, or the front door is locked to the alarm system.", keyPoints: ["Key diagnostic: if EC2 is accessible directly by IP but ALB marks it unhealthy, the EC2 itself is working — the issue is in how the ALB reaches it", "Reason 1 — Health check path misconfigured: ALB sends a probe to a specific URL path (e.g. /health). If that path doesn't exist or returns a non-2xx/3xx status code, ALB marks the target unhealthy — even though the main site works perfectly", "Reason 2 — EC2 security group blocks ALB health check: the EC2's SG must allow inbound traffic from the ALB's security group (or ALB subnet CIDRs) on the health check port. If only 0.0.0.0/0 is allowed on 80 but not from the ALB's SG, OR if port 80 is blocked from the ALB at all, health checks silently fail", "Direct IP access works because the browser connects to the EC2's public IP directly — bypassing the ALB and its security group entirely", "Other possible reasons: health check port set incorrectly in the target group; response timeout too short; health check threshold too aggressive"], when: "Q says 'ELB/ALB marks ALL instances unhealthy' + 'developer can access EC2 directly via IP' → answer is (1) health check path misconfigured AND/OR (2) EC2 security group does not allow traffic from ALB security group", pricing: "Exam trap: 'directly accessible by IP but unhealthy via ALB' always means the ALB-to-EC2 path has an issue — not the EC2 itself. The two select-two answers are always the health check path and the security group." },
      { name: "MGN Lift-and-Shift Process", full: "AWS MGN: Three-Phase Lift-and-Shift Migration Process", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🖥️🔄", tagline: "MGN migration = Install agent → Replicate + Test → Cutover", analogy: "Moving house without ever stopping work: a removal company (MGN agent) continuously copies everything from your old house to the new one while you keep living there (source VM keeps running). When the new house is verified and furnished, you move in (cutover) and hand back the keys to the old place (decommission).", keyPoints: ["Phase 1 — Install MGN Replication Agent on source VMs: the agent starts continuous block-level replication to a staging area in AWS. Source VM keeps running — zero disruption to production", "Phase 2 — Initial replication + launch test instances: once sync completes, launch test EC2 instances from the replicated data. These are isolated and non-disruptive. Validate: OS boots, applications start, network connectivity correct, performance acceptable", "Phase 3 — Launch cutover instance: once testing passes and replication is confirmed up-to-date, trigger cutover. MGN launches the final production EC2 from the latest replica. Redirect traffic. Decommission source VM.", "Why these three answers work together: they are sequential steps of the same process, not independent options. Skipping any step breaks the process", "Why MGN for lift-and-shift: no refactoring needed (block-level copy), supports any OS/application, test before commit, continuous replication keeps lag minimal for cutover", "Wrong answers: DMS (databases only, not VMs), Snowball (bulk data transfer, not live VM replication), SMS (deprecated — replaced by MGN), CloudEndure (deprecated — replaced by MGN)"], when: "Q describes lift-and-shift VM migration + no refactoring + tight deadlines + select three steps → Install MGN agent, perform replication + test instances, launch cutover after validation", pricing: "Exam tip: memorise the three MGN phases — Agent install → Test → Cutover. Questions often give all three as the correct 'select three' answers because they are the complete end-to-end process." },
      { name: "Safe Patching Behind ALB", full: "SSM Patching EC2 Behind ALB: Maintenance Window + AWSEC2-PatchLoadBalancerInstance", color: "#7D6608", lightBg: "#FEFCE8", icon: "🔧⚖️", tagline: "Patch EC2 safely by deregistering from ALB first — never patch while receiving live traffic", analogy: "Taking a plane out of service for maintenance: you don't fix the engine while passengers are boarding. You taxi it to the hangar (deregister from ALB), wait for the last passengers to disembark (connection draining), do the maintenance (patch), then return it to the gate (re-register). The Maintenance Window is the scheduled hangar slot; the Automation document is the mechanic's checklist.", keyPoints: ["Root cause of failures: SSM was patching instances while still registered in the ALB — during patching the instance restarts or becomes unresponsive, ALB keeps routing traffic → timeouts and errors hit users", "Fix part 1 — SSM Maintenance Window: defines a scheduled, auditable time window for patching (compliance requirement). Within the window, tasks run in order: (1) deregister from ALB, (2) wait for connection draining, (3) patch, (4) re-register", "Fix part 2 — AWSEC2-PatchLoadBalancerInstance document: a pre-built AWS SSM Automation document purpose-built for patching instances behind load balancers. Automates the full lifecycle: deregister → drain → patch → re-register. No custom scripts needed", "Why both: Maintenance Window = WHEN (scheduling + compliance). AWSEC2-PatchLoadBalancerInstance = HOW (safe removal mechanism). The window triggers the automation document", "Connection draining (deregistration delay): when an instance is deregistered from an ALB target group, the ALB stops sending new requests but waits for in-flight requests to complete before fully removing it. Default 300s — critical to wait for this before patching begins", "IP-based target groups: the question specifies IP-based (not instance-based) target groups — the automation document handles both; deregistration logic works the same way"], when: "Q describes EC2 behind ALB + SSM patching + failures/timeouts during patch cycle + compliance requirement to remove from traffic before patching → SSM Maintenance Window + AWSEC2-PatchLoadBalancerInstance document (select two)", pricing: "Exam tip: patching EC2 behind a load balancer always requires deregister → drain → patch → re-register. Wrong answers: stopping the instance (abrupt, no draining), ASG lifecycle hooks (for scale events not patching), Patch Manager alone without ALB coordination (this is what was already failing)." },
      { name: "Global S3 Site: Downloads + Uploads", full: "Global S3 Static Site: CloudFront for Downloads + S3 Transfer Acceleration for Uploads", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🌍🪣", tagline: "Two directions, two tools — CloudFront caches downloads, S3TA accelerates uploads", analogy: "A global photo library: CloudFront puts copies of popular photos in branches near every user (fast downloads). S3 Transfer Acceleration gives uploaders a fast motorway on-ramp to get their photos to the central vault quickly (fast uploads). Global Accelerator is a road network — it can't open parcels or cache photos, so it's the wrong tool for both.", keyPoints: ["Downloads: CloudFront caches S3 objects at 200+ edge locations — users fetch from the nearest PoP, not the S3 bucket's region. Zero application changes (just point CloudFront at the S3 bucket as origin)", "Uploads: S3 Transfer Acceleration routes uploads via the nearest CloudFront edge PoP → AWS backbone → S3 bucket. Enable per-bucket; endpoint changes from bucket.s3.amazonaws.com to bucket.s3-accelerate.amazonaws.com — minimal code change", "Minimal development effort: both solutions require no application redesign — CloudFront is a DNS/config change, S3TA is a one-toggle bucket setting + endpoint URL change", "Why NOT Global Accelerator: no native S3 integration; cannot cache downloads; cannot accelerate S3 uploads natively; requires application-level changes to route through the accelerator — violates 'minimal development effort'", "S3TA uses CloudFront edge locations under the hood for uploads — Global Accelerator would duplicate this with more complexity and cost", "Combined result: downloads served from edge (fast for all users), uploads enter AWS at nearest PoP (fast for global uploaders), no redesign needed"], when: "Q describes global users with both upload AND download latency on an S3 static site + minimal changes → CloudFront (downloads) + S3 Transfer Acceleration (uploads). Never Global Accelerator for S3.", pricing: "CloudFront: ~$0.0085/GB transfer + $0.01/10k requests. S3TA: $0.04/GB additional for accelerated transfers. Exam tip: the split is always CloudFront for downloads and S3TA for uploads on S3. Global Accelerator is the common wrong answer — it has no S3 integration." },
      { name: "CloudFront + S3 Custom Domain HTTPS", full: "CloudFront Custom Domain: ACM Must Be in us-east-1 + OAC for Secure S3 Origin", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌩🔒", tagline: "CloudFront cert must be in us-east-1 regardless of where your S3 bucket is", analogy: "CloudFront is a global head office managed from New York (us-east-1). Every branch needs its credentials stamped by HQ — it doesn't matter that your warehouse is in London (eu-west-2). The certificate must come from HQ or the branch won't recognise it.", keyPoints: ["ACM certificate region rule: CloudFront only accepts SSL/TLS certificates from ACM in us-east-1 (N. Virginia). Your S3 bucket can be in any region — the cert region is always us-east-1. This is one of the most commonly tested CloudFront facts", "OAC (Origin Access Control): locks the S3 bucket so it only accepts requests from CloudFront — no direct public access to the bucket. CloudFront signs requests to S3 using the OAC. S3 bucket policy grants CloudFront permission via OAC principal", "For uploads via CloudFront: user uploads through CloudFront → CloudFront forwards PUT request to S3 authenticated via OAC → S3 accepts. Without OAC, S3 would need to be publicly writable — a security risk", "OAC is the modern replacement for OAI (Origin Access Identity): OAC supports SSE-KMS encrypted S3 buckets and more S3 features. OAI is legacy — always use OAC for new distributions", "The two answers satisfy two distinct requirements: ACM in us-east-1 = HTTPS with custom domain. OAC = secure S3 origin only accessible via CloudFront"], when: "Q asks about CloudFront + custom domain + HTTPS → ACM cert must be in us-east-1 (not the S3 bucket's region). Q asks about securing S3 so only CloudFront can access it → OAC (not making bucket public, not OAI)", pricing: "Exam trap: ACM cert in the same region as S3 (e.g. eu-west-2) is wrong — CloudFront only reads from us-east-1. Second trap: OAI vs OAC — OAC is the correct modern answer. Third trap: S3 Transfer Acceleration speeds up direct S3 uploads but doesn't add CloudFront custom domain HTTPS." },
      { name: "RDS SG Inbound Rules", full: "Security Group Inbound Rules: Connection Timeouts When App Can't Reach RDS", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🧱🗄️", tagline: "SG rules live on the resource being connected TO — always an inbound rule on the destination", analogy: "A security group rule is like a guest list on the door of a venue. The rule lives on the venue (RDS), not on the guest (app server). The bastion host is already on the list — the app servers are a different group of guests and need their own entry.", keyPoints: ["Security groups are stateful: allow inbound → response is automatically allowed outbound. You only need to configure inbound rules for the receiving resource", "The SG rule always lives on the resource being connected TO. App server → RDS: the rule goes on the RDS security group as an inbound rule", "RDS SG must have: Inbound TCP port 3306, Source = app server security group (or app server CIDR)", "Why bastion works but app servers don't: the bastion host has its own inbound rule on the RDS SG. App servers are a different source — they need a separate inbound rule. Two different sources = two different rules", "Connection timed out (not 'connection refused'): timeout means the packet never arrived or was silently dropped — classic SG block symptom. 'Connection refused' would mean the packet arrived but the service rejected it (wrong port, service down)", "Two-tier architecture: app servers in public subnet send traffic to RDS in private subnet. RDS has no public IP — only reachable via SG rules from within the VPC"], when: "Q describes app servers getting 'connection timed out' to RDS while bastion host works fine → RDS security group is missing an inbound rule allowing traffic from the app server security group on port 3306", pricing: "Exam tip: 'connection timed out' to a DB = SG or NACL blocking the traffic. Check the inbound rules on the DESTINATION resource's SG. Bastion working ≠ app servers working — they are different sources and need separate rules." },
      { name: "VPN vs Direct Connect Encryption", full: "Hybrid Connectivity: When Encryption Is Required, VPN Wins Over Direct Connect Alone", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒🌐", tagline: "Direct Connect is private but NOT encrypted — VPN (IPsec) provides network-layer encryption", analogy: "Direct Connect is a private road between your office and AWS — no strangers on the road, but the car windows are open. VPN is a private armoured car — the road may be public internet, but nobody can see inside. If the requirement is 'encrypted at the network layer', you need the armoured car.", keyPoints: ["Site-to-Site VPN uses IPsec tunnels — encrypts traffic at the network layer (Layer 3) between on-premises and AWS. Traffic travels encrypted over the public internet", "Session-layer encryption (HTTPS/TLS) can be layered ON TOP of VPN — satisfies 'encryption at both network AND session layers' with one solution", "Security groups + NACLs can be applied alongside VPN for granular traffic control — SGs on EC2/RDS instances, NACLs on subnets", "Direct Connect does NOT encrypt traffic by default — it provides a private connection (no public internet) but data is unencrypted at the network layer. Physical line tap = readable traffic", "To encrypt Direct Connect: (1) MACsec — Layer 2 encryption available on dedicated DX connections (10G/100G); (2) VPN over Direct Connect — run IPsec VPN tunnel over the DX connection for private + encrypted link", "The question fingerprint: 'encrypted at both network and session layers' + 'granular security controls' + 'cost-effective' → Site-to-Site VPN + SGs + NACLs. Direct Connect alone fails the encryption requirement"], when: "Q requires encryption at the network layer for hybrid on-prem ↔ AWS connectivity → Site-to-Site VPN (IPsec). Direct Connect alone is wrong. 'Both network and session layer encryption' → VPN (network) + HTTPS/TLS on the application (session)", pricing: "Exam trap: Direct Connect = private ≠ encrypted. VPN = encrypted (IPsec) but over public internet. VPN over Direct Connect = private + encrypted (best of both, higher cost). If the question says 'encrypted at network layer' and only gives Direct Connect as an option — it is wrong unless combined with VPN/MACsec." },
      { name: "VPN vs Direct Connect: Cost & Speed Decision", full: "Hybrid Connectivity Choice: Site-to-Site VPN vs Direct Connect Based on Requirements", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🔒⚖️", tagline: "Quick + cheap + low traffic → VPN. Consistent + high bandwidth + dedicated → Direct Connect", analogy: "VPN is a courier using public roads — quick to arrange, cheap, gets the job done for small deliveries but speed varies with traffic. Direct Connect is a private motorway built just for you — expensive and slow to construct, but once built gives guaranteed lanes and consistent speed for heavy loads.", keyPoints: ["Choose Site-to-Site VPN when: quick setup needed (minutes to hours vs weeks for DX), low or variable traffic volume, cost-sensitive, encryption over internet is acceptable, temporary or short-term connection", "Choose Direct Connect when: consistent latency and throughput required, large data volumes (high data transfer cost on VPN), dedicated private link mandated by compliance, long-term steady-state connection justifies setup cost", "VPN setup time: provision in the AWS console in minutes. Requires only a Customer Gateway (your on-prem VPN device's public IP) + Virtual Private Gateway. No physical work needed", "Direct Connect setup time: weeks to months. Requires a physical cross-connect at an AWS Direct Connect location, a partner circuit, and often a Letter of Authorization (LOA). Not suitable when 'quick turnaround' is a requirement", "VPN cost: ~$0.05/hr per connection + standard data transfer rates. At low traffic volumes the total cost is very small", "Direct Connect cost: port fee ($0.03–$0.30/hr depending on capacity) + partner/colocation fees + data transfer (lower per-GB rate than internet). Only cost-effective at high sustained data volumes", "The 'most cost-effective' signal: if the question says low bandwidth + small traffic + quick setup → VPN is always more cost-effective. DX has fixed costs that only pay off at scale"], when: "Q says 'quick setup' OR 'small/low traffic' OR 'cost-effective hybrid connection' → Site-to-Site VPN. Q says 'consistent latency' OR 'dedicated connection' OR 'large data transfers' OR 'compliance requires no public internet' → Direct Connect.", pricing: "Exam fingerprints — VPN: 'quick turnaround', 'low bandwidth', 'cost-effective', 'small traffic'. Direct Connect: 'consistent performance', 'dedicated', 'large transfers', 'no public internet'. Knowing these phrase triggers is the entire exam question." },
      { name: "Direct Connect + VPN Backup", full: "Hybrid Connectivity: Direct Connect Primary + Site-to-Site VPN Failover", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🔌🔒", tagline: "Private dedicated line as primary; encrypted internet tunnel as backup", analogy: "A private fibre line to your office (Direct Connect) for everyday use — fast, consistent, no internet. If the fibre gets cut, traffic automatically reroutes through an encrypted VPN tunnel over the public internet (Site-to-Site VPN) so the business keeps running.", keyPoints: ["AWS Direct Connect: dedicated private physical connection between on-premises and AWS — does NOT traverse the public internet; consistent bandwidth and latency; typically 1 Gbps or 10 Gbps", "AWS Site-to-Site VPN: IPsec-encrypted tunnel over the public internet — quick to provision (minutes), lower cost, but variable latency; used here as the failover path", "The combination satisfies two requirements simultaneously: (1) dedicated private connection → Direct Connect; (2) guarantee uptime via public internet if DX fails → Site-to-Site VPN", "Both connect to the same Virtual Private Gateway (VGW) on the AWS side — Direct Connect via a private VIF, VPN via the standard VPN attachment; BGP routing can be configured to prefer Direct Connect and fall back to VPN automatically", "Why not VPN-only? VPN uses the public internet — doesn't satisfy 'dedicated private connection'. Why not two Direct Connect lines? Satisfies uptime but the question explicitly says willing to use public internet, implying VPN as the cheaper backup"], when: "Q says 'dedicated private connection as primary' + 'willing to use public internet if DX fails' OR 'guarantee uptime' + 'encrypted backup' → Direct Connect (primary) + Site-to-Site VPN (backup)", pricing: "Exam tip: Direct Connect alone has no built-in redundancy — a physical fibre cut takes it down. The standard HA pattern is DX primary + VPN backup. Two DX connections is also valid for HA but more expensive and not 'using the public internet'." },
      { name: "S3 Encryption: SSE-S3 vs SSE-KMS vs SSE-C vs Client-Side", full: "S3 Encryption Options: Choosing Based on Key Ownership and Algorithm Control", color: "#C0392B", lightBg: "#FDEDEC", icon: "🔐🪣", tagline: "Four encryption modes — pick based on who owns the key and who runs the algorithm", analogy: "SSE-S3 = valet parking where the hotel keeps the key. SSE-KMS = valet parking where YOU own the key but the hotel still drives the car. SSE-C = you hand a copy of your key to the valet for each trip, they use it and hand it back immediately — they never keep it. Client-Side = you drive the car yourself; the hotel never touches the key or the car.", keyPoints: ["SSE-S3 (default): AWS manages everything — generates, stores, rotates keys; uses AES-256. Zero key management overhead. Use when: no compliance requirement to control keys, just need encryption at rest", "SSE-KMS: you control the key via AWS KMS (create, rotate, audit, set policies); AWS still does the encryption. Gives CloudTrail audit log of every key use. Use when: need key access control, audit trail, or cross-account access to encrypted data", "SSE-C (customer-provided key): YOU provide the key on every API request (in HTTP header); AWS uses it to encrypt/decrypt then immediately discards it — never stores the key. Algorithm is still AES-256 (AWS's). Use when: compliance requires no third party ever retains your key, but you don't want to write your own encryption code", "Client-Side Encryption: you encrypt data BEFORE uploading to S3; S3 stores the already-encrypted blob. You control both the key AND the algorithm. Use when: proprietary/custom encryption algorithm required, or data must never be seen in plaintext by AWS under any circumstances", "Decision rule — Algorithm: if the question mentions 'proprietary algorithm' or 'custom encryption' → Client-Side only. All server-side options (SSE-*) use AWS's AES-256", "Decision rule — Key ownership: 'AWS manages keys' → SSE-S3. 'I control the key via KMS, need audit trail' → SSE-KMS. 'I provide the key per-request, AWS never stores it' → SSE-C. 'I encrypt before upload, AWS never sees plaintext' → Client-Side", "SSE-C gotcha: if you lose the key, AWS cannot recover the data — there is no backup copy of the key anywhere on AWS. Key management is entirely your responsibility"], when: "Q mentions 'proprietary algorithm' → Client-Side (only option). Q mentions 'key never retained by third party, AWS handles encryption' → SSE-C. Q mentions 'audit trail of key usage, cross-account' → SSE-KMS. Q mentions 'simplest, no key management' → SSE-S3.", pricing: "Exam trap matrix: SSE-C ≠ Client-Side. SSE-C = you own the key, AWS runs AES-256. Client-Side = you own the key AND the algorithm. Both give you key control — only Client-Side gives you algorithm control." },
      { name: "CloudFront Signed URLs vs Signed Cookies", full: "CloudFront Content Restriction: Signed URLs (one file) vs Signed Cookies (many files)", color: "#5D6D7E", lightBg: "#F2F3F4", icon: "🌩🔐", tagline: "Both restrict CloudFront content to authorised users — URLs for one file, cookies for many", analogy: "Signed URL = a single VIP ticket to one specific show. Signed Cookie = a season pass that gets you into every show in the venue without needing a new ticket each time. Both prove you're a paying subscriber — they just differ in scope.", keyPoints: ["Signed URL: grants access to one specific file. The signature, expiry, and optional IP restriction are embedded in the URL itself. Each file requires its own signed URL", "Signed Cookie: grants access to multiple files at once. CloudFront checks the cookie on every request — no URL changes needed. Ideal for subscriber access to an entire content library", "Use Signed URL when: (1) access to a single object, (2) client doesn't support cookies (e.g. custom media player, mobile app, RTMP streaming), (3) you want to distribute individual download links", "Use Signed Cookies when: (1) subscriber needs access to many files without URL changes, (2) you want clean URLs without embedded signatures, (3) ongoing session-based access to a whole section", "Both require: a CloudFront key pair (or trusted key group) to sign, and the distribution must have 'Restrict Viewer Access' enabled on the cache behaviour", "Neither exposes the S3 origin directly — CloudFront still serves from the edge; OAC on the S3 bucket ensures direct S3 access is blocked", "Wrong answers: making the S3 bucket public (no restriction), using bucket policies alone (bypasses CloudFront), using Lambda@Edge for auth (valid but not the built-in CloudFront mechanism the question asks about)"], when: "Q asks how to restrict CloudFront content to paying/authenticated subscribers → Signed URLs (one file per link) OR Signed Cookies (ongoing access to many files). Both are valid; select based on scope.", pricing: "Exam tip: both Signed URLs and Signed Cookies are correct CloudFront restriction mechanisms — the exam often asks 'select two' and both appear as answers. Decision rule: one file = Signed URL; multiple files / subscriber library = Signed Cookies." },
      { name: "AWS Organizations Alternate Contacts", full: "Multi-Account Notification Routing: Alternate Contacts + Distribution Lists", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🏢📬", tagline: "Root user email = single point of failure — use Alternate Contacts with team distribution lists instead", analogy: "A large company that routes all mail to the CEO's personal inbox. Important letters get missed when the CEO is away. The fix: set up three separate mailboxes — one for Finance, one for Security, one for IT Operations — each staffed by a team, not one person. The CEO's address stays but stops being the only destination.", keyPoints: ["Root user email problem: AWS sends all account notifications (billing, security, operations) to the root user's email by default. In multi-account orgs, root emails are often shared or unmonitored — a missed alert becomes a real incident", "Alternate Contacts: each AWS account has three configurable alternate contact types — Billing, Security, and Operations. Each can have its own email address, name, and phone number independent of the root user", "Distribution lists not individuals: configure alternate contacts with company-managed group email aliases (e.g. billing-team@company.com, security-ops@company.com). If one person leaves, the group still receives notifications — no single point of failure", "Centralised oversight: AWS Organizations allows you to set alternate contacts across all member accounts centrally via the console or API — no need to log into each account individually", "Root alias → monitored mailbox: the root email should also be redirected to a monitored shared inbox rather than a personal address, as a final catch-all", "Why not individual users: individuals go on leave, change roles, or leave the company. Team distribution lists always have coverage and align with AWS security best practices", "Categories: Billing = cost anomalies, invoice delivery; Security = GuardDuty, Security Hub, IAM alerts; Operations = service health, maintenance notices, support cases"], when: "Q asks how to ensure multi-account AWS notifications reach the right teams reliably without depending on individual users or root email → Alternate Contacts (Billing/Security/Operations) configured with company-managed distribution lists, managed centrally via AWS Organizations", pricing: "Exam tip: root user email = single point of failure. Alternate Contacts = the correct AWS-native mechanism to route categorised notifications to appropriate teams. 'Distribution lists not individuals' is the security best practice the exam tests." },
      { name: "Route 53 Private Hosted Zone VPC Settings", full: "Private Hosted Zones: enableDnsHostnames + enableDnsSupport Must Both Be Enabled", color: "#1A7A4A", lightBg: "#EAFAF1", icon: "🌐🔒", tagline: "Both VPC DNS settings must be ON for Route 53 Private Hosted Zones to resolve", analogy: "enableDnsSupport is the engine — it powers the DNS resolver so queries can be answered at all. enableDnsHostnames is the addressing system — it gives resources actual DNS names for those answers to point to. You need both: an engine without addresses goes nowhere, and addresses without an engine can't be looked up.", keyPoints: ["enableDnsSupport: enables the AWS-provided DNS resolver inside the VPC (available at the VPC base CIDR +2 address, e.g. 10.0.0.2). Without this, the VPC has no DNS resolution capability at all — no internal or external names resolve", "enableDnsHostnames: allows EC2 instances in the VPC to receive DNS hostnames (both public and private). Without this, instances have no DNS names and Route 53 private hosted zone records cannot resolve to them", "Both must be true together: enableDnsSupport alone = resolver exists but resources have no names. enableDnsHostnames alone = names assigned but no resolver to look them up. Both required for end-to-end private DNS resolution", "Also required for VPC Peering DNS resolution: if you want hostnames in a peered VPC to resolve, both VPCs must have both settings enabled, and 'Allow DNS resolution from remote VPC' must be checked in the peering connection", "Default values: enableDnsSupport defaults to true in all VPCs. enableDnsHostnames defaults to true only in the default VPC — custom VPCs have it disabled by default and you must explicitly enable it", "Private Hosted Zone association: after enabling both settings, associate the private hosted zone with the VPC in the Route 53 console. Records in the zone then resolve for all resources in the VPC"], when: "Q asks what VPC settings must be enabled for Route 53 Private Hosted Zones to work → enableDnsHostnames AND enableDnsSupport (select two). Also applies to VPC Peering + DNS resolution questions.", pricing: "Exam tip: this is a classic 'select two' pair. Any time you see Route 53 Private Hosted Zone + VPC, the answer is always both settings. Custom VPCs have enableDnsHostnames OFF by default — forgetting to enable it is the most common misconfiguration." },
      { name: "EFA for HPC Workloads", full: "Elastic Fabric Adapter: OS-Bypass Networking for Tightly-Coupled HPC", color: "#9B59B6", lightBg: "#F5EEF8", icon: "🔬⚡", tagline: "HPC inter-node communication needs OS-bypass — ENA is fast but EFA is purpose-built", analogy: "Standard networking (ENA) is like sending messages through a postal office — fast, but every letter is handled by staff (the OS kernel) before delivery. EFA is like two people passing notes directly through a hole in the wall — no intermediary, no delay, maximum throughput.", keyPoints: ["Three EC2 network adapter types to know: ENI (basic, all instances), ENA (enhanced, up to 100 Gbps, kernel-mediated), EFA (OS-bypass, ultra-low latency, HPC-specific)", "The key EFA feature is OS-bypass: the application sends messages directly to the NIC without going through the OS kernel. This eliminates kernel jitter and latency — critical when thousands of nodes exchange millions of small messages per second", "MPI (Message Passing Interface): the standard API for HPC parallel programs. EFA implements the libfabric interface that MPI libraries use — EFA + MPI = the correct HPC stack on AWS", "Use cases: genomics, computational fluid dynamics (CFD), molecular dynamics, financial risk modelling, seismic analysis — any tightly-coupled simulation where nodes constantly synchronise", "Constraints: Linux only (OS-bypass requires Linux kernel extensions); same AZ only (inter-node EFA traffic doesn't cross AZs); use Cluster Placement Group to minimise physical distance between nodes", "ENA is the wrong answer for HPC: ENA gives higher bandwidth than ENI but is still kernel-mediated. For general high-performance workloads ENA is fine — for tightly-coupled HPC with constant inter-node messaging, only EFA is correct", "Cluster Placement Group: packs instances onto the same underlying hardware rack — minimises physical network distance, maximises bisection bandwidth, pairs with EFA for lowest latency"], when: "Q mentions HPC + genomics/CFD/molecular dynamics/seismic + low-latency inter-node communication + parallel computing → EFA. If also asking about placement → Cluster Placement Group. Never choose ENA for tightly-coupled HPC.", pricing: "Exam tip: EFA fingerprints — 'HPC', 'MPI', 'tightly coupled', 'inter-node communication', 'OS-bypass', 'genomics'. ENA = general fast networking. EFA = HPC-specific kernel-bypass. ENI = basic networking." },
      { name: "Cognito Identity Pool + S3 VPC Endpoint", full: "Secure S3 Access from Private VPC: Cognito Identity Pool + Gateway Endpoint", color: "#2E86AB", lightBg: "#EBF5FB", icon: "🤝🔒🪣", tagline: "Who can access S3 (Identity Pool) + how traffic reaches S3 (Gateway Endpoint) — both required", analogy: "A secure office building with a badge reader (Cognito Identity Pool — proves who you are and gives you a keycard) and a private internal corridor to the file room (VPC Gateway Endpoint — keeps you off the public street). You need both: the badge to open the file room door, and the corridor to get there privately.", keyPoints: ["Cognito User Pool vs Identity Pool: User Pool = authentication (login, issues JWT). Identity Pool = AWS authorisation (exchanges JWT for temporary IAM credentials via STS). They are different services — User Pool alone cannot grant S3 access", "Flow: User logs in via User Pool → gets JWT → app passes JWT to Identity Pool → Identity Pool calls STS → returns temporary IAM role credentials → app uses credentials to call S3 API", "Fine-grained S3 access: the IAM role can use ${cognito-identity.amazonaws.com:sub} as the key to scope each user to their own S3 prefix — e.g. s3:PutObject on bucket/users/${sub}/* — no user can touch another user's files", "VPC Gateway Endpoint for S3: adds a route to the VPC route table that directs S3-bound traffic to the endpoint instead of the internet. Traffic stays on AWS backbone — no NAT Gateway, no internet exposure", "Why both answers together: Identity Pool = WHAT access (fine-grained, scalable, per-user IAM). Gateway Endpoint = HOW traffic flows (private, no internet, satisfies private network requirement). Neither alone satisfies both requirements", "Bucket policy enforcement: add aws:SourceVpce condition to the S3 bucket policy to ensure ALL access must come through the VPC endpoint — even valid credentials can't access the bucket from outside the VPC", "Scalable: temporary credentials auto-expire (no static IAM users), Identity Pool scales to millions of users, Gateway Endpoint handles any traffic volume"], when: "Q describes private VPC + Cognito auth + users accessing their own S3 files + private network requirement → Cognito Identity Pool (temporary creds + fine-grained access) + S3 Gateway VPC Endpoint (private path). Select two.", pricing: "Exam trap: Cognito User Pool alone cannot grant S3 access — it only authenticates. Identity Pool is the bridge to AWS services. Gateway Endpoint is free; a NAT Gateway would work too but is costly and keeps traffic on the internet." },
      { name: "ASG + EIP: Monolith Single-Server HA", full: "Monolith HA Without Load Balancer: ASG (min=max=1) + EIP + EC2 Instance Role", color: "#E8983C", lightBg: "#FEF5ED", icon: "📦📌🔁", tagline: "Can't distribute the monolith? Use ASG to self-heal + EIP to preserve the IP — no ALB needed", analogy: "A solo performer who must always be on stage, but has an understudy ready in another theatre. If the main theatre collapses (AZ failure), the understudy steps in at a different venue. Because audiences know only the phone number (EIP), not which theatre, they automatically reach whoever is performing — the number follows the performer.", keyPoints: ["Constraint: the app cannot be distributed — must run on exactly ONE instance at all times (monolith, licensing, stateful session, etc.). ASG with min=1, max=1, desired=1 enforces this hard limit — never runs two instances simultaneously", "AZ failure recovery: ASG spans 2 AZs. If AZ-A fails, ASG automatically launches a replacement in AZ-B. No manual intervention required — this is the self-healing mechanism", "EIP preserves the static public IP: without EIP, the replacement EC2 gets a new random public IP. Clients (or DNS records, firewall whitelists) that point to the old IP would break. EIP is re-attached to the new instance so users reach it at the same address", "EC2 user-data script attaches the EIP: on instance launch, user-data runs aws ec2 associate-address --allocation-id ... This is why the EIP is re-attached automatically without human intervention", "EC2 Instance Role provides credentials: the user-data script calls ec2:AssociateAddress API. The instance role grants IAM permissions for this call — no hardcoded credentials, no secrets in user-data, follows least-privilege best practice", "Sequential flow on AZ failure: AZ fails → ASG launches replacement in surviving AZ → instance boots → user-data runs → Instance Role creds → EIP re-attached → users reach new instance at same IP", "Why NOT ALB: ALB requires at least 2 healthy instances across AZs or it becomes an unnecessary cost. With max=1, there is never a second instance for ALB to route to. ALB adds cost for zero benefit here", "Why NOT Route 53 failover: Route 53 health checks + failover routing requires two endpoints. With only one running instance at any time, there is no secondary endpoint to fail over to", "Cost efficiency: never runs more than 1 instance (max=1 cap). EIP is free when attached to a running instance. No ALB, no Route 53 paid health checks — minimum cost for AZ-resilient single-instance HA"], when: "Q describes a monolith that MUST run on a single EC2 + survive AZ failure + cost-efficient + select three → ASG (min=max=1 across 2 AZs) + EIP (attached via user-data) + EC2 Instance Role (IAM creds for the attach API call)", pricing: "Exam trap: don't confuse this with normal horizontal scaling. The max=1 cap is intentional — the ASG is used purely for self-healing across AZs, not for scaling out. EIP + Instance Role are the glue that makes the static IP re-attach automatically after failover." },
    ]
  },
};

const compareData = {
  networking: [
    { label: "Private network in AWS", service: "VPC", note: "Foundation of every deployment" },
    { label: "Internet access for public subnet", service: "Internet Gateway", note: "The VPC's front door — free" },
    { label: "Outbound internet for private subnet", service: "NAT Gateway", note: "One-way door — costly at high volume" },
    { label: "Private AWS service access (no internet)", service: "VPC Endpoints", note: "Gateway (free for S3/DynamoDB) or Interface" },
    { label: "Encrypted on-prem tunnel", service: "VPN", note: "Over internet, fast to set up, $0.05/hr" },
    { label: "Dedicated on-prem connection", service: "Direct Connect", note: "Private, consistent, expensive" },
    { label: "Instance-level firewall", service: "Security Groups", note: "Stateful — free" },
    { label: "Subnet-level firewall", service: "NACLs", note: "Stateless — must set both directions" },
    { label: "Spread HTTP/HTTPS traffic", service: "ALB", note: "Layer 7, path-based routing" },
    { label: "Spread TCP/UDP traffic", service: "NLB", note: "Layer 4, millions of req/sec" },
    { label: "DNS record types: A, CNAME, Alias, PTR", service: "DNS Record Types", note: "Alias = CNAME at zone apex + free for AWS resources. CNAME = subdomain only. PTR = reverse DNS." },
    { label: "Domain names / DNS", service: "Route 53", note: "Points URLs to resources + failover" },
    { label: "VPC resolving on-prem DNS names (hybrid DNS)", service: "Route 53 Resolver", note: "Outbound endpoint + forwarding rule → on-prem DNS server" },
    { label: "Cache content globally (CDN)", service: "CloudFront", note: "Edge caching, 200+ locations" },
    { label: "Code at the edge", service: "Lambda@Edge", note: "Auth, rewrites, personalisation" },
    { label: "Lock S3 to CloudFront", service: "OAC / OAI", note: "Prevent direct public S3 access" },
    { label: "Speed up live dynamic traffic", service: "Global Accelerator", note: "AWS backbone, no caching" },
    { label: "Expose an API", service: "API Gateway", note: "Pairs great with Lambda" },
    { label: "Resources need internet access", service: "Public Subnet", note: "Has IGW route; place ALBs, NAT GWs here" },
    { label: "Resources should stay private", service: "Private Subnet", note: "No IGW route; use NAT GW for outbound" },
    { label: "Connect many VPCs + on-prem", service: "Transit Gateway", note: "Hub-and-spoke; replaces full-mesh peering" },
    { label: "Share a service privately across VPCs", service: "PrivateLink", note: "No internet, no peering needed" },
    { label: "Private access to AWS APIs (no NAT)", service: "Interface Endpoint", note: "ENI in subnet via PrivateLink" },
    { label: "Connect two VPCs privately (simple, one-to-one)", service: "VPC Peering", note: "Not transitive; no overlapping CIDRs; cross-account/cross-region supported" },
    { label: "Load balance to 3rd-party firewall/IDS appliances", service: "GLB", note: "GENEVE protocol; transparent traffic inspection; scale virtual appliances" },
    { label: "Static public IP for EC2 (survives stop/start)", service: "Elastic IP", note: "Charge when unattached; remap for failover; 5/region default" },
    { label: "Dual-NIC EC2 / MAC-address failover", service: "ENI", note: "Detach/reattach with IP + MAC; EFA variant for HPC/MPI" },
  ],
  compute: [
    { label: "Full VM", service: "EC2", note: "You manage OS & software" },
    { label: "Clone an EC2 environment", service: "AMI", note: "Pre-baked image to stamp out instances" },
    { label: "Run code only", service: "Lambda", note: "No servers at all" },
    { label: "Scale EC2 fleet automatically", service: "Auto Scaling", note: "Min/desired/max — pairs with ELB" },
    { label: "The pool of EC2s being scaled", service: "ASG", note: "Launch Template + min/desired/max" },
    { label: "Keep a metric at a target (e.g. 50% CPU)", service: "Target Tracking", note: "Simplest — recommended default" },
    { label: "Scale more aggressively as breach grows", service: "Step Scaling", note: "Tiered response to alarm severity" },
    { label: "One alarm → fixed adjustment (legacy)", service: "Simple Scaling", note: "Waits for cooldown; prefer Target Tracking" },
    { label: "Pre-scale for a known busy period", service: "Scheduled Scaling", note: "Cron-based; proactive not reactive" },
    { label: "Pre-scale using ML traffic forecast", service: "Predictive Scaling", note: "Learns weekly/daily patterns; needs 24hr history" },
    { label: "Docker (AWS style)", service: "ECS", note: "AWS-native orchestration" },
    { label: "Docker (industry standard)", service: "EKS", note: "Kubernetes managed by AWS" },
    { label: "Docker, no servers", service: "Fargate", note: "Pairs with ECS or EKS" },
    { label: "Just deploy my app", service: "Beanstalk", note: "Platform-as-a-Service feel" },
    { label: "AWS APIs on-premises (data residency / ultra-low latency)", service: "Outposts", note: "AWS rack in your facility — same APIs, managed by AWS" },
    { label: "Simple VPS / small app, flat monthly price", service: "Lightsail", note: "Fixed bundles, simplified console — DigitalOcean-like" },
    { label: "Long-running batch compute jobs (no 15-min limit)", service: "AWS Batch", note: "Managed EC2/Fargate; supports Spot for 90% savings; no time limit" },
    { label: "Automate golden AMI creation + patching", service: "EC2 Image Builder", note: "Build → test (Inspector scan) → distribute pipeline; scheduled" },
  ],
  storage: [
    { label: "Frequent access", service: "S3 Standard", note: "Default — no retrieval fee" },
    { label: "Infrequent access, multi-AZ", service: "S3 IA", note: "Cheaper storage, pay to retrieve" },
    { label: "Infrequent access, single AZ", service: "S3 One Zone-IA", note: "20% cheaper, one AZ only" },
    { label: "Unpredictable access patterns", service: "Intelligent-Tiering", note: "Auto-moves to cheapest tier" },
    { label: "Archive, instant retrieval", service: "Glacier Instant", note: "Cheap + ms access, 90-day min" },
    { label: "Archive, minutes–hours", service: "Glacier Flexible", note: "Expedited/Standard/Bulk" },
    { label: "Cheapest long-term archive", service: "Glacier Deep Archive", note: "~$0.001/GB/mo, 12hr retrieval" },
    { label: "Prevent object deletion/overwrite (WORM compliance)", service: "S3 Object Lock", note: "Governance or Compliance mode; Retain Until Date per version" },
    { label: "Auto-move/delete objects", service: "S3 Lifecycle", note: "Rules based on age, prefix, or tags" },
    { label: "Speed up global S3 uploads", service: "S3 Transfer Acceleration", note: "CloudFront edge as upload on-ramp" },
    { label: "Shared file system (multi-EC2)", service: "EFS", note: "NFS, auto-scales, cross-AZ" },
    { label: "Centralised backups", service: "AWS Backup", note: "Policy-driven, covers all services" },
    { label: "On-prem hybrid storage bridge", service: "Storage Gateway", note: "File (NFS/SMB→S3), Volume (iSCSI→S3), Tape (VTL→Glacier)" },
    { label: "On-prem NFS/SMB files → S3 (no app changes)", service: "File Gateway", note: "NFS share backed by S3; local cache; pair with Lifecycle policies" },
    { label: "On-prem iSCSI block volumes → AWS backup", service: "Volume Gateway", note: "Cached (data in S3) or Stored (data on-prem, snapshots to S3)" },
    { label: "Replace physical tape library, keep existing backup software", service: "Tape Gateway", note: "Virtual Tape Library (VTL) via iSCSI → S3/Glacier; no software changes" },
    { label: "Replicate S3 objects to another region (DR)", service: "S3 Replication", note: "CRR: cross-region. SRR: same-region. Versioning required both ends." },
    { label: "Share private S3 file temporarily without making bucket public", service: "S3 Presigned URLs", note: "Time-limited signed URL; anyone with URL can access; also used for direct client uploads" },
    { label: "Windows file shares (SMB, NTFS, AD-integrated)", service: "FSx for Windows", note: "Multi-AZ, shadow copies, DFS; SSD or HDD" },
    { label: "HPC / ML training (Linux, extreme parallel throughput)", service: "FSx for Lustre", note: "Sub-ms latency, S3 integration; Scratch (ephemeral) or Persistent" },
    { label: "NFS for Linux/macOS/Windows + ZFS features", service: "FSx for OpenZFS", note: "Snapshots, clones, compression; migrate on-prem ZFS/NFS" },
    { label: "SMB + NFS simultaneously (Windows + Linux + macOS)", service: "FSx for ONTAP", note: "Dual-protocol; NetApp SnapMirror; SSD + S3 tiering; iSCSI" },
    { label: "Persistent disk for EC2", service: "EBS Overview", note: "Block storage, one EC2 at a time — charged per provisioned GB" },
    { label: "Highest IOPS temporary scratch disk for EC2", service: "Instance Store", note: "Ephemeral — data lost on stop/terminate; free with instance" },
    { label: "Default EBS — most workloads", service: "gp3 (SSD)", note: "3k IOPS free, independently scalable" },
    { label: "Legacy general SSD", service: "gp2 (SSD)", note: "IOPS tied to size — migrate to gp3" },
    { label: "Highest-performance DB", service: "io2 Block Express", note: "Up to 256k IOPS, sub-ms latency" },
    { label: "High IOPS (older)", service: "io1 (SSD)", note: "Same cost as io2 — io2 is better" },
    { label: "Big data / log processing", service: "st1 (HDD)", note: "High throughput, sequential, cheap" },
    { label: "Cold / rarely accessed EC2 disk", service: "sc1 (HDD)", note: "Cheapest EBS ~$0.015/GB/month" },
    { label: "Back up an EBS volume", service: "EBS Snapshots", note: "Incremental, cross-region capable" },
    { label: "Understand IOPS vs throughput", service: "I/O Operations", note: "IOPS = random; throughput = sequential" },
  ],
  databases: [
    { label: "Standard SQL", service: "RDS", note: "Managed MySQL/Postgres/etc." },
    { label: "High-performance SQL", service: "Aurora", note: "Faster + scales better than RDS" },
    { label: "SQL across regions / DR", service: "Aurora Global DB", note: "Sub-second replication, ~1min failover" },
    { label: "Staying up / recovering fast", service: "HA / DR", note: "Multi-AZ = HA; Multi-Region = DR" },
    { label: "Massive scale, no SQL", service: "DynamoDB", note: "Key-value, ms latency" },
    { label: "Cache for DynamoDB specifically", service: "DAX", note: "Microsecond latency, drop-in API" },
    { label: "Auto-expire stale data", service: "TTL", note: "DynamoDB + Redis/ElastiCache" },
    { label: "General cache layer", service: "ElastiCache", note: "Redis or Memcached" },
    { label: "Cache with rich data types", service: "Redis", note: "Lists, sets, pub/sub, TTL, persistence" },
    { label: "Cache replicated globally", service: "Global Datastore", note: "Redis across regions" },
    { label: "Relationships / graphs", service: "Neptune", note: "Social networks, fraud detection" },
    { label: "Lambda hitting RDS at high concurrency", service: "RDS Proxy", note: "Connection pooling — prevents 'too many connections'" },
  ],
  security: [
    { label: "Control who can do what", service: "IAM", note: "Users, Roles, Policies" },
    { label: "Single sign-on / multiple accounts", service: "SSO / IAM Identity Center", note: "One login for all accounts + SaaS apps" },
    { label: "Encrypt data at rest", service: "KMS", note: "Manage keys for S3, EBS, RDS, etc." },
    { label: "Store passwords / API keys", service: "Secrets Manager", note: "Auto-rotates, never hardcode" },
    { label: "Block web attacks", service: "WAF", note: "SQLi, XSS — CloudFront/ALB" },
    { label: "Block DDoS", service: "Shield", note: "Standard free; Advanced $3k/mo" },
    { label: "Detect suspicious activity", service: "GuardDuty", note: "AI-driven, no agents" },
    { label: "Scan for vulnerabilities", service: "Inspector", note: "EC2, Lambda, containers" },
    { label: "Find PII in S3", service: "Macie", note: "Sensitive data discovery" },
    { label: "Unified security dashboard", service: "Security Hub", note: "Aggregates all findings" },
    { label: "Managed AD for Windows workloads / domain join EC2", service: "Directory Service", note: "Managed Microsoft AD; AD Connector for on-prem proxy; Simple AD (Samba) for lightweight" },
    { label: "FIPS 140-2 Level 3 hardware HSM — AWS cannot access keys", service: "CloudHSM", note: "Dedicated single-tenant HSM; Oracle TDE; SSL key storage" },
    { label: "Deep packet inspection / IDS/IPS / domain filtering in VPC", service: "Network Firewall", note: "Stateful + stateless rules; Suricata-compatible; per-subnet inspection" },
    { label: "All IAM policy types and evaluation logic", service: "IAM Policies", note: "Identity-based, resource-based, SCPs, permission boundaries, trust policies, session policies" },
    { label: "Add login to your app", service: "Cognito", note: "User Pools + Identity Pools" },
    { label: "HTTPS certificates", service: "ACM", note: "Free, auto-renewing SSL/TLS" },
    { label: "Audit who did what", service: "CloudTrail", note: "Full API call history" },
    { label: "Centralise all security logs for SIEM / threat hunting", service: "Security Lake", note: "OCSF-normalised S3 data lake — your account, your data" },
  ],
  messaging: [
    { label: "Managed ActiveMQ/RabbitMQ (lift-and-shift messaging)", service: "Amazon MQ", note: "AMQP/MQTT/STOMP support; existing apps unchanged; active/standby HA" },
    { label: "Send transactional or bulk email from an app", service: "SES", note: "DKIM, SPF, domain verification; inbound email → S3/Lambda/SNS" },
    { label: "Decouple two services", service: "SQS", note: "Queue, consumer pulls when ready" },
    { label: "Broadcast to many", service: "SNS", note: "Pub/sub, push to all subscribers" },
    { label: "Route AWS/SaaS events", service: "EventBridge", note: "Rules-based event bus" },
    { label: "Real-time stream ingestion with replay", service: "Kinesis Data Streams", note: "Shards, multiple consumers, 24hr–365day retention" },
    { label: "Load stream data into S3/Redshift/OpenSearch", service: "Kinesis Firehose", note: "Managed, buffered (~60s), no shard management" },
    { label: "SQL/Flink queries on a live stream", service: "Kinesis Data Analytics", note: "Windowed aggregations, anomaly detection in real time" },
    { label: "Fan-out to queues", service: "SNS + SQS", note: "SNS topic → multiple SQS queues" },
    { label: "Replace a cron job", service: "EventBridge", note: "Scheduled rules, no server" },
  ],
  serverless: [
    { label: "Orchestrate multi-step workflows", service: "Step Functions", note: "State machine; retries, branching, waits" },
    { label: "Run code on events", service: "Lambda", note: "No servers; pay per ms" },
    { label: "HTTP endpoint for Lambda", service: "API Gateway", note: "Use HTTP API for 70% cost saving" },
    { label: "Low-latency NoSQL key-value store", service: "DynamoDB", note: "On-Demand mode = truly serverless" },
    { label: "Route or schedule events", service: "EventBridge", note: "Replaces CloudWatch Events + cron" },
    { label: "Decouple with a queue", service: "SQS", note: "Buffer between producer and consumer" },
  ],
  analytics: [
    { label: "Query S3 with SQL (no DB)", service: "Athena", note: "Serverless, pay per TB scanned" },
    { label: "ETL jobs + schema catalog", service: "Glue", note: "Serverless Spark + Data Catalog for Athena" },
    { label: "Build ETL pipelines visually (no Spark)", service: "Glue Studio", note: "Drag-and-drop canvas, generates Glue job code" },
    { label: "Clean and prep data visually (no code)", service: "Glue DataBrew", note: "250+ transforms, data profiling, recipes" },
    { label: "Pull SaaS data into AWS (no custom code)", service: "AppFlow", note: "50+ connectors: Salesforce, Slack, ServiceNow…" },
    { label: "Custom Spark / Hadoop at scale", service: "EMR", note: "Managed cluster, use Spot to cut costs" },
    { label: "SQL data warehouse for BI", service: "Redshift", note: "Columnar, petabyte-scale" },
    { label: "Full-text search or log analytics", service: "OpenSearch", note: "Elasticsearch-compatible, Kibana dashboards" },
    { label: "Self-service BI dashboards", service: "QuickSight", note: "Serverless, pay-per-session" },
    { label: "Fine-grained access control (column/row) for S3 data lake", service: "Lake Formation", note: "On top of Glue; column/row permissions for Athena/Redshift/EMR queries" },
  ],
  ai: [
    { label: "Build / train / deploy ML models", service: "SageMaker", note: "End-to-end managed ML platform" },
    { label: "Analyse sentiment or entities in text", service: "Comprehend", note: "NLP — no ML expertise needed" },
    { label: "Convert audio or video to text", service: "Transcribe", note: "Batch or real-time streaming" },
    { label: "Detect anomalies in business metrics", service: "Lookout", note: "Auto-learns normal; alerts on spikes/drops" },
    { label: "Detect objects, faces, or unsafe content in images/video", service: "Rekognition", note: "Object detection, facial analysis, content moderation, OCR — no ML expertise needed" },
  ],
  monitoring: [
    { label: "Monitor metrics & alarms", service: "CloudWatch", note: "Core observability service" },
    { label: "Centralise & search logs", service: "CloudWatch Logs", note: "All service logs in one place" },
    { label: "Memory/disk from EC2", service: "CloudWatch Agent", note: "Not in default metrics" },
    { label: "Trace requests across services", service: "X-Ray", note: "Find latency & errors" },
    { label: "Auto-fix from alarms", service: "EventBridge", note: "Alarm → EventBridge → Lambda" },
  ],
  devops: [
    { label: "Repeatable infra from templates", service: "CloudFormation", note: "YAML/JSON IaC, free" },
    { label: "Infra as real code (Python/TS)", service: "CDK", note: "Compiles to CloudFormation" },
    { label: "Shell into EC2 without SSH", service: "Systems Manager", note: "Session Manager — no bastion needed" },
    { label: "Store config / non-rotating secrets", service: "Parameter Store", note: "Free standard tier; cheaper than Secrets Manager" },
    { label: "Compile and test code in CI (managed build server)", service: "CodeBuild", note: "buildspec.yml; Docker environments; pay per build minute; pairs with CodePipeline" },
    { label: "Automate source → build → deploy", service: "CodePipeline", note: "Managed CI/CD orchestrator" },
    { label: "Zero-downtime fleet deployments", service: "CodeDeploy", note: "Blue/green and rolling strategies" },
  ],
  migration: [
    { label: "Move >10TB offline (no internet)", service: "Snowball Edge", note: "Physical device, AWS ships it to you" },
    { label: "Move entire data centre (100PB+)", service: "Snowmobile", note: "Shipping container on a truck" },
    { label: "Online file/NAS sync to AWS", service: "DataSync", note: "Fast, scheduled, incremental" },
    { label: "Migrate a database with live CDC", service: "DMS", note: "Source stays online; heterogeneous support" },
    { label: "Track all migration progress", service: "Migration Hub", note: "Central dashboard, no migration work here" },
    { label: "Legacy SFTP/FTP/FTPS vendors uploading to S3 (fully managed)", service: "AWS Transfer Family", note: "SFTP/FTPS/FTP endpoint → stores directly in S3/EFS; IAM role per vendor; no EC2" },
  ],
  pricing: [
    { label: "Unpredictable / short-term", service: "On-Demand", note: "Full price, no commitment" },
    { label: "Steady workload, 1–3 years", service: "Reserved", note: "Up to 72% off" },
    { label: "Flexible commitment", service: "Savings Plans", note: "Up to 72% off, more flexible" },
    { label: "Fault-tolerant batch jobs", service: "Spot Instances", note: "Up to 90% off, interruptible" },
    { label: "Learning / prototyping", service: "Free Tier", note: "750hrs EC2, 1M Lambda, 25GB DynamoDB" },
    { label: "Reduce data egress", service: "Data Transfer", note: "Same AZ free; use CloudFront/VPC Endpoints" },
    { label: "Prevent surprise bills", service: "AWS Budgets", note: "Alerts + auto-actions at threshold" },
    { label: "Analyse actual spend", service: "Cost Explorer", note: "Spot waste, forecast, rightsizing" },
    { label: "Plan before you build", service: "Pricing Calculator", note: "calculator.aws — free tool" },
    { label: "ML-based rightsizing for EC2 / Lambda / ECS / EBS", service: "Compute Optimizer", note: "14–93 day CloudWatch analysis; shows over/under-provisioned; advisory only" },
    { label: "VPC tenancy vs instance tenancy interaction (which wins?)", service: "VPC & Instance Tenancy", note: "VPC=dedicated overrides LT setting. VPC=default defers to LT. Asymmetric rule." },
  ],
  governance: [
    { label: "Manage multiple AWS accounts", service: "Organizations", note: "OUs, consolidated billing, enables SCPs" },
    { label: "Restrict what accounts can do", service: "SCPs", note: "Max-permission guardrails; overrides IAM" },
    { label: "Set up a landing zone at scale", service: "Control Tower", note: "Accounts, guardrails, Account Factory" },
    { label: "Share resources across accounts", service: "RAM", note: "Transit GW, subnets, licences — no duplication" },
    { label: "Audit config changes over time", service: "Config", note: "History + compliance rules + conformance packs" },
    { label: "Account-wide best-practice checks", service: "Trusted Advisor", note: "Cost, security, limits — full access needs Support plan" },
  ],
  scenarios: [
    { label: "🔒 Design Secure Architectures", isHeader: true, color: "#6B48C8" },
    { label: "Restrict which EC2s can mount EFS", service: "EFS Access Control", note: "IAM policy (mount permission) + Security Group (port 2049)" },
    { label: "Serve S3 via CloudFront, block direct access", service: "S3 + CloudFront OAC", note: "OAC + private bucket policy — deny direct S3 URLs" },
    { label: "Encrypt data with company-managed keys", service: "Encryption Layers", note: "SSE-KMS CMK = you control keys + CloudTrail audit" },
    { label: "S3 retention rules not working as expected", service: "S3 Retention Config", note: "Explicit object-version setting overrides bucket default; versions can differ" },
    { label: "Alert on unusual/illegal API calls in near-real time", service: "API Call Monitoring", note: "CloudTrail → CW Logs → Metric Filter → CW Alarm → SNS" },
    { label: "Restrict content streaming to specific countries", service: "Geo Content Restriction", note: "CloudFront geo restriction (CDN layer) + Route 53 geolocation routing (DNS layer)" },
    { label: "Per-Pod IAM least privilege in EKS", service: "EKS IRSA", note: "IRSA: service account → IAM role → scoped credentials per Pod" },
    { label: "Lambda in one account needs to mount EFS in another", service: "Cross-Account EFS Lambda", note: "EFS resource policy + VPC peering + EFS access point" },
    { label: "Encrypt an existing unencrypted RDS database", service: "Encrypt Existing RDS", note: "Snapshot → copy as encrypted → restore new DB → terminate old" },
    { label: "Multiple services sharing one S3 bucket, access by prefix", service: "S3 Access Points per Service", note: "One access point per service with prefix-scoped policy — scales without bloating bucket policy" },
    { label: "Web + DB tier: most secure connectivity between EC2 layers", service: "Security Group Chaining", note: "SG B inbound from SG A on port 1433 — no IP ranges, follows instances as they scale" },
    { label: "Restrict API Gateway to trusted IP ranges, least complexity", service: "API GW IP Allowlist", note: "Resource policy with explicit Deny for all except allowlisted IPs — no WAF needed" },
    { label: "Legacy SFTP vendors uploading files to S3, fully managed", service: "Transfer Family SFTP to S3", note: "Transfer Family SFTP endpoint → S3 + IAM role per vendor + Cognito IdP" },
    { label: "ALB + EC2 ASG + RDS: configure three security groups", service: "ALB + EC2 + RDS Security Groups", note: "ALB: 443 from internet; EC2: 80 from ALB SG; RDS: 5432 from EC2 SG" },
    { label: "EC2 has public IP + NAT instance in same subnet — who does NAT?", service: "IGW does NAT, not NAT instance", note: "IGW performs 1:1 NAT for any instance with a public IP (private IP ↔ public IP). NAT instance/gateway is only for private instances with no public IP. The OS never sees its own public IP — IGW handles the translation." },
    { label: "DNS record updated but users still hitting old Load Balancer — why?", service: "DNS TTL Caching", note: "TTL still in effect — resolvers cached the old record and won't re-query Route 53 until TTL expires. Fix: lower TTL well before migration (e.g. 60s), wait for old TTL to drain, then cut over." },
    { label: "🔁 Design Resilient Architectures", isHeader: true, color: "#2E86AB" },
    { label: "HA vs read performance for RDS", service: "Multi-AZ vs Read Replica", note: "Multi-AZ = failover HA; Read Replica = read scaling" },
    { label: "Match RTO/RPO to DR budget", service: "DR Strategies", note: "Backup < Pilot Light < Warm Standby < Multi-site" },
    { label: "SNS → Lambda notifications dropping at peak traffic", service: "SNS Lambda Concurrency", note: "Lambda account concurrency quota hit — raise via AWS Support" },
    { label: "VPC resolving on-prem private DNS hostnames", service: "Route 53 Resolver Hybrid", note: "Outbound endpoint + forwarding rule → on-prem DNS server" },
    { label: "RDS Multi-AZ DB engine upgrade — does it cause downtime?", service: "RDS Multi-AZ Maintenance", note: "Yes — engine upgrades hit both primary and standby simultaneously; Multi-AZ doesn't prevent it" },
    { label: "ASG not terminating an unhealthy instance — why?", service: "ASG Unhealthy Instance Not Terminated", note: "Grace period active, instance Impaired status, or SG health check type is EC2 not ELB" },
    { label: "Minimum instances for 4 always available across AZ failures", service: "HA Min Cost 3 AZs", note: "3 AZs × 2 instances = 6 total; any one AZ failure leaves exactly 4" },
    { label: "⚡ Design High-Performing Architectures", isHeader: true, color: "#E8593C" },
    { label: "Decouple, fan-out, or route events", service: "SQS vs SNS vs EventBridge", note: "Queue / broadcast / rule-based routing" },
    { label: "Speed up content vs speed up dynamic traffic", service: "CloudFront vs Global Accelerator", note: "CloudFront = Layer 7 CDN, caches at edge, HTTP only. Global Accelerator = Layer 3/4, routes through AWS backbone, any TCP/UDP, 2 static IPs. Cacheable content → CloudFront. Dynamic/non-HTTP/static IPs needed → Global Accelerator." },
    { label: "Users losing sessions after Auto Scaling adds instances", service: "Stateless vs Stateful Scale", note: "Externalise session to ElastiCache Redis or DynamoDB" },
    { label: "Aurora under read pressure from frequent repeated identical queries", service: "ElastiCache (not more replicas)", note: "Repeated identical queries = caching problem, not scaling problem. ElastiCache Redis caches results so 10,000 identical queries hit the DB once. More read replicas still execute every repeated query — treats symptom not cause." },
    { label: "Lambda timing out / too many connections to RDS", service: "RDS Proxy Scenario", note: "RDS Proxy pools connections — Lambda never connects directly" },
    { label: "S3 requests throttling at peak load (>5,000 req/s)", service: "S3 Prefix Scaling", note: "Add prefixes — each gets its own 5,500 GET req/s limit, free" },
    { label: "Slow large-file S3 uploads from overseas offices", service: "S3 Upload Speed", note: "S3 Transfer Acceleration + multipart uploads (both together)" },
    { label: "Global users uploading AND downloading large files to S3 — lowest latency", service: "S3 + S3TA vs CloudFront", note: "S3TA (not CloudFront). CloudFront = download CDN only; S3TA accelerates both uploads and downloads via edge locations. EC2 + Global Accelerator adds unnecessary complexity and cost." },
    { label: "HPC workload needing low latency between EC2 instances", service: "EC2 Placement Groups", note: "Cluster placement group — same AZ, same rack, max throughput" },
    { label: "Ordered SQS processing at 1,000+ messages/sec", service: "SQS FIFO Throughput", note: "FIFO + batch mode (batch of 4 = 1,200/s, batch of 10 = 3,000/s)" },
    { label: "Which EBS types cannot be used as boot volumes", service: "EBS Boot Volumes", note: "st1 and sc1 (HDD types) cannot — only SSD types (gp2/gp3/io1/io2) can" },
    { label: "Oracle DB needing OS customisation + HA on AWS", service: "RDS Custom Oracle", note: "RDS Custom Multi-AZ — AWS manages infra, DBA keeps OS/engine access" },
    { label: "Lightweight validation + long-running backend, fully managed", service: "API GW + Lambda + Fargate", note: "API Gateway → Lambda (validate) → ECS Fargate (heavy compute)" },
    { label: "Replace nightly batch EC2 with event-driven image processing", service: "S3 Event → SQS → Lambda", note: "S3 notification → SQS buffer → Lambda; SQS adds retry + durability" },
    { label: "Stream existing S3 files + ongoing updates to Kinesis — fastest solution", service: "DMS as S3 → Kinesis Bridge", note: "DMS supports S3 as source and Kinesis as target — handles full load (existing files) + CDC (new/updated files). No custom code. Exam trick: DMS isn't only for DB migrations." },
    { label: "Big data / parallel analytics workload on EFS", service: "EFS Max I/O Mode", note: "Max I/O performance mode — highest aggregate IOPS for thousands of concurrent clients" },
    { label: "EC2 user data — two key default behaviours", service: "EC2 User Data Facts", note: "Runs as root; runs only on first boot by default" },
    { label: "Container scripts up to 30 min, cron-based, serverless", service: "EventBridge + Fargate Cron", note: "EventBridge Scheduler → ECS Fargate task; no Lambda (15-min limit), no EC2 cron" },
    { label: "EC2 app takes 3 min to boot — minimize stop/start time", service: "EC2 Hibernate", note: "Hibernate saves RAM to EBS — resumes instantly, skipping re-initialization" },
    { label: "Auto Scaling scale-in: which instance is terminated first?", service: "AS Default Termination Policy", note: "Most-instances AZ → oldest launch config (legacy) before launch template → billing hour" },
    { label: "Replicate new on-prem NFS files to EFS over Direct Connect", service: "DataSync NFS → EFS via Direct Connect", note: "DataSync agent → private VIF → PrivateLink EFS endpoint; scheduled incremental sync" },
    { label: "RDS storage growing unpredictably, minimum effort fix", service: "RDS Storage Auto-Scaling", note: "Enable storage auto-scaling with a max threshold — zero app changes, transparent" },
    { label: "💰 Design Cost-Optimized Architectures", isHeader: true, color: "#1A7A4A" },
    { label: "Private subnet EC2 accessing S3 cheaply", service: "VPC Endpoint vs NAT", note: "Gateway Endpoint for S3/DynamoDB — free, no NAT" },
    { label: "Cheapest model for steady vs bursty workloads", service: "Instance Types", note: "Reserved (steady) · Spot (fault-tolerant) · Savings Plans (flexible)" },
    { label: "On-prem NFS workload scaling to cloud with auto-tiering", service: "File Gateway Migration", note: "File Gateway (NFS mount) + S3 Lifecycle (auto-tier) — no app changes" },
    { label: "On-prem Windows apps need SMB file shares backed by cloud storage", service: "File Gateway (not FSx)", note: "File Gateway = on-prem SMB/NFS → S3 backend. FSx for Windows = Windows file server IN AWS for cloud workloads. Key: where are the apps? On-prem → File Gateway. In AWS → FSx for Windows." },
    { label: "S3 vs EBS vs EFS cost for same file", service: "S3 vs EBS vs EFS Cost", note: "S3 < EFS < EBS — EBS charges provisioned GB not used GB" },
    { label: "Move on-prem backups to cheapest AWS archive via Snowball", service: "Snowball → Glacier Deep Archive", note: "Snowball → S3 bucket + lifecycle to Deep Archive on day 0" },
    { label: "Cheapest way for EC2s across multiple accounts to communicate privately", service: "Cross-Account VPC Sharing via RAM", note: "Share VPC subnets via RAM — no peering charges, no TGW fees" },
  ],
};

const tabs = Object.entries(data).map(([key, val]) => ({ key, label: val.tab }));

export default function App() {
  const [tab, setTab] = useState("networking");
  const [selected, setSelected] = useState(null);
  const [view, setView] = useState("cards");
  const [search, setSearch] = useState("");
  const [subTab, setSubTab] = useState(null);

  const list = data[tab].items;
  const compare = compareData[tab];
  const tabSubTabs = data[tab].subTabs || [];

  const searchResults = search.trim().length > 0
    ? Object.entries(data).flatMap(([key, val]) =>
        val.items
          .filter(svc => !svc.isHeader)
          .filter(svc => {
            const q = search.toLowerCase();
            return [svc.name, svc.full, svc.tagline, svc.analogy, svc.when, svc.pricing, ...(svc.keyPoints || [])]
              .some(f => f?.toLowerCase().includes(q));
          })
          .map(svc => ({ svc, tabLabel: val.tab }))
      )
    : null;

  return (
    <div style={{ fontFamily: "var(--font-sans)", maxWidth: 1400, margin: "0 auto", padding: "16px 12px" }}>
      <div style={{ marginBottom: 10 }}>
        <input
          type="text"
          placeholder="🔍  Search services, concepts, keywords…"
          value={search}
          onChange={e => { setSearch(e.target.value); setSelected(null); }}
          style={{
            width: "100%", boxSizing: "border-box", padding: "8px 14px",
            borderRadius: 10, border: "1.5px solid var(--color-border-secondary)",
            fontSize: 13, outline: "none", background: "var(--color-background-secondary)",
            color: "var(--color-text-primary)",
          }}
        />
      </div>
      <div style={{ display: "flex", gap: 5, marginBottom: 12, flexWrap: "wrap" }}>
        {tabs.map(t => (
          <button key={t.key} onClick={() => { setTab(t.key); setSelected(null); setSearch(""); setSubTab(null); }}
            style={{
              padding: "5px 10px", borderRadius: 20, border: "1px solid var(--color-border-secondary)",
              background: tab === t.key && !search ? "var(--color-text-primary)" : "transparent",
              color: tab === t.key && !search ? "var(--color-background-primary)" : "var(--color-text-secondary)",
              cursor: "pointer", fontSize: 11, fontWeight: 500,
            }}>
            {t.label}
          </button>
        ))}
        <div style={{ display: "flex", gap: 5, alignItems: "center", marginLeft: "auto" }}>
          {["cards", "compare"].map(v => (
            <button key={v} onClick={() => setView(v)}
              style={{
                padding: "5px 10px", borderRadius: 20, border: "1px solid var(--color-border-secondary)",
                background: view === v ? "var(--color-background-secondary)" : "transparent",
                color: view === v ? "var(--color-text-primary)" : "var(--color-text-tertiary)",
                cursor: "pointer", fontSize: 11,
              }}>
              {v === "cards" ? "Cards" : "Compare"}
            </button>
          ))}
        </div>
      </div>

      {tabSubTabs.length > 0 && (
        <div style={{ display: "flex", gap: 5, marginBottom: 10 }}>
          {tabSubTabs.map(st => (
            <button key={st.key} onClick={() => { setSubTab(subTab === st.key ? null : st.key); setSelected(null); }}
              style={{
                padding: "4px 12px", borderRadius: 20, border: "1px solid var(--color-border-secondary)",
                background: subTab === st.key ? "#E8593C" : "transparent",
                color: subTab === st.key ? "#fff" : "var(--color-text-secondary)",
                cursor: "pointer", fontSize: 11, fontWeight: 500,
              }}>
              {st.label}
            </button>
          ))}
        </div>
      )}

      {searchResults ? (
        <div style={{ display: "flex", gap: 16, alignItems: "flex-start" }}>
          <div style={{ flex: 1, minWidth: 0 }}>
            {searchResults.length === 0 ? (
              <div style={{ color: "var(--color-text-tertiary)", fontSize: 13, padding: "20px 0" }}>No results for "{search}"</div>
            ) : (
              <>
                <div style={{ fontSize: 12, color: "var(--color-text-tertiary)", marginBottom: 10 }}>{searchResults.length} result{searchResults.length !== 1 ? "s" : ""}</div>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(180px, 1fr))", gap: 10 }}>
                  {searchResults.map(({ svc, tabLabel }) => (
                    <div key={svc.name + tabLabel} onClick={() => setSelected(selected?.name === svc.name ? null : svc)}
                      style={{
                        border: `1.5px solid ${selected?.name === svc.name ? svc.color : "var(--color-border-tertiary)"}`,
                        borderRadius: 12, padding: "11px 13px", cursor: "pointer",
                        background: selected?.name === svc.name ? svc.lightBg : "var(--color-background-secondary)",
                        transition: "all 0.15s", position: "relative",
                      }}>
                      <div style={{ fontSize: 9, color: "#aaa", marginBottom: 4, fontWeight: 500 }}>{tabLabel}</div>
                      <div style={{ display: "flex", alignItems: "center", gap: 7, marginBottom: 5 }}>
                        <span style={{ fontSize: 17 }}>{svc.icon}</span>
                        <span style={{ fontWeight: 600, fontSize: 13, color: svc.color }}>{svc.name}</span>
                      </div>
                      <div style={{ fontSize: 10, color: "var(--color-text-tertiary)", marginBottom: 3 }}>{svc.full}</div>
                      <div style={{ fontSize: 11, color: "var(--color-text-secondary)", lineHeight: 1.4 }}>{svc.tagline}</div>
                    </div>
                  ))}
                </div>
              </>
            )}
          </div>
          {selected && (
            <div style={{ width: 320, flexShrink: 0, position: "sticky", top: 16, borderRadius: 14, border: `1.5px solid ${selected.color}`, background: selected.lightBg, padding: "15px 17px", maxHeight: "calc(100vh - 32px)", overflowY: "auto" }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
                <div>
                  <span style={{ fontSize: 20 }}>{selected.icon}</span>
                  <span style={{ fontWeight: 700, fontSize: 15, color: selected.color, marginLeft: 8 }}>{selected.name}</span>
                  <div style={{ fontSize: 11, color: "#666", marginTop: 2 }}>{selected.full}</div>
                </div>
                <button onClick={() => setSelected(null)} style={{ background: "none", border: "none", cursor: "pointer", fontSize: 15, color: "#999", flexShrink: 0, marginLeft: 8 }}>✕</button>
              </div>
              <div style={{ marginTop: 11 }}>
                <div style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888", marginBottom: 5 }}>The analogy</div>
                <div style={{ fontSize: 12, color: "#333", lineHeight: 1.6, fontStyle: "italic" }}>"{selected.analogy}"</div>
              </div>
              <div style={{ marginTop: 11 }}>
                <div style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888", marginBottom: 5 }}>Key facts</div>
                {selected.keyPoints.map((p, i) => (
                  <div key={i} style={{ fontSize: 12, color: "#444", marginBottom: 4, display: "flex", gap: 6 }}>
                    <span style={{ color: selected.color, flexShrink: 0 }}>•</span> {p}
                  </div>
                ))}
              </div>
              <div style={{ marginTop: 9, background: "rgba(0,0,0,0.04)", borderRadius: 8, padding: "8px 11px" }}>
                <span style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888" }}>Use when: </span>
                <span style={{ fontSize: 12, color: "#333" }}>{selected.when}</span>
              </div>
              <div style={{ marginTop: 7, background: "rgba(0,0,0,0.04)", borderRadius: 8, padding: "8px 11px" }}>
                <span style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888" }}>💰 Pricing: </span>
                <span style={{ fontSize: 12, color: "#333" }}>{selected.pricing}</span>
              </div>
            </div>
          )}
        </div>
      ) : subTab === "ebs" ? (
        <div>
          <p style={{ fontSize: 13, color: "var(--color-text-secondary)", marginBottom: 12 }}>EBS volume type comparison — key specs and differentiators at a glance.</p>
          <div style={{ overflowX: "auto" }}>
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
              <thead>
                <tr style={{ borderBottom: "2px solid var(--color-border-secondary)" }}>
                  {["Type", "Category", "Max IOPS", "Max Throughput", "Multi-Attach", "Boot Vol", "Use Case", "Price"].map(h => (
                    <th key={h} style={{ textAlign: "left", padding: "8px 10px", color: "var(--color-text-secondary)", fontWeight: 600, fontSize: 11, textTransform: "uppercase", whiteSpace: "nowrap" }}>{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {data.storage.ebsCompare.map((row, i) => (
                  <tr key={row.type} style={{ borderBottom: "1px solid var(--color-border-tertiary)", background: i % 2 === 0 ? "transparent" : "var(--color-background-secondary)" }}>
                    <td style={{ padding: "9px 10px", fontWeight: 700, fontFamily: "var(--font-mono)", fontSize: 12, color: row.category === "SSD" ? "#2E86AB" : "#1A7A4A", whiteSpace: "nowrap" }}>{row.type}</td>
                    <td style={{ padding: "9px 10px" }}>
                      <span style={{ fontSize: 10, fontWeight: 600, padding: "2px 8px", borderRadius: 10, background: row.category === "SSD" ? "#EBF5FB" : "#EAFAF1", color: row.category === "SSD" ? "#2E86AB" : "#1A7A4A" }}>{row.category}</span>
                    </td>
                    <td style={{ padding: "9px 10px", fontFamily: "var(--font-mono)", whiteSpace: "nowrap", color: "var(--color-text-primary)" }}>{row.maxIOPS}</td>
                    <td style={{ padding: "9px 10px", fontFamily: "var(--font-mono)", whiteSpace: "nowrap", color: "var(--color-text-primary)" }}>{row.maxTP}</td>
                    <td style={{ padding: "9px 10px", textAlign: "center", fontSize: 15 }}>{row.multiAttach ? "✅" : "❌"}</td>
                    <td style={{ padding: "9px 10px", textAlign: "center", fontSize: 15 }}>{row.bootVol ? "✅" : "❌"}</td>
                    <td style={{ padding: "9px 10px", color: "var(--color-text-secondary)", fontSize: 12 }}>{row.use}</td>
                    <td style={{ padding: "9px 10px", fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--color-text-tertiary)", whiteSpace: "nowrap" }}>{row.price}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
          <div style={{ marginTop: 12, fontSize: 12, color: "var(--color-text-secondary)", background: "var(--color-background-secondary)", borderRadius: 8, padding: "10px 14px", lineHeight: 1.9 }}>
            <span><strong>Multi-Attach:</strong> io1 and io2 only — up to 16 EC2 instances in the <em>same AZ</em> simultaneously. All others: single-instance.</span>
            <span style={{ margin: "0 10px", color: "var(--color-border-secondary)" }}>|</span>
            <span><strong>Boot volumes:</strong> SSD only (gp2, gp3, io1, io2). HDD (st1, sc1) cannot boot.</span>
            <span style={{ margin: "0 10px", color: "var(--color-border-secondary)" }}>|</span>
            <span><strong>gp3 vs gp2:</strong> gp3 sets IOPS and throughput independently of size — always prefer gp3.</span>
            <span style={{ margin: "0 10px", color: "var(--color-border-secondary)" }}>|</span>
            <span><strong>io2 vs io1:</strong> same price, io2 has 4× more IOPS and 99.999% durability — always prefer io2.</span>
          </div>
        </div>
      ) : view === "compare" ? (
        <div>
          <p style={{ fontSize: 13, color: "var(--color-text-secondary)", marginBottom: 12 }}>Match your situation to the right service.</p>
          <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
            <thead>
              <tr style={{ borderBottom: "1px solid var(--color-border-secondary)" }}>
                {["Your situation", "Use", "Why"].map(h => (
                  <th key={h} style={{ textAlign: "left", padding: "8px 10px", color: "var(--color-text-secondary)", fontWeight: 500 }}>{h}</th>
                ))}
              </tr>
            </thead>
            <tbody>
              {compare.map((row, i) => row.isHeader ? (
                <tr key={i}>
                  <td colSpan={3} style={{ padding: "14px 10px 5px", fontWeight: 700, fontSize: 12, color: row.color || "var(--color-text-primary)", borderBottom: `2px solid ${row.color || "var(--color-border-secondary)"}` }}>{row.label}</td>
                </tr>
              ) : (
                <tr key={i} style={{ borderBottom: "1px solid var(--color-border-tertiary)", background: i % 2 === 0 ? "transparent" : "var(--color-background-secondary)" }}>
                  <td style={{ padding: "9px 10px", color: "var(--color-text-primary)" }}>{row.label}</td>
                  <td style={{ padding: "9px 10px" }}>
                    <span style={{ fontWeight: 600, color: "var(--color-text-primary)", fontFamily: "var(--font-mono)", fontSize: 12 }}>{row.service}</span>
                  </td>
                  <td style={{ padding: "9px 10px", color: "var(--color-text-secondary)" }}>{row.note}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ) : (
        <div style={{ display: "flex", gap: 16, alignItems: "flex-start" }}>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(180px, 1fr))", gap: 10 }}>
              {list.map(svc => svc.isHeader ? (
                <div key={svc.name} style={{ gridColumn: "1 / -1", padding: "14px 4px 6px", borderBottom: `2px solid ${svc.color}`, marginBottom: 2, marginTop: 8 }}>
                  <span style={{ fontWeight: 700, fontSize: 13, color: svc.color }}>{svc.name}</span>
                </div>
              ) : (
                <div key={svc.name} onClick={() => setSelected(selected?.name === svc.name ? null : svc)}
                  style={{
                    border: `1.5px solid ${selected?.name === svc.name ? svc.color : "var(--color-border-tertiary)"}`,
                    borderRadius: 12, padding: "11px 13px", cursor: "pointer",
                    background: selected?.name === svc.name ? svc.lightBg : "var(--color-background-secondary)",
                    transition: "all 0.15s",
                  }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 7, marginBottom: 5 }}>
                    <span style={{ fontSize: 17 }}>{svc.icon}</span>
                    <span style={{ fontWeight: 600, fontSize: 13, color: svc.color }}>{svc.name}</span>
                  </div>
                  <div style={{ fontSize: 10, color: "var(--color-text-tertiary)", marginBottom: 3 }}>{svc.full}</div>
                  <div style={{ fontSize: 11, color: "var(--color-text-secondary)", lineHeight: 1.4 }}>{svc.tagline}</div>
                </div>
              ))}
            </div>
          </div>

          {selected && (
            <div style={{ width: 320, flexShrink: 0, position: "sticky", top: 16, borderRadius: 14, border: `1.5px solid ${selected.color}`, background: selected.lightBg, padding: "15px 17px", maxHeight: "calc(100vh - 32px)", overflowY: "auto" }}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
                <div>
                  <span style={{ fontSize: 20 }}>{selected.icon}</span>
                  <span style={{ fontWeight: 700, fontSize: 15, color: selected.color, marginLeft: 8 }}>{selected.name}</span>
                  <div style={{ fontSize: 11, color: "#666", marginTop: 2 }}>{selected.full}</div>
                </div>
                <button onClick={() => setSelected(null)} style={{ background: "none", border: "none", cursor: "pointer", fontSize: 15, color: "#999", flexShrink: 0, marginLeft: 8 }}>✕</button>
              </div>
              <div style={{ marginTop: 11 }}>
                <div style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888", marginBottom: 5 }}>The analogy</div>
                <div style={{ fontSize: 12, color: "#333", lineHeight: 1.6, fontStyle: "italic" }}>"{selected.analogy}"</div>
              </div>
              <div style={{ marginTop: 11 }}>
                <div style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888", marginBottom: 5 }}>Key facts</div>
                {selected.keyPoints.map((p, i) => (
                  <div key={i} style={{ fontSize: 12, color: "#444", marginBottom: 4, display: "flex", gap: 6 }}>
                    <span style={{ color: selected.color, flexShrink: 0 }}>•</span> {p}
                  </div>
                ))}
              </div>
              <div style={{ marginTop: 9, background: "rgba(0,0,0,0.04)", borderRadius: 8, padding: "8px 11px" }}>
                <span style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888" }}>Use when: </span>
                <span style={{ fontSize: 12, color: "#333" }}>{selected.when}</span>
              </div>
              <div style={{ marginTop: 7, background: "rgba(0,0,0,0.04)", borderRadius: 8, padding: "8px 11px" }}>
                <span style={{ fontSize: 10, fontWeight: 600, textTransform: "uppercase", color: "#888" }}>💰 Pricing: </span>
                <span style={{ fontSize: 12, color: "#333" }}>{selected.pricing}</span>
              </div>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
